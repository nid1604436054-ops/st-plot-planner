# 设计文档：产品理念与领域模型

> 本文是剧情规划器的「意图源头」：每个核心概念的身份、边界、消费者与生命周期都记在这。
> 材料 = 产品作者 2026-08-25 的口述草稿与三轮问答（桌面「简单的开发者文档草稿.txt」「依次来回答你的12个问题.txt」「4题.txt」）。
>
> 使用约定：
> - **动任何功能之前先重读本文**。与本模型冲突的实现改动，先改本文再动代码，概念级变更与代码走同一提交。
> - **改码定位先查 docs/CODE_MAP.md**（代码地图：每文件职责、五条核心链路、「改 X 先读 Y」路由；2026-08-30 第十七轮立，缘由＝用户指出「每次一点小改动都要阅读很久」——新增文件/链路/职责变化须与代码同提交更新）。
> - 普通功能改动不进本文，只进 DEVELOPMENT_PLAN §12 变更日志。
> - 条目分两栏：【实战】= 真实聊天里验证过；【意图】= 设计意图待验证。约 12 小时冲刺开发后插件已进入实战使用期（1.0 于 2026-08-27 交付；知识库真机十七轮修订、长线管线真机五轮反馈修至 2026-08-31、监听初步实测过），但「第一次完整战役＋首次三千层大修」仍未发生——**多数条目仍在【意图】栏**，待那两件事后逐条回填（见 §7 台账）。

## 1. 这个产品是什么

一句话：坐在角色扮演聊天之上的**剧情守护层**——替扮演模型记住该记的、看见该看的，替作者管住剧情的方向与质量。

作者原话（产品的全部出发点）：

> 「我在酒馆玩角色扮演……都是在模型的设定和智力内让它更好的扮演角色，更接近真实，更像人，创造出更精彩的剧情。」

三个具体目的（作者原话）：**管理文件、提纯提示词、让扮演更加贴合。**

为什么需要它（作者的全知视角论，三个工具共同的存在理由）：

> 「过去的时间就是过去了，我参与的事情没必要再讨论，我没推动的事情不存在，角色不会自己发生我不知道的事情——换句话说，我是全知的。哪怕约会一千次，约会的流程也只取决于模型固定的创造力，而不会有新的变化。」

RP 的世界里没有「镜头外发生的事」，一切新鲜感必须被显式制造——这是随机事件、路人反应、剧情规划三个工具的立身之本。

## 2. 核心概念

### 2.1 一行记忆 = 对角色行为有影响的事件

不是「一条记录」。判断一行记忆价值的标准只有一个：**它会不会改变角色接下来的行为**。镜像、打标、召回的一切设计都为这个标准服务。

### 2.2 双读者模型：原表与镜像

插件里有两位读者，各看各的数据，**分叉是有意的**：

- **扮演模型**（主对话）看**原表**——记忆增强表格插件的数据；
- **规划/检查模型**（本插件的规划向导、检查报告）看**镜像**——本插件按聊天维护的副本。

镜像的定位是**规划侧的策展层**：删掉对规划无意义的表（时空记录表、物品表等每轮更新的噪音），让规划模型注意力集中、降本增效。作者原话：「我给扮演模型看的内容和我给规划模型看的内容可以不一样。」

- 日常操作以**删**为主；
- **改单元格**基本不用但保留——它是双读者差异的具象化（有些扮演要注意的细节，规划不需要落实）；
- 边界要说破：镜像里删一行，扮演模型照样「记得」这件事；**修扮演侧的事实只能去源插件改**。本插件对原表唯一的写路径是备份恢复（只增不改）。

### 2.3 行的身份 = 内容指纹

镜像行的身份 rid = hash(表uid + 列名 + 内容)。这是自然键身份：**内容一变，身份即换**——被改写的行会以「新行」姿态重新进来，旧标签、已读标记、删除墓碑都认不出它。

- 日常修正只发生在表尾两三行（最新进展），指纹漂移影响的只是本来就在变动的行。【实战：这是作者长期使用源插件的真实习惯，先于本插件存在】
- **大修**（每约三千层一次）：合并改写压缩 + 删旧行，覆盖近三千层新增内容。指纹雪崩（标签作废、墓碑失效、满屏新行）在这个场景**真实会发生**。作者接受代价并把处置纳入大修流程本身：**大修后人工核验一遍 + AI 重新打标**。这是标准流程，不是技术债。【意图：大修流程从未实跑过】

### 2.4 口径：这次给模型带哪些记忆

默认答案（作者原话）：「**越是贴合角色的越要，越是无关的越舍。**」相关性三要素：出场角色、剧情合理不重复、前进方向。

- 单主角卡：全量输入（完整关系成长经历贴合扮演）；
- 群像卡（如十人卡）：**名字标签 + 事件类型标签**组合筛（为此移除过单行 3 标签上限）。

口径是**两层模型**，各管各的：

| 层 | 位置 | 管什么 |
|---|---|---|
| 全局默认层 | 记忆表格页（每表一份） | **召回列**——剔除「角色名/备注」类对模型无用的列（列要留着给源插件显示，只在召回时剔除） |
| 单次覆盖层 | 向导第 1 步 | 表勾选 + 三档位（停用/按标签/常驻全量）+ 标签 + 表尾最新 N 行 |

- 「参与召回」总开关已退役（向导勾选足够）；
- **检查报告口径（已落地）**：第 3 步人工二检**继承向导第 1 步**的表/标签选择（读本对话存的第 1 步勾选，与「按建议重写」同一份）——单人卡全量无感；群像卡只查同类事件重复、无关事件不计入；兜底 = 写指导意见打回。

### 2.5 三工具模型：规划 / 随机事件 / 路人反应

三者不是流水线，是**工具箱**：

- **随机事件**与**路人反应** = 对等的并行工具（作者原话「不是附庸品」），随时单独取用；
- **规划** = 「有多次重试机会的个性定制思维链」——**只规划不扮演**，让规划模型与扮演模型各自能力最大化。

**单元制**（互相干涉的正解；2026-08-25 定稿并落地，取代早期「自动对称读取」草案——作者要控制权，不要自动；2026-08-26 三处修订见下）：两个现场工具的产物 = **单元**（暂存态，每工具名下 ≤3，满了禁止再生成、先手动删）。生成任一工具时可**手动导入**另一工具的单元做材料，规则只有一条：只能导入「加工史里还没有本工具」的单元（加工史 = 单元经过的工具集合，界面显示为左上/右上小标记，左 = 先进的工具）——同工具回流禁、两标满禁，套娃在结构上不可能。导入不消耗（被导入单元原地保留，可再导入重 roll、可自己注入）；导入产物 = 带累积徽章的新单元（如反应卡带 [事件,反应] 两标），原单元本体不动。**导入＝既定方向（2026-08-26 定则，起因：用户报事件单元导进路人反应不起关联——接线本对称，弱在任务指令没把导入当锚点）**：导入的单元按「将要且一定会发生的方向」对待，生成结果必须与它咬合，是硬要求不是可有可无的参考——事件侧顺着导入的反应口径发展（不复写同一件事、不与它矛盾），反应侧围绕导入的事件出卡（显著性、即时口径、余波都按它定；没导入才从最近对话里找）。**导入最多勾一项 + 产物正文自带前因（2026-08-26 E11 定则，起因：用户报多勾无意义、双徽章单元正文只剩最后一道工具的内容——原始单元一删前因就断，注入后模型也不知道「这件事」指什么）**：导入选材最多勾一项（勾了新的自动替掉旧的）；导入产物的正文把源单元内容原样嵌在开头作「前因」小节（存 payload.origin，随分析发送、转隐身注入、正文重算与注入到期逐层重算都带着）——原始单元删了链条也在，模型看到的是「前因＋本工具内容」的完整链条。**单元 = 带标签的纯文本提示词块（2026-08-26 定则，后续一切修改的准则）**：像生产材料一样作为纯粹提示词使用（定则管的是**材料语义**——发给模型的就是拼好的整块文本；卡面显示保留各工具生成结果的原有形态，反应卡的分栏字段展示不在替换范围，同日返工澄清）——工具面板只管生成，入池单元统一在向导第 1 步「插入单元」区查看与操作（勾选随分析发送 / 点单元名看正文 / 转隐身注入 / 删除 / 各组清空键）。**参不参与规划是规划侧的事（2026-08-26 修订）**：入池单元在第 1 步「插入单元」勾选区决定是否随分析发送——工具面板的卡片上没有这个开关（已注入的反应单元灰显不可勾：生效注入会让规划与检查自动附带同一口径，再勾就是同文进两次）；采纳规划后单元池不清空，清理由「插入单元」区各组的清空键手动执行。**生成先出草稿、立卡即定稿（2026-08-26 两轮修订合流）**：两工具的生成（事件：掷骰 / 大模型随机 / 按意见生成；反应：生成反应卡）都先出草稿卡，点「立为单元」才入池（再生成换掉草稿，草稿随聊天存不丢）；事件的走向选项**三选一或不选**，立卡时裁剪——没选 = 选项全砍（只作参考材料）、选了 = 只留那一个，入池后不可改选走向（想换 = 重新生成）。意见路径走大模型：「按意见生成」遵循意见即兴（意见是方向不是剧本）；意见与随机**严格二选一**——意见框有字时掷骰 / 大模型随机 / 导入勾选全部灰置。「大模型随机」无条件把事件库已有条目作为**防复刻清单**随行（2026-08-26「参考事件库」勾选退役）；反应卡由模型顺带给浓缩短标题作单元名（与事件同款）。先后顺序由作者的导入动作天然产生，系统只记录不仲裁（徽章即顺序的显示）。**三个功能（随机事件 / 路人反应 / 规划）共用同一套提示词材料模板**：第 1 步确认勾选一遍，三处生成吃同一份材料（各自的生成任务指令不同，材料部分严格同源）。**注入模板序固定**：剧情 → 事件 → 反应，不给排顺序的旋钮。

### 2.6 一份规划的生命周期

材料收集（表/标签/世界书/玩法/最近对话/指导意见）→ 单元取用（随机事件/路人反应两工具面板里生成与互导——面板只管生成；入池单元在第 1 步「插入单元」区勾选、查看与注入；可全不用）→ 生产草稿 → 审核（人工，或盲盒模式见 2.8）→ 注入生效（深度/角色/过期可配）→ 检查报告（继承向导口径）→ 按建议重写 或 完结归档。

- 历史归档上限 20 条，进行中那条永不丢；
- **跨聊天一切归零**：向导进度、注入、反应卡全是聊天作用域，没有任何东西需要跟到新聊天（作者会在一切稳定后才开新聊天）。

### 2.7 过期语义

- 事件与反应 = 「石子落进水里，要的只是扔下去那一刻的动静」——自动过期无所谓；
- **执行中的剧情注入不许自动消失**——手动管理可接受（「不介意通过增加工作量来提高扮演的稳定性」）。【意图：事件按层数过期从未实战跑过】

### 2.8 盲盒模式（信息差内容）【全部为意图】

适用四场景（作者枚举）：

1. **信息不对称**——牌局、道具选择、角色藏起来待兑现的计划或玩笑；
2. **真相隐藏层**——悬疑、密室逃脱（提前解密失去乐趣，不规划剧情又发散）；
3. **心理博弈张力**——预设节点、结局、情感走向的跌宕起伏；
4. **决策隐藏层**——信息不对等下真实做选择、增强沉浸，开放/多维结局。

解法（作者设计，推翻了助手的三个候选方案）：

- **生成时模型自判是否向作者公开剧情**（对「不公开导致剧情偏离的风险」做判断再决定揭不揭露）；
- 生成时提供「是否要查看剧情」的选项由作者预先选择，决定模型采用什么输出策略；
- 判断可选路径较窄时**预警**（多重兜底）；
- 剧情未执行且作者不知情时出了问题，不叫「事后偏离」——直接**重新设计整体结构 / 走开放结局风险回避**。

界面：**默认折叠 + 劝退提示，想看随时能看**；保密是自律级（本插件的提示词查看器翻得到注入内容，技术上无法对用户自己隐藏，作者确认「没必要做成军工级保密」）。

配套提示词：**强化扮演角色主导故事走向的能力**，把作者从「导演」挪到「观众 + 守护者」——防止被规划文本带偏成强制 OOC。

### 2.9 历史摘要 vs 记忆表格

共用入口，职责不重叠：

- **历史摘要** = 短期剧情的**详写** + 暂存（方便二次修改、防短时间内多次构思无留存）——服务两位数楼层内结束的短规划；
- **记忆表格** = 浓缩的**精华**、事实源——三位数、四位数楼层长线的进度就靠读表。

## 3. 数据生命周期总账

**每聊天数据六块**（chatdata 双层存储：热层 localStorage 毫秒写、完全不碰聊天文件；冷层 settings.json 低频冲写——切聊天/采用剧情/表格合并/关面板/5 分钟兜底；聊天身份 = 角色头像文件名｜聊天文件名，改名/分支会被认成新聊天，用设置页「数据备份与搬家」接回；异常退出最多丢 5 分钟窗口内的改动）：

| 数据块 | 内容 | 谁写 | 活多久 / 怎么清 |
|---|---|---|---|
| memory | 镜像 sheets、原表库快照、标签、已读、墓碑、召回配置、备份列表、清空事故标志 | 记忆表格页操作 + 三类事件的自动同步 | 跟聊天永存；备份超上限丢最旧；源表清空事故触发保护、绝不拿空数据覆盖 |
| story | 进行中剧情 + 历史归档（≤20 条，含检查报告） | 采用 / 完结 / 挂报告 / 删除 | 归档超上限丢最旧的非进行中条目；清历史保留进行中 |
| wizard | 向导进度快照（当前步 + 全部输入 + 生成结果） | 每次重渲染与输入改动 | 确认采用 / 取消向导即清空；分析在途刷新回「分析前确认」重发 |
| picks | 向导第 1 步的勾选口味（跨向导记住） | 第 1 步每次勾选变化 | 跟聊天永存 |
| books | 世界书页「启用」勾选（对话书单） | 世界书页勾选时 | 跟聊天永存 |
| units | 两工具暂存单元池（每工具 ≤3）+ 两工具生成草稿 + 面板意见草稿 | 工具面板只管生成（先草稿）/导入；第 1 步「插入单元」区查看/勾选/注入/清空 | 跟聊天永存；采纳规划**不清池**，清空走「插入单元」区各组清空键（只清本工具名下） |

**全局设置**（settings.json 一整包，1 秒防抖）：API 连接与供应商方案库、搜索配置、检索五参数、指导预设（全局生效）、注入默认参数、事件库 + 维度层 + 掷骰三板块 + 最近掷出、世界书库 + 回收站、注入项、额外存储条目 + 玩法扫描窗口、玩法咨询草案、面板缩放、每聊天冷层 chatData。

**注入项生命周期**：scope=global/chat（chat 项绑定创建时的聊天 id）；按层数过期的注入创建时记下**楼层基线**（楼数 = 已落地的角色回复数，user 楼不计——一层 = 一条角色回复），已过层数 = 当前楼数 − 基线：滑动/重新生成只是替换最后一条回复、楼数不变不吃层，删楼层楼数只减、推导不为负（2026-08-26 B1 拍板，取代旧的「每次生成 age+1」，老注入按已有进度折算基线不跳变）；到期自动撤下（反应类注入同时未手改则逐层换段）；切聊天时撤销外聊注入、重放本聊的。

**旧数据**：曾存聊天文件 chatMetadata 的五块，读取时一次性迁走并从聊天文件删除（聊天文件瘦身）。

## 4. 外部依赖（宿主升级失联时先查这三扇门）

| 依赖 | 用在哪 | 失联症状 |
|---|---|---|
| SillyTavern 1.15.0 事件系统（eventSource / event_types） | 聊天切换 / 新消息 / 消息编辑驱动一切自动逻辑 | 自动同步、注入过期、监听全部静默 |
| 记忆增强表格插件（window.externalDataAdapter） | 读原表 + 备份恢复的唯一写路径 | 同步报「找不到表格插件」/ 恢复失败 |
| 酒馆全局（jQuery / toastr / setExtensionPrompt / extension_settings / saveSettingsDebounced） | 面板、提示、注入、设置持久化 | 整个面板起不来 |

## 5. 功能删除检查单（recallTags 教训——删任何功能前过一遍）

1. rg 全仓追每个消费方（包括提示词文案里的提及）；
2. 旧数据残留：读取时 delete，让下次落盘自然清掉——残留值不能继续悄悄影响行为；
3. CSS 孤儿类；
4. 相邻功能的说明文案是否还提它；
5. DEVELOPMENT_PLAN §12 记录（验证链 + 删除边界）。

## 6. 2.0 长线规划（设计定稿 2026-08-28；监听已落码，其余整节【意图】）

> 状态：监听（6.1）已落码（js/listener.js，2026-08-28）；知识库（6.9）已落码（2026-08-29，真机十七轮修订）；**长线生成管线（6.2 六步）已落码（2026-08-30 第十八轮，js/longform.js＋「长线规划」页签，落码注记见 6.2 末）**。其余 2.0 部分——混合重编、暂停恢复、归档、场景元素卡、动作指导卡——仍零代码，写码须用户明确指示的节奏口径不变。
> 2.0 是什么：1.0 管短期规划（单场景、几层楼内的剧情指导）；2.0 补长线——以「一本书」为尺度的剧情规划，横跨几十到几百层楼，产出指导文本与节点表。但这些产物**不直接注入扮演模型**，全部交给「监听」逐回合执行（6.1）。
> 共享地基：监听与长线管线是同一块地基——**把剧情状态从上下文窗口搬进结构化对象持续维护**。上下文窗口是易失内存（十几万层一满就「忘」），剧情状态是磁盘（推进锚、进度、大纲长期存活）。

### 6.1 监听 = 执行引擎（2.0 头号架构决策）

早期设想把监听排在最后做、可无限期推迟——**已作废**：指导文本与节点表一旦直接注入扮演模型，它看到全图会第一轮就剧透、一口气把后面的剧情赶完。正解是全部指导喂给监听，逐回合判定、逐回合放行微量指导；指导不注入就没法执行，监听是整条执行链的地基，故最先开发。原「先做手动模式保底」的提案随之作废。

**分界线（2026-08-28 用户定案）**：场景元素类内容（状态句，无推进判定需求）直接隐身注入两轮、不经过监听；剧情指导类内容（章文本、节点表、动作指导序列）全部经过监听。

**逐回合循环**：扮演模型每轮输出后立即判定（赶在用户下一次输入前备好指导）→ 拿剧情上下文与当前节点的总结性文本比对 → 判定进度（达成＝「点亮」该节点，进度随之推进）→ 生成下一回合的微量指导 → 隐身注入扮演模型（隐身注入＝不显示为聊天消息、直接进入扮演模型输入的注入方式）。

- **输入（全量、不封顶）**：当前聊天全部未隐藏楼层 ＋ 当前最小剧情单位全文 ＋ 出场角色的世界书条目（不限条数）＋ 两个小附件——下一节点的标题一句（收尾方向用，知名不知戏）＋ 本章已点亮节点的标题串（防重复演绎的索引；全量楼层读入后只剩这一职责）。防剧透边界：以最小剧情单位为界，章文本与节点表的全图不进任何输入。可再附加世界书与记忆表格——监听材料与 1.0 材料面板**共用一套获取逻辑**（用户口径：现在不剥离、不定死，将来要分时做减法即可）。**单位槽来源不限（2026-08-28 新增）**：2.0 管线产物、手动导入、1.0 剧情规划产物直接挂载——1.0 规划视为同等的最小剧情单位、同样分节点（节点化方式与退位语义见 6.8 挂账）。
- **输出（微量指导）**：结构＝目标句＋动作提示；**不设字数上限**——要限制的是监听读取的剧情单位（输入侧），不是输出；一轮多角色且各有负责内容时两三句指导不完是常态，宁详勿简。每轮与已推进内容、进度比对后**重新生成**，措辞随进度变化、不复读。
- **注入通道**：独立注入槽，比 1.0 现有注入更靠近上下文末端；系统便签风格；滚动覆盖（新一轮盖掉上一轮）。与短期剧情规划的注入一般不同时存在，共不共用通道不重要。同轮并存时场景元素卡在前、监听指导在后。
- **频控**：2~3 轮内只要有有效对话（剧情在推进）就不降级——指导内容天然随进度变化，无复读问题；复读只在推进停滞时才成为风险。频控行为统一归**介入强度旋钮**调节（见下），不设固定档位规则。**节点衔接＝用户手动**：最小单位刚执行完、要不要接续下一个单位由用户决定（防剧情过紧、失去自由扮演空间；2026-08-28 拍板：只做手动，不留「自动接续」档）。慢热缓冲保留：刻意慢节奏的回合没有任何节点推进才是正确结果，不算卡死。
- **行为规范**：判节点达成必须引用具体楼层内容作证（防「假完成」）；某轮决定不发指导必须留痕说明原因（防无声跳过；留痕面板滚动留存默认 50 轮、设置可调）；判定增设第三态「卡死」——出现时不自动微调规划，升级用户人工拍板。
- **指示器与旋钮**：偏离提醒＝红点（常驻指示器，点开看问题与指导建议），亮在酒馆「魔法棒」菜单的剧情规划器条目处——写码前先在真实界面核对魔法棒面板的注册与角标机制，勿凭想象写选择器。设置项＝两轴旋钮：**达成判定松紧**（多严算偏离）＋**介入强度**（指导发多勤多强，接管全部频控行为）。
- **监听模型**：设置页**固定项**（2.0 其余生成都是每次自选模型，唯监听例外），默认＝设置里第一个副API（规划模型）供应商方案。成本立场：逐轮判定交给便宜模型或主流模型轻量版本，用户对监听成本无容忍上限；注意全量楼层输入下单轮 token 随聊天长度增长，实际开销以实测为准。工作难度定位（用户校准、勿夸大）：多数回合只是轻量比对，真正难点三处——分级判定（偏多少算达成；「圆不回来」是对未来剧情的反事实推测）、微量指导的现场生成（推剧情、不剧透、不抢跑三线走钢丝）、边缘回合（戏中戏的「假装完成」、用户发的脱离角色元对话、刻意慢节奏——没有推进才是对的，不得误报卡死）。
- **三级降级路径**：单次失败→坏输出回传重发一次，仍败→本轮静默跳过并留痕（**绝不复用过期指导**）；连续 3 次失败→「监听失联」提醒＋暂停监听；API 彻底不通→挂起等用户处理。任何降级都不得滑向「把全图直接注入扮演模型」的盲指挥；进度账（6.4 两本账）不因监听故障变动。
- **无长线规划时也跑**：轻量的脱离角色检测＋文风重复检测（便宜模型），结果逐轮留存可回看（帮作者改善文风）；「这段值得开个规划」的反向提醒**暂缓**（判断标准未定）。正常运行不设「暂停监听」开关——知情时跟着走或手动改即可，异常停机只走三级降级。执行中发现规划本身不合适、或作者主动跳出角色→换一种提示方式（与偏离红点区分）。

### 6.2 长线生成管线（六步）

1. **骨架**：输入楼层总数与材料（1.0 材料面板自选＋世界书条目自选＋「本次长线想法」输入框＋保底楼数），产出全书压缩大纲，只到「卷」级措辞；各卷楼数总和由插件校验，不让模型自行提议密度分布。
2. **切块**：切成部分/卷，只分配楼层预算、不产执行节点（文本未生成，完成标准没有对照物）；子块至少 2、不设上限。
3. **分块具体化**：逐块生成卷级详细文本，内嵌「推进锚」（预先埋设的剧情检查点；每卷至少 2、一般每章 1-2 个——2026-09-01 用户拍板，锚跟章走，原「每卷至少 4」作废）；锚是阶段级里程碑（比章和节点粗至少一级），锚与锚之间标注「本锚间自由演绎」；展开程度匹配楼层预算；卷文本站「事件与关系」层＋**台词硬禁令**（不得出现台词原话与整句对白、动作不逐拍描写——台词与动作细节属章层；第二十五轮，起因＝实测卷文本写到角色具体说什么）；**写法定调两头锁（第二十六轮，起因＝卷宗二极管摆动：要么浓缩成一笔带过、情节没了、章只能照抄，要么又写得比章还多）——「禁的是写法精度、不是情节本身」：每件具体情节一件不少都在、每件写到「发生了什么、导致什么」收笔；一笔带过禁令（多件事揉成一句总括不许当正文——没写明的事件等于不存在）；「更详细」＝多排事件、不是给事件加台词与分镜**。
4. **拼接**：各卷文本拼成完整大纲。
5. **审阅改**：整体审阅、修改。
6. **再切小**：卷切成章（一章至少 20 层楼、不设上限，2026-08-28 定；原 10-20 区间作废）、章内切出节点（带可对照的完成标准，每章至少 3 个），一步到位；推进锚是切章的刀口；章文本必须重写、不得照抄或缩抄卷文本（比卷细一级——台词与动作细节在章层允许；第二十五轮），卷层一句话的事件在章层展开成这场戏怎么发生的过程（展开是重写不是复述；第二十六轮），章文本按【节点 N】行分段与节点一一对应（节点行之间的正文＝该节点的演绎区间——挂载判定表与正文看得见挂钩；nodes 数组缺行时从文本行兜底解析）；节点判据站全书粒度最细一级、从对应段正文提炼具体事件（言语写「说出了什么」不引原话），空话措辞（气氛变好/关系推进类）与相邻判据同义反复都禁（第二十六轮，起因＝卷层浓缩后节点跟着说车轱辘话）。

之后的执行交给监听（6.1）。种子（新角色/伏笔）在规划阶段显式列出，「是否要新角色」做成选项；已完成部分不喂摘要——实际进度认进度账（6.4），规划对象只保留大纲与走向。

**落码注记（2026-08-30 第十八轮，随同次交付；自主裁决待用户过目，均可驳）**：

- 落点：js/longform.js（数据层＋四份管线提示词＋六步编排）＋js/ui/tabs/tab-longform.js（「长线规划」页签，页签序在监听之后设置之前）＋监听侧放行（listener 单位来源新增 `longform`、lfRef 随单位走）＋css。数据在 chatdata 的 longform 块（书-卷-章-节点＋进度账＋挂载记录，按聊天走）。
- **①骨架＋②切块合并为一次调用**：模型一次出「卷数组（标题/概要/种子/楼数）」，楼数算术全在本地——总和≠楼层总数按比例重配＋最大余数法分尾差，重配后任一卷低于一章下限（20 层）即判失败并指路（加楼数或重跑）；「不让模型自行提议密度分布」落成「模型给结构、插件管算术」。
- **③具体化与⑥再切小都是逐卷并行**（一次一卷、单卷失败不拖垮其余、可整批重试只补失败卷）；每批的材料与全书骨架块只拼一次、逐卷共享同一字符串（同一前缀，走前缀缓存的服务商只付一次全价）。
- **④拼接是纯展示**（卷文本就地可看可编辑，无独立拼接产物）；**⑤审阅改＝「按意见修订全书」一次调用整书重出（意见必填，长线不设「换一版」档——要换方向走「重新生成骨架」）＋卷文本就地手改**；修订/手改后已切的章表标「过期」（textAt＞splitAt），重切时按章位置沿用旧章的点亮进度。
- 材料口径（**第十九轮 2026-08-30 修订**，起因＝用户实测反馈「沿用 1.0 的勾选就不能单独选世界书和知识库；记忆表格想要默认全量、不要标签」）：长线页**自备材料面板**（照「剧情指导」第 1 步复制、去掉标签列）——记忆表格一个勾「全量附带（全部表格的全部行）」**默认开**（不带档位/标签/最新行数）；玩法条目勾选（未动过时默认跟随生效中条目）；世界书自选＝独立悬浮面板，勾选**与 1.0 分开存**（长线勾长线的）；知识库清单逐张勾选——勾中的清单**整表可用条目随行**（冷却中跳过；不分抽样/全量、不抓取不轮换），材料小节按清单分组带硬口径「凡涉及该清单领域的内容必须从条目里选用、不得自拟同类」，用户想法点名了具体内容的以点名为准；长线侧**不结冷却**（冷却账只属于向导「确认采用」流，§6.9）。勾选存 longform 块的 mats（作废本长线时保留——偏好不是书数据）。**仍不带**：插入单元、近期草稿骨架、联网搜索——要给长线吸收的内容写进「本次长线的想法」框。
- 换算锚与监听共用设置页「监听」区的有效推进区间（settings.listener.progressMin/Max，一处设置两侧同源）；楼层总数缺省兜底 120（§6.3「用户不填用默认值兜底」的具体取值，助手默认）。
- 生成模型＝页内「单次选用」下拉（主连接或供应商方案，会话内记住上次选择），每步生成都可换；输出上限乘数＝骨架 ×2、卷文本/修订/切章 ×3（设置 0＝不限则保持 0），不引入新设置项。
- 挂载与接续：章卡「挂载」→ mountChapter 挂进监听单位槽（单位 source='longform'、unitId 记在章上重挂不换身份；退位槽占用时监听侧照旧拒绝）；「接续下一章」只在当前章演完（节点全点亮）后放行，否则指路监听页「标记达成」——节点衔接只做手动（§6.8）的界面落法。进度账的持久方是 longform 块（监听每轮判定后 syncLfProgress 把 nodeIdx 写回章的 lit/done；单位被卸下/顶掉账不丢）。
- 四份提示词（骨架/具体化/审阅改/再切小）按 §6.7 结构要点全新重起草（旧 v3 全文失传，不冒充已审原文），全文随交付报告送审；§6.11 的硬约束组（时间顺序/事实一致性/user 不可编排/点名硬要求/通则一）以长线版收进四份提示词常驻。
- **第二十轮（2026-08-31，起因＝用户首轮实测五条反馈）**：
  - **骨架可就地编辑**：卷卡新增「编辑骨架」（卷名/楼数/概要/种子四字段＋「删除本卷」两步确认）。楼数改动后**楼层总数＝各卷楼数之和**（总数跟着卷走，不再只认参数表单里那个数）；删除挂载中卷被拒、删前卷后挂载索引顺移；切过章的卷改楼数⇒章预算与卷预算不一致⇒该卷回到「未切章」待重切（旧章表保留、重切按位置沿用进度，文本过期标不变）。概要/种子在卷文本定稿后改过⇒卷卡提示「卷文本没跟着动」。自主裁决：编辑入口常驻所有阶段（执行期也允许改——改的是产出文本账，进度账不动）。
  - **生成中实时状态**：四步生成全部改走流式（api.chatCompletion 的 onDelta，与向导运行页同管道）——busy 横幅实时显示已收字数；批量步（具体化/再切小）另显「已完成 X/Y 卷」、卷卡徽章随每卷落定就地重刷（不整页重渲；用户开着骨架编辑/卷文本编辑时跳过重刷防丢输入，修订意见草稿会话内留底）。
  - **锚密度与预算挂钩**：「至少 4 个、不设上限」补上密度口径——一般每 25-35 层楼一个锚、不逐层设锚（锚是章级检查点不是逐层检查点），每卷建议数按预算注入任务文本（max(4, 预算÷30 四舍五入)）。起因：24 层卷被模型排 24 锚。
  - **章数上限随卷预算注入**：切章任务明说本卷最多切 ⌊预算÷20⌋ 章、预算只够一章时整卷作一章不硬拆；骨架提示词同步补「每卷一般 40-60 层、全书建议切 N-M 卷」。起因：120 层被切成 5 卷×24 层，每卷只装得下一章。
  - **切章失败报错大白话化**：报数（本卷预算 X、模型切了 N 章、N 章至少需要 N×20 层、本卷最多几章）＋两条指路（重试让模型少切／卷卡「编辑骨架」改楼数），不再只说「有 3 章低于 20」。
  - 竖排反馈经查第十九轮护栏已在且选择器优先级正确，判定为旧构建未强刷——本轮顺带补文本输入框护栏（编辑骨架的卷名框），复验指引里再点名强刷。
- **第二十一轮（2026-08-31，用户真机截图推翻上面那条判定）**：截图里能看到「编辑骨架」按钮（第二十轮才有的新功能），说明跑的就是新构建——「旧构建未强刷」判定作废，竖排问题从来就没修过。真根因：`.pp-item` 是「左标题右按钮」的横向 space-between 列表行样式，卷卡借了这张皮但内容是复杂块，凡没带 `flex:1 1 100%` 基准的子块（第二十轮新加的编辑骨架表单、编辑卷文本框、概要/种子框）都不换行，被 space-between 甩到卡片右侧与按钮行并排，框宽塌到内在宽度（约 250px 的窄竖条）——前两轮的宽度护栏根本没碰到这个失效模式。修复：卷卡（`.pp-lf-vol`）单独改纵向堆叠＋子块占满整行，标题行改横排（与知识库行同款）；后续往卷卡里加任何块不再需要自带 100% 基准。教训入册：**同一个页面「看着对了」不等于级联对了——卷卡从第十八轮起标题/按钮就一直横着挤在首行，只是当时没有不带基准的子块，问题潜伏到第二十轮加编辑表单才爆**。
- **第二十二轮（2026-08-31，用户点名「编辑框完全折叠不起来」）**：卷文本编辑框此前唯一的出口是「保存卷文本」——不想保存就关不掉；「卷文本」预览开关管的是另一个独立展开键，点了它编辑框纹丝不动，两者还能同时显示。修复三件：①编辑框按钮行新增「取消」（内容没动一键收起；改过内容两步确认「确认放弃？」、4 秒回退，防手滑丢字——「删除本卷」同款 armed 模式）；②打开编辑器时只读预览自动收起（同一份文本不开两份视图）；③编辑器开着时「卷文本」按钮暂时隐藏（保存/取消后回来）——编辑期间离开编辑框只有「保存」与「取消」两条路，不再有第三个不管用的开关。
- **第二十三轮（2026-08-31，用户点名「这几个文本不能叠在一起」＋「按意见修订全书没影响到文本」）**：两个问题。①叠层：卷卡三个展开块（只读预览 / 卷文本编辑 / 骨架编辑）各有独立展开键、互不相干，开两个就叠着显示——第二十二轮只做了「开编辑器收预览」单向一条线。修复：统一走 `toggleVolSection` 互斥切换，同一时间最多开一块，开新的先收旧的；三颗区块按钮全部常驻不因状态隐藏（第二十二轮的「编辑中藏卷文本按钮」作废——按钮忽隐忽现正是用户嫌「杂乱」的来源），再点同一颗＝收起（卷文本编辑的收起走取消的脏检查两步）；切换别块时正开着的编辑块若有没保存的改动先拦一道（`veDirty`/`skDirty` 脏检查，防静默丢字），干净状态才切。②修订静默白跑：`runLfRevise` 写回循环里「该卷空文本＝保留原文」的兜底在**全部卷都空**时一路静默跳过、最后照弹「全书已按意见修订」成功——「生成正常但文本没变」的直接来源。修复：逐卷计数（改了 / 原样带回 / 没给正文），全部没给正文＝硬报错（提示可能被输出上限截断、已保留原文不动），提示语按实际计数如实报告；原样带回的卷不再刷新 `textAt`（章表不白标过期）。用户同轮提出「重新生成没有按卷档」——明确暂缓（用户原话「第三个问题可以先不管」），入积压清单待拍板。
- **第二十四轮（2026-09-01，用户三大反馈：页面拉太长/楼数均匀不能调/锚与注释滥用；次轮补充拍板修订下沉每一步＋参数材料折叠＋输入框样式与交互＋busy 信息＋容错）**：**页面重构（tab-longform 整体重写）**——顶部操作条（生成骨架/具体化各卷/按意见修订/再切小四按钮常驻、没到的阶段置灰悬浮指路＋生成模型下拉随行）；参数与材料收进两个折叠区（生成前展开、骨架落定后收起，折叠头一行概览）；卷列表一行一卷、点开后**三页签切换**（骨架/卷文本/章与节点——同一附属区切着看）；章卡从页底「执行总览」搬进各卷页签，执行区只留当前章＋接续＋一行总进度。**修订下沉每一步（用户拍板「每一步都能用大模型改或手动改」）**：新增骨架整书修订（`runLfSkeletonRevise`：不增删卷、楼数可改重配到当前总数）、单卷骨架修订（`runLfVolSkeletonRevise`：模型拿全书骨架当上下文只改一卷、楼数改后总数跟各卷之和）、单卷卷文本修订（`runLfVolTextRevise`：一次只出一卷全文——整书修订撞输出上限时的治本替代）、单卷带意见重切（`runLfVolSplit`：意见注入切章任务、已切过的卷可原地重切沿用进度）、章文本手动编辑（挂载中改文提示卸下重挂才生效、进度保留）；整书「按意见修订」按钮随阶段改目标（骨架阶段改骨架、有卷文本后改卷文本）。**楼数分配禁平均（用户点名「均匀分配＝废话卷与不够卷」）**：骨架提示词撤销第二十轮「每卷一般 40-60 层」带子（它把差异压到 ±20%），改「每卷至少 20 层、大小按剧情体量分配、禁止近似均分」＋概要末尾一句体量理由；**锚口径（用户拍板）**：下限 4→2、密度「一般每章 1-2 个」（建议数＝建议章数＋1）、写法改「阶段级里程碑」——一句话只说推进到哪个阶段、概括向卷概要看齐、禁写到动作/对话/可核对细节精度（那是节点的事）。**骨架编辑交互**：默认只读展示、点「编辑骨架」才出表单（与卷文本同款）；表单输入框补 text_pole 主题样式（修白底框）。**busy 横幅增强**：补模型名·用时（每秒走表）·思考字数（onReasoning 计数，`lfCall` 透传）＋批量完成数与已收字数。**容错补账（用户点名问「中途报错，前面生成的内容保不保得住」）**：批步骤本就逐卷落袋（成功即 persist、重跑只补未完成卷、不覆盖已成的——本轮在报错 toast 里明说）；「重新生成骨架」此前一按就清空旧书、新骨架失败即两空的缺口补上——`stashLfRegenBackup` 把旧书整份存进 longform 块（刷新不丢），生成失败/中断 `restoreLfBackup` 自动恢复、成功才作废备份。**注释清理**：删 stage=none 流程说明整段/材料区「另自动随行」行（并入折叠头悬浮）；各区说明行转悬浮；⚠ 功能性警告保留压短。离线台 87→104 项全绿（3 条锚密度旧口径断言随本轮改写＋新增 ⑬ 组 20 条：锚数值/禁平均措辞/三档修订/单卷重切意见注入与原地重切/备份恢复整份快照/整书骨架修订重配与卷数拒绝）。
- **第二十五轮（2026-09-01，用户复验两反馈：①按钮该在材料/参数下面；②卷文本仍写台词原话、章文本照抄卷文本、章与节点无挂钩——附卷宗对照实证：卷 1500 字含整句台词、章 1300 字几乎照抄）**：**操作条挪位**——第二十四轮放在页面最顶的操作条（四步按钮＋生成模型下拉）挪到参数/材料两个折叠区下面、卷列表上面（用户原话：实际编辑卷时每次都要略过那两行、增加滑动工作量）；三处「顶部」指路文案随之清理（改「点上面的…」/「操作条第一颗按钮」——按钮不在顶部，指错方向比没有方向更糟）。**信息层级三层落死**：①卷文本站「事件与关系」层——**台词硬禁令**（禁引号对白/「某某说：『……』」式整句台词/逐拍动作，对话一律概括成「谁与谁谈了什么、谈出什么结果」；旧软口径「不写台词级细节」被实测判无效——知识库轮「约束要么硬要么别写」教训在长线重演），两档卷文本修订提示词（整书/单卷）同步带禁令，修订不把台词带回来；②**章文本必须重写、禁照抄/缩抄卷文本**——比卷细一级（场景怎么开、节拍顺序、关键行动怎么做、关键台词要点，在章层允许），卷层被抽走的台词与动作细节自然成为章层的增量（治「章比卷还短」的照抄病根——卷不再有台词后，章想照抄也没得抄）；③**节点内嵌挂钩**——章文本按「【节点 N】节点名——完成标准」独立行分段（与卷文本【锚 N】同一套设计语言），节点行之间的正文＝该节点的演绎区间、执行时按顺序点亮即进入下一段；nodes 数组与文本行一一对应、两边都要给全，数组缺行时 `nodesFromText` 从文本行兜底解析（镜像 `anchorsFromText`——节点是监听的判定表，丢了挂载就没得判；兜底进 `runLfSplitOne` 的章映射，不再整卷报「节点少于下限」）。离线台 104→113 项全绿（新增 ⑭ 组 9 条：台词禁令措辞/禁照抄/节点挂钩条款/两档修订带禁令/nodesFromText 解析/切章兜底全链/挂载单位带兜底节点/操作条模板顺序/顶部指路清零）。
- **第二十六轮（2026-09-01，用户复验纠偏：主要病根在卷宗「二极管」两头摆——要么浓缩完具体情节没了只一笔带过、落到章只能照抄，要么又写得比章还多；节点车轱辘话是下游症状）**：**卷宗写法定调两头锁（detailSystemPrompt）**——「禁的是写法精度、不是情节本身」：每件具体情节一件不少都在、每件写到「发生了什么、导致什么」收笔（不写台词怎么说、不写动作怎么分解）；**一笔带过禁令**（「众人各怀心思地散去」「随后冲突进一步升级」这类多件事揉成一句的总括不许当正文——切章模型只能从写明的事件里切章排节点、没写明的事件等于不存在）；**方向锁**（「更详细」的方式永远是多排事件、不是给事件加台词与分镜——治「比章还多」那头）；两档卷文本修订提示词补「重出全文时未被意见点名的事件一件不少照写、不许顺手浓缩成总括」（防修订顺手把情节揉没）。**章层展开规矩（splitSystemPrompt）**——卷层一句话的事在章层展开成这场戏怎么发生的过程（怎么开场/你来我往/怎么收尾），展开是重写不是复述（薄卷宗下章有得展开，治照抄的第二条腿）。**节点判据细端写硬（同轮先行修、用户定调后保留为下游补强）**——节点站全书信息粒度最细一级、判据从该节点对应段落的正文提炼（言语写「说出了/谈成了什么」不引原话；带精度对照例：卷层写「审问中被审者反将一军」合法、节点判据要落到「出言动摇对方、对方当场失态」）；空话禁令（气氛变好/关系推进/张力加深/初步显露/进一步发展类无可指认事件的措辞不得单独当完成标准）＋相邻判据不得同义反复＋每条判据答得出「演到什么样算这个节点完成」；章文本条补因果句（段里没具体事、判据只能写空话）。教训：**粒度条款只写「变粗」方向、不写「变细」方向＝把浓缩信号一路带到执行层——两端都要写硬，中间档靠两头禁令夹出来**。离线台 113→121 项全绿（⑮组 8 条：定调/一笔带过/方向锁/修订不浓缩/最细一级与提炼/空话与同义反复/展开规矩/素材源因果句）。
- **第二十七轮（2026-09-01，用户实测报「九成提示词命中不了缓存、成本翻 10 倍」，点名查提示词排序）**：三个病根、三处修。①**批量步并发互抢缓存**——具体化/再切小用 `Promise.allSettled` 一口气发全部卷：前缀缓存要等第一个请求跑完才落盘，同前缀的并发请求互相看不到对方还没写完的缓存、全部按未命中计价（R18 注记「同一前缀只付一次全价」的布局对了、发射顺序把它废了）；修＝`warmFirstAllSettled` 先焐热再并行（第一卷跑完才发射其余，读的是已存在的缓存不再互抢；批次总时长多约一卷的生成时间，输入费省大半）。②**七份提示词 system 各不相同＝材料块跨步零共享**——材料+骨架排在互不相同的 system 后面，骨架/具体化/修订/切章每步都把材料整块重付全价；修＝抽 `lfCommonSystem` 公共头（换算锚＋硬约束组五条＋JSON 元规则从七份里搬出常驻，字节级不变），七份任务提示词瘦身成「身份+任务要求+schema」挪进 user 消息排在材料之后；材料再拆**稳定区/会变区**（`lfStableAndVolatile`：「检索命中」「最近对话」开头会变，垫到任务段之后发——前缀断在会变小节自己身上、不再断在材料中间）——跨步共享前缀＝[公共头+材料稳定区(+骨架)]，材料只在状态变化时付一次全价。③**监听楼层放在消息中段**——楼层每轮在末尾追加，排楼层后面的块（角色摘要+检索命中+判定规则+输出契约，上千 token）每轮按未命中重计价，节点推进轮连开头说明块都变＝整发全价；修＝块序重排（说明/单位全文/角色摘要/判定规则/输出契约前置；【当前节点状态】挪到楼层前面——推进轮不再整发全价；楼层→上一轮指导→检索命中→附加材料垫底），角色摘要与世界书检索拆开发（前者跟角色卡走稳定、后者按最近楼层重扫逐轮变，混一段会把稳定的拖下水）。监听提示词文字未改、只挪块（判定任务行加「见下方」指路）。离线台 121→136 项全绿（⑯组 15 条：公共头三查/七份零残留/监听两套块序/焐热次序并发峰值 1/卷间与跨步分歧点实测盖到任务尾巴）。
- **第二十八轮（2026-09-01，用户实测报「每次点按意见修订全书：token 照扣、钱照吃，卷文本页面一点不变」，追问澄清调用真实发生不是没发请求）**：病根＝**整书卷文本修订「一次调用、要求模型把全部卷全文重出」必撞输出上限**——输出上限＝设置 maxTokens（默认 1500）×倍率 3＝4500 token，五卷的书整书全文要 8000+ 字（还不算 JSON 转义与锚）：截断→JSON 解析失败→修复梯子补第二发（上限照旧、还被要求「精简各字段的文字」）——两遍 token 都扣了，捞回的卷要么原样带回要么缺正文，代码按「宁缺勿毁」一处不写：页面纹丝不动。与第二十三轮「生成正常但文本没变」同源（当轮只把全空静默弹成功改成硬报错，没意识到病根是输出上限经济学——R24 在按钮悬浮里写过「长书易撞输出上限」的警告，等于已知此路不通还留着主按钮走它）。修＝**整书修订改逐卷执行**（`runLfRevise` 重写）：每次调用带全部卷当前文本＋整书意见（跨卷意见——把乙卷的事挪到甲卷——两头都落得了）、只要求重出点名的那一卷（单卷体量远在上限内）；先焐热再并行（通则三）＋逐卷落袋互不连坐（失败卷进 failed 带卷号原因、成功卷照写）；会计 updated/unchanged/keptNoText/failed 逐卷报，页面 busy 显示「已完成 N/M 卷 · 已收字数」、每卷落定重刷卷卡（开编辑框时照旧跳过防丢输入）。旧 `reviseSystemPrompt`（整书重出契约）删除、新 `bookReviseVolSystemPrompt`（分卷执行契约：台词禁令/不浓缩/锚同步等口径原样随行）；操作条按钮悬浮说明撤「长书易撞输出上限、撞了改用单卷修订」旧警告。离线台 136→146 项全绿（⑰组 10 条：逐卷调用/焐热峰值 1/卷间分歧点＝本卷任务尾巴/只点名本卷/跨卷可见/落盘正确/失败不连坐/失败卷原文保留/全空硬报错照搬）。
- **第二十九轮（2026-09-01，用户四反馈：①前几轮 API 报错在成功生成后仍挂在页面、点重新生成那一下也不消失；②长时间不吐字后秒数像停了；③报错文案笼统、不带服务商原生报错；④问卷文本页「按意见修订本卷」与操作条按钮是否重复）**：①**报错留存的清账缺口**——页顶「最近一次操作失败」行来自 longform 块的 `error` 字段，旧实现只在骨架生成成功/作废/回参数时清（数据层注释写着「成功后清」但从未实现——具体化/切章/修订成功后旧报错一直挂着）。修＝`startBusy` 开工那一下就清（报错跟着「最近一次操作」走、不跟着历史走：新一轮点下去即消失，成则保持消失、败则由 markErr 写新错）；整书修订的部分失败也照批量步口径写 `error` 留痕（此前只有具体化/切章批次留）。②**静默秒数表针**——busy 计时本身是每秒一拍的 setInterval、不依赖吐字（查证不会停；若整页挂着后台标签页、浏览器会把计时器限到约每分钟一拍，属浏览器行为）；缺的是长时间没吐字时页面毫无表示、看着像卡死。修＝`busyFeedAt` 表针（吐字/思考增量/逐卷落定都算「有动静」），20 秒没动静横幅明说「已 N 秒无新内容」、秒数照跳。③**报错带原生返回**——api.js 两处裸抛收口：流式读流中途断线原先直接把浏览器原文（`terminated` 一类）抛上来到页面，现在包成 ApiError 带上等了多少秒/已收多少字/底层报错原文（AbortError 与思考标头照旧原样上抛不包装）；非流式 200 但内容不是 JSON（网关错误页）原先裸抛 SyntaxError，现在带返回原文前 150 字；整书修订的逐卷失败原因带上屏（toast 与页顶留痕都带，此前只报卷号）。④**按钮查证（不重复，零代码）**——操作条「按意见修订（卷文本）」＝整书：意见下给全部卷、逐卷重出（没被点名的卷原样带回）；卷文本页「按意见修订本卷」＝只改这一卷、一次调用。操作条没有「修订章」因为章层带意见的操作是各卷「章与节点」页签的「按意见重切本卷」；骨架阶段的操作条按钮＝整书骨架修订、单卷骨架修订在各卷「骨架」页签。两颗按钮标签相近确实易混（用户本轮即撞上）——改名提案待用户拍板。离线台 146→149 项全绿（⑱组 3 条：流式中断带等待秒数/已收字数/底层报错、AbortError 不被包装、200 非 JSON 带原文片段；测试台 jsonResponse 桩的 text 字段同时对齐真实 Response——非流式路径改读 text 再本地 parse）。
- **第三十轮（2026-09-01，用户 UI 指导四条＋四张截图：操作条按阶段重排、四项改名、继续按钮「报错才出现」、「按意见修订所有章」入列）**：①**操作条改阶段驱动**（废除第二十四轮「四步按钮常驻、没到的阶段置灰悬浮指路」——四张截图实证各阶段都全排五颗、灰一片）：第一步（没骨架）只有「生成骨架」＋生成模型下拉；之后恒三颗——**从零开始**（原「重新生成骨架」改名、排最前；行为不变＝回参数表单、旧书整份自动备份）→ **按意见修订骨架/所有卷**（原「按意见修订（骨架）/（卷文本）」去括号改名；卷文本没全部齐全前显示骨架档）→ 下一步位（**具体化各卷 → 生成章节〔原「再切小」改名〕→ 按意见修订所有章〔新操作，见②〕**，看推进到哪）；「**继续具体化（未完成的卷）/继续切章（未完成的卷）**」从常驻按钮降为半拉子批次的**恢复位**——只在有卷没跑完时垫在三颗之后、全部跑完自动消失。用户口径「报错才出现、重新生成完内容后消失、再报错再出现」落成**数据驱动**（有卷没跑完才出现）：中途失败/中断才会留下半拉子、跑完即齐——比盯报错文案稳（报错会在点任何生成开工时清，第二十九轮；只看报错的话，跑一次别的操作成功后按钮就没了、没跑完的卷永远补不上）。busy 横幅标签随改名（「按意见修订所有卷中」「生成章节中」）。②**新增「按意见修订所有章」**（`runLfChapterRevise`）：对已切章的卷逐卷带整书意见重切——第二十八轮整书卷文本修订同一套经济学（全部卷文本进共享段＝跨卷意见两头落得了、每次调用只重出点名的一卷——章＋节点全文比卷文本更易撞输出上限〔通则四〕、先焐热再并行〔通则三〕、逐卷落袋互不连坐）；失败卷旧章原样保留并标 error、**重试时 error 态带旧章的卷仍是目标不跳过**；没切过章的卷不进目标（归「生成章节」）；卷文本一字不动（要改卷文本走「按意见修订所有卷」）。章产物校验从 runLfSplitOne 抽成 `settleSplitResult` 共用（批量切章/单卷重切/整书章层修订三处同源）。③**意见框改三目标**（`revKind`：骨架/所有卷/所有章）：点哪颗修订按钮开哪种意见框、框标题与确认键随目标；再点同一颗收起、开着框点另一颗切目标（确认键原「按意见修订全书」随目标拆三）。④**改名与指路扫尾**（用户可见逐条）：重新生成骨架→从零开始、按意见修订（骨架）→按意见修订骨架、按意见修订（卷文本）→按意见修订所有卷、再切小→生成章节、意见框确认键随目标；指路文案随改名五处（章与节点空态「点上面的生成章节」、删卷「要重来走从零开始」、挂载缺节点表「先完成生成章节」、骨架校验「走从零开始重新生成」、单卷修订意见提示）；骨架整书修订提示词内一处按钮引述同步（「要增删整卷走『从零开始』」）。离线台 149→166 项全绿（⑲组 17 条：操作条六状态渲染断言——第一步单按钮/骨架期三颗排序/具体化半拉子恢复位/卷文本齐/切章半拉子/章齐恢复位全消；整书章层修订行为——逐卷焐热峰值 1、跨卷文本可见、分歧点＝本卷任务尾巴、旧点亮进度沿用且卷文本不动、失败不连坐、error 卷重试不跳过、无章硬报错指路、空意见硬报错、无章卷不进目标）。

- **第三十一轮（2026-09-01，用户真机报挂载出口错乱：监听页卸下/丢弃后长线页最下面仍显示「正在执行」、页面没有任何变化；丢弃退位副本后这一章挂载按钮消失、挂不回去）**：病根＝长线块自记的执行位（mount）只在挂载那一下写、从不对账——在监听页卸下/丢弃/顶掉只动监听的账，长线页永远停在旧状态（挂载按钮的显隐也读它，按钮跟着消失）。修＝**syncLfProgress 改对账制**（长线页每次渲染先对账〔renderTab 原本就先调它、只是旧函数只管在岗写回〕＋监听页六个单位槽操作〔卸下/接回/丢弃/接续下一单位/手动导入挂载/1.0 规划挂载〕成功后即时对账）：①活动单位是长线章→进度写回账本、执行位跟随（被接回也能重新认回来）；②退位槽里的长线章→冻结进度抢救进账本（丢弃前先落账，账不丢）；③没有长线章在岗→执行位清空（「执行中」不再假显示、挂载按钮回来）。账本只进不退（旧副本接回不倒账）、重切形状（节点数对不上）不回写防错账。**mountUnit 同章重挂规则**（不造重复副本）：退位槽里同 id 旧副本作废（进度账在长线账本、新副本自带）；挂载中重挂同章就地换新、不进退位槽（编辑章文本后「卸下再挂载」的实际路径）。**监听页配套两件**：单位槽操作成功后就地重渲染（不再等下一轮判定或切页签才刷）；退位槽行移出单位块——卸下后没有活动单位时「接回/丢弃」也够得着（旧行为跟着单位卡一起消失）。离线台 166→180 项全绿（⑳组 14 条：挂载→执行中显示、在岗推进写账、卸下→执行位清空＋进度落账＋执行区回「还没挂载章」、卸下后监听页退位槽行独立显示、丢弃后再挂回且进度带回、同章重挂不占退位槽、被顶掉冻结进度落账、接回执行位重立、旧副本不倒账、重切形状不回写）。

- **第三十二轮（2026-09-01，用户真机报：监听页卸下当前规划后「本轮指导」不撤——下一轮照样注入、页面照样显示，两头都要改）**：病根＝指导有两条命且都只在「下一轮判定」时滚动覆写——**注入槽**（writeSlot 写进酒馆扩展提示位：成功轮滚动覆写、失败轮清空、关总开关/切聊天清空，唯独单位槽操作全都不碰）与**留痕最新一条**（面板「本轮指导」区读它、标题自称「注入槽里当前生效的指导全文」）。卸下/换挂/接回只动单位槽，两条命都留着旧主人的指导：下一次生成照吃旧指导（注入位是真的）、面板照旧显示「生效中」。修＝**指导随主人作废制**：单位槽换主人的三个操作（挂载/卸下/接回）成功即清注入槽＋立作废标记（`guideVoidReason` 存原因：挂载新单位/重挂同一单位/接回退位单位/卸下单位），关总开关与切聊天两个既有清槽点补立标记；丢弃退位槽**不作废**（没换人、在岗指导继续有效）；三轮落账（单位判定/轻量检查/失败）自动解除标记——静默轮也是新轮的新状态。面板「本轮指导」区作废行优先显示（「上一轮指导已随「X」作废：注入槽已清空、下一轮不再注入；等新一轮判定重新生成」），红点区的「指导建议」行同步设闸；防复读输入线（lastGuidance）随作废一并清空（旧措辞对新单位/新模式没有参照意义，同失败轮口径）。**竞态收口**：判定在途时单位槽换主人（监听调用几秒窗口内挂/卸/接回）——轮完成对账主人变了就整轮作废（不写槽、不落账、不点亮、不清作废标记，这轮就当没发生；手动判定弹提示），否则旧单位的判定会给新单位点亮节点、旧指导会在扣住的发送放行瞬间写回槽里。离线台 180→199 项全绿（㉑组 19 条：作废前后对照、卸下/接回/换挂三出口清槽＋标记、丢弃不作废、新一轮落账解标记、关总开关、旧存档缺字段兼容、切聊天源码接线、在途卸下竞态整轮作废、不换人对照轮照常落账——测试台替身升级：setExtensionPrompt 全量留痕〔只记录不改变行为〕、document 补 dispatchEvent）。

- **第三十三轮（2026-09-01，用户三轮追问拍板：重挂后进度从哪续、卸下后给不给扮演模型发「别推进」提示、判定读什么材料、材料能不能看）**：三件套落码。**①回归判定（重挂对账，用户设计）**：重挂有进度的长线章（长线页挂载成功或监听页接回，lit>0＝非首挂）自动跑一次「回归判定」——对照**五章规划窗口**（用户拍板口径：以当前章为中心、卷内前二后二补足五章；前面没有章节就只往后补、后面不足往前补；**卷不足五章整卷判、窗口不跨卷**；reentryWindow）＋全部楼层＋角色摘要＋世界书＋记忆表，回答两件事：**走到哪**（reached＝实际达成节点总数，批量补点亮、只进不退）与**偏没偏**（三档：没偏／偏了但能自然拉回／⚠偏大了——偏大亮红点）。报告只给用户看：监听页新「回归判定」卡（偏离结论／走到哪与补点亮数／对照窗口／大白话总结／楼层作证）＋留痕一条 mode:'reentry'；**不写注入槽、不出指导**（后两章的规划在窗口里，任何指导都可能把后续剧情漏给扮演模型）；不动轮次计数/指导线/作废标记/失败计数（与例行引擎不共账）。零进度首挂与手动单位不触发（没有要对账的账）；总开关关/失联/无楼层时静默跳过；挂载瞬间例行轮在途＝等它落地（旧主人轮会被 owner 对账作废）再跑，回归判定自身在途换主人同样整轮作废（成功失败都一样）。**②材料透明化（用户点名「监听没有可以给我看的文本，这是纯粹的空白」）**：监听页「本轮指导」区新增**「看提示词全文」**按钮——展开最近一次判定（例行轮或回归判定）实际发出的完整提示词（内存留底 lastPromptText，只保最近一份、刷新即清：全文随楼层线性膨胀，进存档会把聊天文件撑翻倍）；每轮留痕落**材料清单**小账（单位/窗口字数、节点位置、角色摘要字数、楼层范围、世界书命中条数、记忆表开关）随卡片显示。**③角色摘要上限 800→20000**（用户点名「我一个角色都不止 800 字」——判定要拿角色卡对照 OOC/事实，800 字等于半瞎；只放宽监听侧 assembleCharSummary，1.0 检查与规划侧不动；材料清单如实报字数、近上限标注可能截断）。**顺手堵漏**：第三十二轮的 owner 对账只盖了成功路径——在途轮**失败**落地时若主人已换，失败留痕与失败计数不再写进新主人的账（catch 分支补同一对账）。离线台 199→233 项全绿（㉒组 34 条：五章窗口四种夹取、首挂/手动单位不触发、契约硬字段违契约即抛、落账只进不退＋偏大亮红点、重挂全链〔自动跑/批量补账/账本跟到 done/注入槽零写入/不动三本账〕、提示词留底内容、材料清单入账、面板报告卡三要素、在途卸下整轮作废、失败留痕不碰失败计数、例行轮失败路径 owner 补漏、三处源码接线）。

- **第三十四轮（2026-09-01，用户对第三十三轮的批评拍板：十人卡两万字卡边、要看要勾不要只看、缓存顺序保楼层垫底）**：监听材料线整组重做，三件。**①角色卡摘要整条退役**（用户明示「我的角色摘要都在我的世界书里面的，我会自己勾选……既然你这里已经有世界书了，那就不用专门搞角色摘要了」）：监听三路提示词（例行单位/轻量/回归判定）删掉「出场角色设定摘要」块；第三十三轮的 800→20000 上限随块一并作废——十人卡体量两万字级、任何写死的上限都是卡边（教训入记忆：体量类数值先按十人卡口径核对）。1.0 检查与规划侧的 characterSummary 不动。**②世界书自选固定进材料（用户设计）**：监听页「本轮指导」区新增**「世界书自选（已勾 N 条）」**按钮开悬浮勾选器（交互照长线页同款：按书折叠、搜索、整书全勾/全清；挂 body 的 pp-ls-float、条目行样式扩到第三作用域）；勾选键存**监听自己的聊天块**（state.lorePicks，旧聊天块自动补空数组；与向导第 1 步/长线页的勾选各管各的，先例＝长线勾选存 longform 块）；复用第七轮 §6.10 的 resolveLorePicks——**勾选即点名，不看关键词/常驻/书与条目的启用状态，整条原文不设上限**；例行判定与回归判定都带，排进提示词**稳定段**（单位全文之后、判定任务之前——跟勾选走、吃前缀缓存）；与每轮重扫的检索命中**自动去重**（自选优先：勾中的条目从 scanLorebooks 结果里让位，同一条不进材料两次；检索开关关掉不影响自选）。**③楼层范围可选**：设置页监听区新增**「监听楼层数」**（floorLimit，默认 0＝全量不动老行为；N>0＝只带最近 N 层角色楼＋其间夹的用户消息，楼层号保持全聊天绝对号、判定引证不受影响；砍楼层伤「走到哪」的引证完整度，默认全量、用户自己权衡）；例行与回归同口径（成功与失败留痕同账）。**缓存顺序重申并写死口径**（用户要求「楼层落在最后面、缓存基本吃满」）：落法＝大块材料里楼层最后——其后只留三个每轮整个重写的小块（上一轮指导/检索命中/附加材料）。理由：前缀缓存按最长公共前缀计价，楼层若排在这三小块之后，指导一换、整段楼层全部按未命中重算，缓存反而吃不满；现状块序已是该最优解，本轮自选条目插入稳定段、楼层数不动位置。离线台 233→254 项全绿（㉓组 21 条：limitFloors 四种取法〔0/超界/等长全量、最近 N 层裁剪＋绝对楼层号、空聊天〕、默认值、旧聊天块补字段、单位轮全链〔自选整条原文进稳定段/撤摘要/楼限标注与裁剪/材料账五项〕、去重两验〔楼限内检索词不在扫描文本、楼限归零后检索词在但条目被勾→让位只进一次〕、面板按钮、回归判定同口径、源码接线四处；㉒组材料账与源码断言随撤摘要更新）。

- **第三十五轮（2026-09-01，用户拍桌「谁告诉你记忆表是小块？我记忆表 4 万多字」）**：第三十四轮块序口径修正——**记忆表格归稳定段**。病根：第二十七轮立块序时把「附加材料」（＝记忆表）归进「每轮都变的小块垫底」，第三十四轮照抄该分类没按用户实际数据复核——记忆表**两头都不成立**：体量可以到几万字（用户实测 4 万+），变化只随记忆更新、不随轮次。排楼层后面的实际代价：楼层每轮追加，前缀缓存在楼层新尾巴处断开，**其后的记忆表每轮整块按未命中全价重算**——4 万字 × 每轮判定。修法：三路提示词（例行单位/轻量/回归判定）删掉尾巴「附加材料（如有）」块，记忆表改为独立块「<记忆表格（既有事件记录；…参考）>」排进稳定材料区（单位全文/自选条目之后、任务之前；assembleExtra 改名 assembleMemory 返回纯内容）；楼层后面只剩**真正**每轮整写的两小块（上一轮指导/检索命中）。记忆更新那一轮前缀在记忆块处断开、楼层全价一次，之后重新吃满——偶发一贵轮换长期缓存价，划算。块序通则立死：**按「大而少变在前、逐轮整写在后」排，不按材料类别贴标签**。离线台 254→255 项全绿（⑯组块序断言改三十五轮口径：记忆表格进稳定段、附加材料尾巴块零残留；㉓组补全链记忆块位次断言）。

- **第三十六轮（2026-09-01，用户四点点名：一件即修、三件待拍板）**：①**悬浮窗按钮竖排（已修）**——世界书自选悬浮窗「展开/收起」按钮被挤成一个字一行：铁则 1 的 `.menu_button` 横排规则只写了 `.pp-drawer`/`.pp-viewer` 双作用域，挂 `body` 的 `.pp-ls-float` 吃不到（铁则 5 的坑以按钮形态复发）；选择器补 `.pp-ls-float` 根治（DEVELOPMENT_PLAN §13 立铁则 6：新挂 body 的悬浮窗必须同步把作用域类补进铁则 1 选择器）。②**功能按钮不与生成文本混排（用户立规，修复方案待拍板）**——「看提示词全文」「世界书自选」两按钮现塞在监听页「本轮指导」文本区（R33/R34 所为）。③**轮号语义（待拍板）**——「本轮指导（第 N 轮）」的 N＝本聊天例行判定累计次数（回归判定不计数）；卸下/切聊天只作废指导不清轮号，作废行挂在「第 N 轮」标题下语义矛盾。④**材料控制分家（待拍板）**——「附带世界书检索」「附带记忆表格」开关与「监听楼层数」在设置页、世界书自选按钮在监听页，材料控制两处找不齐；记忆表目前全量带入、无条目级挑选（1.0 第 1 步有现成挑选机器未接监听侧）。

- **第三十七轮（2026-09-01，用户拍板：判定材料区两套页签＋记忆挑选器照第 1 步＋轮号按甲案；开工前九条假设全默认通过）**：①**判定材料区**（监听页最下面——「功能按钮不与生成文本混排」立规的落地）：两套页签各自独立、按聊天存——「日常监听」管每轮例行判定（单位轮＋轻量轮共用），「重挂对账」管重挂有进度的章那一刻、用户开口对话前自动跑的一次性回归判定（时机沿用第三十三轮、只换材料来源）。选择范围相同四样：世界书自选（两页各有勾选器，重挂单存 state.matReentry.picks）、记忆表格挑选器、世界书检索开关、楼层数（两页各一，重挂默认 0=全量）。②**记忆挑选器照向导第 1 步同款克隆**（memModes 档位停用/标签/常驻＋memTags 标签 chips＋memRecent 表尾最新行——buildMemoryContext 同一套参数）。**用户定则（通则立死）：后续一切提示词材料输入都在全量版本上做减法——一套挑选机器，不做每板块单独算法**。③**引擎两路各读各的单**：例行读 state.matRoutine，回归判定读 state.matReentry；旧全局 withLorebook/withMemory/floorLimit 第一次见到聊天块时播种带走、全局键随即删除（E14 残键纪律；withMemory=false 播成现有表全停用映射，新表后来默认常驻——与第 1 步同口径）。材料账 memory 字段改记字数（全停用＝0；旧布尔留痕面板兼容显示）。④**轮号甲案**：「本轮指导」标题不再带「（第 N 轮）」（N＝本聊天例行判定累计数、回归不计数；卸下/切聊天只作废指导不清轮号）；轮号挪到留痕行「已判定 N 轮」、指导作废期间隐藏。⑤**按钮搬家＋设置页撤三控件**：「看提示词全文」进留痕行、「世界书自选」进判定材料区、「本轮指导」区只留纯文本；设置页「监听楼层数/附带世界书检索/附带记忆表格」撤走留指路牌（改名清单随交付报告）。离线台 255→276 项全绿（㉔组 21 条：迁移播种三键带走即删、形状收敛、两路独立性〔日常关检索/停记忆/楼限 vs 重挂默认全开全量〕、挑选器标签档/表尾窗/常驻、轮号甲案与作废隐藏、按钮搬家、面板材料区、源码接线；测试台教训：测「升级后首见」型迁移不能靠置空＋persist——listenerState 内部的播种会在 persist 那一下当场重播，须先摆好旧键再删字段）。
- **第三十八轮（2026-09-01，用户拍板：给关键词激活单做一枚楼层数、与正文的分开、差异写悬浮注释；开工前核验确认「按对话里出现的关键词激活」＝已落地的世界书检索，轻量/单位两模式共用一套）**：①**scanFloors 新字段**——matRoutine/matReentry 各加「关键词扫描层数」（0＝全聊天〔播种默认〕、N＝只回看最近 N 层角色楼＋其间用户消息，limitFloors 同一机器）；「楼层数」继续只管「剧情上下文」正文带几层。②**引擎解耦**——例行轮与回归轮都改为正文窗口、扫描窗口各裁各的：楼层收集一次（allFloors）、两窗口分别 limitFloors，扫描文本 formatFloors(limitFloors(allFloors, scanFloors)) 喂 scanLorebooks。**行为变化**：第三十四至三十七轮扫描文本跟着正文楼层数走，此后默认全聊天——以前靠楼层数顺手收窄激活范围的，改填「关键词扫描层数」。③**界面**——判定材料区「楼层数」旁并排「关键词扫描层数」输入框（两页签各一）；楼层数/关键词扫描层数/世界书检索勾选框三处悬浮注释互相指路说差异。三份判定提示词零改动（变的只是检索命中块的内容来源窗口）。离线台 276→283 项全绿（㉕组 7 条：默认补 0、形状收敛〔小数取整/垃圾归零〕、双向解耦〔正文限 1＋扫描全量→窗外关键词仍激活；正文全量＋扫描限 1→窗外不激活、常驻条照常〕、面板双输入框并排、例行与回归源码接线）。

### 6.3 楼层与预算

- **换算锚**：一层楼＝一条角色回复（口径同 §3 注入项）；一轮总输出约 1000 字、其中约 200-600 字回应用户输入，真正推剧情的「有效推进」在一个区间内估量（用户输入推动越少越近下限；两端做设置项，400/800 是用户举例的示意默认值而非实测事实——用法＝当成区间综合衡量，不做固定数值换算，2026-08-28 澄清）。所有涉楼提示词带换算锚「一层楼≈有效推进字数区间」（区间两端读设置）——「楼层」是酒馆玩家行话，API 模型不懂。
- **预算管线**：楼层数是最先输入的参数，切块时分配一次、再切小时再分配一次；用户不填用默认值兜底。楼层只是生成参数，不做「进度太慢」类节奏警报。
- **预算可手改（第二十轮）**：切块的分配结果不是死的——卷楼数在长线页就地编辑（卷卡「编辑骨架」），楼层总数随之＝各卷之和；改完切过章的卷要重跑「再切小」重配章预算。
- **预设不传导、允许溢出**：各预算单元的楼层一经分配即固定，写超不向后传导（例：10 层分 3+3+4，第一个单元实写 5 层，超出的 2 层只算它自己的事，其余单元与总数不变）。
- **书-卷-章-节点四层**：全书分卷、卷分章、章内排节点；每一层都有继续细切的权利。

### 6.4 关键机制

- **节点**：最小剧情单元，文本块带可逐条对照的完成标准；达成口径＝「角色动作偏向某目标即算达成」（直接写进提示词）。
- **两本账**：**产出文本账**（未执行的规划内容，混合时随便重写）＋**进度数据账**（哪些节点已完成——正常路径是监听判定点亮，用户的显式操作也可改；混合重写与再生成不得动它）。
- **伏笔＝注定任务**：不做独立台账。骨架阶段点名后挂到具体节点成硬规则，完成判定并入该节点的完成标准；执行失败＝报错交用户处置（回滚重埋或整条删除）。
- **打碎重混（操作名「混合」）**：用户显式发起——把新导入的指导材料与当前剩余规划交给规划模型打碎重编成一份新规划，两根进度条合为一根；已完成节点冻结进历史，新规划只覆盖剩余剧情、只重写当前最小单元。混合时给模型看的既有剧情＝「校准窗口」，按 1.0 材料管线口径取数，窗口大小数值可调、设 0＝全部未隐藏内容可见。**服装/场景类不参与混合**（分界＝是否需要节点推进，不是材料体量）。任何指导材料都可导入打碎重编（动作指导卡、1.0 规划产物、路人反应单元、随机事件单元共用一套组装模板）；每次混合都带「本次想法」指导框，其内容＝最高优先级。
- **暂停恢复**（短期剧情规划与长线的交互）：用户往当前剧情注入短期剧情规划时，长线进度自动暂停（界面提示「注入当前剧情会暂停长线进度」）。指导结束后两档处置——**A** 按卷级单位（书-卷-章-节点第二层）对照判断影响、输出**影响系数**：影响不大→从上次进度继续；影响大→提醒用户、由用户重新规划剧情。**B** 直接把当前最小长线单位与刚做的剧情规划打碎混合（同「混合」操作）。影响评估的对照问题清单与评估提示词待起草送审（评估调用哪个模型随之一并定）。
- **归档＝注入前过滤**：防重复不在注入层下「别重复某某」的指令——直接下禁令反而诱发重复（白象效应）——而是在注入前把用过的内容过滤掉。构成：**整套记录**（整套服装/造型用过就整套装档）＋**单品禁令**（标志性单品永不再用）＋**场景桶**（场景分桶存放；重复同场景外出必须备注「特地选了和上次一样的」，新颖互动绝不复用）。各角色各自建档、互不封锁；条目涉及哪些角色由模型生成时自标。2026-08-29 定：归档与知识库用后账**共用一套记账设计**（见 6.9）；服装跨清单组合（一套服装的多个元素来自多张清单）用过的**组合整体入账**、可固化为**固定搭配**有意复用——组合机制细节随服装库落地时设计。
- **场景元素卡**：描述当前场景/服装状态的「状态句」，直接隐身注入两轮后自动过期（两轮保证加载：卡是种子，上下文里长出来的描写才是活状态）。历史卡滚动留存默认 5 份（设置可调，2026-08-28 定；原 3-5 区间作废），超出清最旧；留存档与归档数据分库。换场景才生成新卡；没有卡时 1.0 记忆表格的服装列兜底。不参与混合、不经过监听。
- **动作指导卡**：只指导标志性动作——每个角色一组有序的「目标动作序列」（目标有序，序列即节点序列）；至多 10 个角色同场、输出写长写细；一轮连贯动作偏向任一目标即算该目标达成；建议楼数由模型给范围而非定值。经过监听执行。
- **两套规划并存**：1.0 短期规划与 2.0 长线规划并存，混合重编不默认偏向哪套。

### 6.5 API 与模型

- **主API＝扮演模型；副API＝规划模型**（用户固定叫法，勿叫反）。
- 2.0 每次生成从设置里已保存的供应商方案**单次选用**模型，不影响当前正在使用的模型；长线产物一次要用很久＝质量优先选贵模型，速度优先只属于短期临时物。**唯一例外：监听模型是设置页固定项**（6.1）。
- **思考开关分家（2026-08-30 第十七轮，用户点名「这一轮就做你说的开关区分」）**：设置页「关闭思考」总开关只管**生成侧**（规划分析/第二遍/检查报告/随机事件/路人反应/AI 打标建库/联网判断/玩法咨询）；**监听侧恒关思考**——api.chatCompletion 新增按次覆盖的 `thinkingOff` 参数，监听发调点（listenerAttempt）恒传 true、不吃总开关。理由：监听每轮都跑、开了思考成本会爆炸；规划要聪明可以自己开思考——此前一个总开关两边同进退，要规划开思考就得陪监听一起开。端点拒绝关闭参数时重试梯子照走（§6.12 第八/九轮），全被去掉时 toast 如实报。
- **用户环境（2026-08-30 用户告知，基本不会变）**：**DeepSeek 官方 API**——关闭思考方言以 deepseek 官方口径（`thinking` 字段）为第一优先，其余三家（Qwen/OpenAI 系/vLLM）是兼容层；中转/OpenRouter 私有方言暂不做，用户将来换供应商时报证据再补。
- 模型返回的 JSON 全走 1.0 现成的 parseModelJson 解析；解析失败把坏输出回传重发。

### 6.6 存储与界面

- 归档与长线数据**按聊天分开存**（什么剧情放什么聊天里）；知识库条目全局共享。
- 2.0 界面一律**单开新页签**（细节指导、长线各自独立），方便调试，最后再议合并。
- **知识库**：设计定稿见 6.9（2026-08-29 三轮问答收口）。要点：自制不收编第三方、题材现阶段只做恋爱、条目全局共享；清单只喂剧情规划类生成，扮演模型注入一概不碰（原「单轮背景注入」用途作废、「注入时条目由用户手动挑」升级为清单级手选＋条目级抓取）。

### 6.7 提示词通则与结构要点

> ⚠️ 七份提示词（管线四份＋两张卡＋混合重编）曾起草到第三版并经用户逐项审改，但全文只存在于已结束的旧对话记录里、**已无法取回**。以下只是结构要点；将来落全文必须按要点重新起草、**交用户重新审，不得冒充已审原文**。
> 落码进度：**管线四份（骨架＋切块合并/分块具体化/审阅改/再切小）已随第十八轮（2026-08-30）重起草并落码，全文随交付报告送审**；其余三份（混合重编/场景元素卡/动作指导卡）仍待起草。
> 另：监听自身的提示词不在七份之列（监听架构反转晚于七份定稿），从未起草过，属全新起草、同样须用户审（已随监听落码并过三轮反馈审定）。

- **通则一·示例即上限**：一切数量要求写成「至少 N、不设上限」并显式说明无上限；例句一律标注「意思模板＋授权自行措辞」——便宜模型会照抄示例数量、逐字复读示例措辞。
- **通则二·每次调用自包含**：每份提示词都是无状态的独立请求；共享说明块（单元定义、背景语境）由插件拼装时完整注入每一份，绝不假设模型记得上一环节。
- **通则三·前缀缓存块序（2026-09-01 第二十七轮立，长线与监听先行）**：服务商前缀缓存从请求第一个字节起逐字节匹配，第一个变化字节之后全部按未命中计价。铁律四条——跨步共享的东西放最前且字节不变（长线公共头 lfCommonSystem：换算锚/硬约束组/JSON 元规则；各步任务要求排在材料后、放 system 里＝材料每步重付全价）；会变的小节垫底（检索命中/最近对话/上一轮指导/楼层——放中间会把它后面的稳定块拖下水）；同前缀的批量调用**先焐热再并行**（前缀缓存要等第一个请求跑完才存在，一口气全发＝互抢未建成的缓存全部按未命中计价）；监听的楼层放消息末段只垫每轮都变的块（节点状态比楼层稳定、放楼层前——推进轮不再整发全价）。
- **通则四·输出体量上限（2026-09-01 第二十八轮立）**：一次调用要求模型**重出**的全文体量必须装得进输出上限（设置 maxTokens × 步骤倍率；默认 1500×3＝4500 token）——「整书全文重出」类任务在多卷书上必截断：截断→修复梯子补第二发（上限照旧）→两遍钱照扣、捞回的原样/缺正文、一处写不进＝页面不动（用户两轮实测同一症状）。要求全文重出的任务按最小独立单元拆开逐个调用（整书修订拆逐卷：上下文照给全部、输出只要一卷）；拆的是输出、不是上下文——通则二「每次调用自包含」不受影响。
- 各份要点：**骨架**＝换算锚＋卷级措辞＋插件校验楼数总和＋楼数按剧情体量分配禁平均＋概要带体量理由（第二十四轮，撤销第二十轮的「每卷一般 40-60 层」均匀带子）＋卷数建议随总数；**切块**＝换算锚＋子块至少 2；**分块具体化**＝推进锚＝阶段级里程碑（一句话只说推进到哪个阶段、禁细节精度）＋每章 1-2 个＋锚间自由演绎＋展开度匹配预算（第二十四轮口径）＋台词硬禁令与「事件与关系」层（第二十五轮）＋写法精度与情节数量分家：情节一件不少禁一笔带过、加详细＝多排事件（第二十六轮治二极管摆动）；**再切小**＝章至少 20 层＋每章节点至少 3＋楼层不传导＋伏笔挂节点＋章数上限随卷预算注入（⌊预算÷20⌋、只够一章不硬拆，第二十轮）＋章文本重写禁照抄＋【节点 N】内嵌挂钩（第二十五轮）＋卷层事件展开成过程＋节点判据从段正文提炼、禁空话与同义反复（第二十六轮）；**审阅改**＝整书/单卷两档×骨架/卷文本两层（第二十四轮起「每一步都能按意见修订或手动改」；整书卷文本档第二十八轮改逐卷执行——整书全文重出必撞输出上限，见通则四）＋单卷带意见重切；**场景元素卡**＝状态句正文＋防复读备注句（意思模板写法）；**动作指导卡**＝角色×目标动作序列＋达成口径＋楼数给范围；**混合重编**＝材料端统一导入＋输出七字段一个不能少（新章文本/节点表/楼层推荐/材料消化/变更说明/伏笔处理/校准备注——逐项交代每份材料用到哪了，防偷懒防静默吞材料）。

### 6.8 数值终审与挂账（数值已于 2026-08-28 用户终审）

- **数值已定（2026-08-28）**：推进锚每卷下限 4（**2026-09-01 用户修订：下限 4→2、密度改「一般每章 1-2 个锚」、锚写法改阶段级里程碑——原 25-35 层一锚判太密、锚细到跟节点没区别**）／一章至少 20 层楼、不设上限（原 10-20 区间作废）／有效推进＝区间综合衡量、两端设置项（400/800 为示意默认值，非实测）／场景元素卡滚动留存默认 5 份、设置可调（原 3-5 区间作废）／监听留痕面板滚动默认 50 轮、设置可调。
- **唯一未点名**：每章节点下限 3——终审时用户未逐条点名，按提案值当默认执行，要改一句话的事。
- **行为已定**：节点衔接只做手动、不留「自动接续」档（2026-08-28 拍板）。
- **后期调整（用户预告，不阻塞开工）**：推进锚数量从固定下限 4 改为「生成前用户可选，或让模型遵循默认配置/自行判断」——**2026-09-01 部分落地**：下限降 2＋密度与写法口径改定（见上条），「生成前用户可选」档未做。
- **知识库**：设计定稿（2026-08-29，见 6.9），**已落码**（同日，落码注记见 6.9 末）；冷却默认值等挂账仍开。
- **待起草**：场景元素卡、动作指导卡、混合重编三份提示词全文＋影响评估对照问题清单——均须用户审（管线四份〔骨架/具体化/审阅改/再切小〕已随长线落码全新起草、按「未点名＝过审」规则过审，见 §6.2 落码注记——原「七份待起草」已消四份）。监听两份提示词（主执勤 v1.1＋轻量 v1）**不在此列**：三轮反馈审定后已随代码落地（js/listener.js，后续改词仍须过审）。
- **新增功能挂账（2026-08-28）**：1.0 剧情规划可挂载进监听、视为同等最小单位——①节点化方式：**已按提案 A 先行落地**（规划 beats 行直接当节点、完成标准＝阶段内容发生即偏向达成；解析不出编号行退回单节点），待用户实测判定质量，不行再上 B（挂载时自动节点化转换）；②退位语义：**已按提案先行落地**（被顶下的单位进退位槽、进度账原样冻结，接回/丢弃由用户在监听页签操作；退位槽只一个、占用时拒绝新挂载防数据静默蒸发）——两条均**未正式拍板，按提案默认执行**。
- **监听落码注记（2026-08-28，自主裁决待用户过目）**：①总开关默认关（防意外计费，设置页「监听」区与监听页签都有开关）；②手动导入单位先做成单节点（整个文本一块判，2.0 管线产物自带节点表后只是测试入口）；③单位末节点点亮后自动转轻量执勤（「停住等手动接续」落在节点层不再判定、检查层继续跑）；④红点＝魔法棒「剧情规划器」条目上的自建小圆点＋监听页签问题区（自建 DOM，未动酒馆内部）；⑤排队闸＝捕获阶段拦截 #send_but 点击与 #send_textarea 回车，放行时补一次真实点击（真实酒馆下的拦截效果属实测项）；⑥每轮调用超时 90 秒、重试一次、排队闸硬上限 240 秒（提案值）；⑦监听输入附加材料只挂了世界书检索＋记忆表格两样（玩法条目/进行中剧情/历史摘要未挂，做减法原则下从简）。
- **旧概念防回流**（用户桌面的原始构思文件写于研讨前，再遇旧方案先对照本条）：监听最后做→已反转为最先；先做手动模式保底→作废；骨架让模型自议各卷密度→已撤（插件校验）；原始文件里的「章」≠四层结构里的「章」；长线指导拼接进注入层直接给扮演模型→否决（剧透＋两根进度条无法收束）；按材料体量分层处理→否决（分界是类别不是体量）；伏笔四态台账→被「伏笔＝注定任务」取代；「监听判定不准是架构级风险」→已降级为设计细节（最难的剧情构思已由规划模型在管线完成，监听只做轻量比对）。

### 6.9 知识库（设计定稿 2026-08-29；已落码 2026-08-29，见末尾落码注记）

**定位＝反模型偏好**：模型在「约会去哪、消费什么」这类内容选择上有训练分布尖峰——换模型、重 roll 也只在几个常见选项里打转（用户 1.0 起实测确认，2026-08-29 重申）。知识库把候选集合整个换掉：从模型自带的常见几样换成用户自建清单。清单**只喂剧情规划类生成**：先只接 1.0 规划向导；随机事件/路人反应不接（两者依赖上下文生成、不依赖客观知识）；扮演模型注入一概不碰（原「单轮背景注入」用途 2026-08-29 作废）。与世界书页签并存两套、不合并：世界书＝关键词/常驻检索式材料（现状），知识库＝轮换随机抓取式生成素材（轮换制见下）。

**清单与条目**：

- 类目开放自建（地点、约会活动、消费、情趣道具、服装、行为指导……随建随加——行为指导库存放模型不生成、甚至不在训练范围内的动作）；现阶段只做恋爱题材；条目全局共享、不绑聊天不绑角色；自制不收编第三方。
- 每张清单**自定义表头**：导入时用户定这张清单有哪些字段（「名字＋说明＋标签」只是其中一种，字段名可任意定）；结构化调用照表头把粘贴文本填成条目（走副 API，从设置里供应商方案**单次选用**，与其他生成一致）；抓取按条抓。表头定死在导入时、**永不做事后迁移**——用户留存原始文本，重导即重建，永远不算高成本。
- 内容生产流程＝用户在外部用提示词批量起草 → 粘贴进插件 → 模型照表头结构化 → 用户审后入库。

**抓取机制**（挂在剧情指导页签第一步「材料」）：

- 清单勾选放材料步（与玩法勾选、插入单元同区）；「抓取」做成按钮＋**悬浮面板**（与随机事件/路人反应现场工具同款交互）——面板里展示抓到的条目，用户看得见，单条可踢、可整把重抓。**用户是最后一道闸**：觉得不合理的内容进不了生成层。
- 每张清单一个**投喂方式**属性（2026-08-29 真机第七轮加，总纲「全量分两种」的礼物侧）：**抽样**（默认，即原行为）＝轮换抓一小把让模型挑；**全量**＝整表条目全部随分析发给模型、由它从中挑——适合礼物这类「整张候选表都该在场」的清单。全量清单**不抓取不重抓、不消费轮换队列**（冷却中的条目同样跳过），面板显示「全量 N 条·约 X 字」、单条可踢（踢＝本次不发）；「采用方式」二选一在全量下不适用（整表都在场，只剩可踢）。冷却链路照旧复用（自报选用→确认采用时结算，挑中的进冷却）。**采用口径（第十五轮统一改硬，用户拍板）**：无论全量还是抽样——规划凡涉及该清单领域的内容必须从发送的条目里选用、不得自拟同类（抽样只发一小把，这一把就是该领域本次的全部候选面；抽样的软口径「优先从中选用、没覆盖的方向可以自拟」实测管不住模型按偏好自拟选材，随第十五轮废止）；唯一例外＝用户构思或修改意见里点名了该领域的具体内容时以点名为准、不硬换成清单条目（模型须在 risks 里如实说明因点名未用清单）。材料小节里抽样分组逐张带清单名（多张同场时「必须从中选」按领域对号入座）。投喂方式新建清单时定、清单展开区可改。
- 每张**抽样**清单**各抓 N 条**（默认 5、设置项可调）；**轮换随机、无语境过滤**（无语境过滤系用户明确否决过滤机制；「纯随机」2026-08-29 真机第六轮改定为轮换制，见下方修订——起因：用户 20 把实测看到 38 条从未出现、1 条出现 5 次，纯随机的「疙瘩」长相与直觉落差太大）。抓到的条目在面板顶部**「采用方式」二选一**（2026-08-30 真机反馈第三、四轮定形）：**全部采用**（默认）＝整把随分析发送、规划模型自己挑着用——随机抓一把让模型挑＝本功能的主用法；**自选**＝从一条没勾开始、勾谁发谁，一条不勾＝本次不带知识材料。发送的随材料进确认页细账（助手默认）。**（「面板顶部一个全局开关」的作用域已被下方第十四轮修订改成单张清单级——全局值只作旧向导快照的回退）**
- 界面按紧凑一行一条＋搜索＋标签筛选做；规模预期每张几十至几百条。

**用后账（与归档一套账，2026-08-29 用户拍板「在我心里本来就是一样东西」）**：

- 生成产物 JSON 加自报字段报「本次选用了哪几条」（先例：建库时模型自标角色）；没选用任何条目就报空（助手默认，空选不算异常）。选过的条目进**冷却**——冷却期内抓取自动跳过，防「模型从小把里连挑最熟那条」。**结算时机＝规划正式上场时**（确认采用或转隐身注入，2026-08-29 真机第五轮改定）：分析成功只快照发送帐，草稿放弃/重写不碰冷却。冷却按采用次数计、数值设设置项，默认值待数值终审；冷却可**手动清零**（知识库页点冷却徽章）。
- 服装类跨清单组合：一套服装的多个元素可能来自多张清单——用过的**组合整体入账**留存，可固化为**固定搭配**有意复用，与归档「整套记录/单品禁令」同账（组合机制细节随服装库落地时设计）。

**挂账**：冷却默认值待数值终审；服装跨清单组合机制细节待设计；「或自己给意见」二选一调用接不接知识库＝后议；后续 2.0 生成器接知识库＝**长线管线已接（2026-08-30 第十九轮，整表随行不结冷却，见 §6.2 落码注记）**，动作指导卡等待议；「纯文本知识」不再单列（原用途＝背景注入已作废，需要时用单字段清单表达）——此条系助手推论，用户过目时可驳。

**落码注记（2026-08-29，随同次交付；自主裁决待用户过目）**：
- 落点：js/knowledge.js（数据层：清单/条目/冷却账/抓取/结构化导入）＋js/ui/tabs/tab-knowledge.js（管理页签，页签序在世界书之后）＋tab-guidance.js 材料步接入（清单勾选＋「知识库抓取」悬浮面板＋确认页细账＋用后结算）＋planner.js（知识材料小节＋plan.knowledgeUsed 自报字段）＋设置页「知识库」折叠区（每清单抓取条数/冷却生成次数）＋settings.knowledge 全局存储。
- 冷却语义细化（助手默认）：只在「带了知识材料且生成成功」的规划调用后结算；结算时**全部**条目冷却 −1（空选也照走——生成次数推动冷却钟），自报导选用过的重置为上限、使用次数 +1。自报编号按「清单序号-条目号」（如 1-03），对不上的编号静默忽略。**（「生成成功即结算」口径两天后被真机第五轮推翻——见下方第五轮修订**，结算口径细节照旧，只是时机挪到正式上场）。
- 结构化提示词与规划系统提示词的知识段为全新起草（监听先例：新提示词须过审），全文随交付报告送审；建清单/改字段名等管理文案同批过目。
- 条目筛选：搜索按编号与全部字段值匹配；「标签筛选」认表头里名字含「标签」的第一个字段（表头自定义的自然推论，助手默认），无标签字段的清单不显示筛选。
- 首开抓取：悬浮面板打开时给「勾选中且还没抓过」的清单各自动抓一把；重开面板不重抓（已抓的保留），踢掉不补抓（要换血点整把重抓）。
- 导入草稿与粘贴的原文随设置留底（settings.knowledge.ui 单槽，跟着最近一次导入走）：刷新页面不丢，入库或「丢弃草稿」才清；收起清单、切换清单不再丢草稿（**2026-08-29 真机反馈第二轮拍板「要做保留」，反转本注记原「瞬态刷新即弃」设计**）；清单勾选随对话记忆（picks 块）保存、抓到的条目与勾选采用状态随向导快照（wizard 块）保存——刷新页面都在。
- 离线测试 34 项全绿（%TEMP%\pp-kb-test：清单/条目/抓取/冷却账/结构化导入/planner 接入六组）；监听回归 61 项全绿、含新页签的 smoke 通过。首轮真机实测通过（2026-08-29：建库、自定义表头、结构化导入流程正常，101 条清单在库）。
- **真机首轮反馈五条 UI 修订（2026-08-29 第二次交付，用户逐条拍板）**：①「素材清单」标题块移出灰底区块、常驻显示（与清单行拉开区分度）；②清单行压成单行——展开箭头取代「条目」钮、「改名」与清单名同行、「导入」入口从清单行撤下挪进展开后的条目区工具行（功能未删只挪入口，助手默认，要彻底删随时可撤）；③条目列表自带滚动条（上限 min(60vh, 560px)，原地翻看不拖整页；标签行与计数留在滚动区外）；④「新建清单」从页底移到页顶；⑤抓取面板逐条勾选「直接选用」——不想发的取消勾选即可（勾选状态 kbSel 随向导快照留底），「踢」保留＝从这把里移除、与取消勾选只差行不再显示（助手默认），全部取消勾选＝本次不带知识材料（**⑤的「默认全勾、反着取消」次日被用户推翻，第三、四轮改定「采用方式」二选一，见下方修订**）。修订后界面与冷却体感待复验。
- **真机第二轮反馈五条修订（2026-08-29 第三次交付，用户逐条拍板）**：①已入库清单加条目不再翻页底——展开区挪到自家清单行正下方（原垫在所有清单行之后），且手动添加的条目插到条目列表最上面（编号照发新号不重排，助手默认——结构化入库仍接在末尾）；②结构化导入 JSON 报错率高＝助手推论根因「101 条一把结构化、输出被单次回复长度上限拦腰截断，本地修不回、重试照样截断」——长草稿自动分批（按空行切段打包约 2000 字符/批、一批一次调用、user 消息带「第 x/y 批」标注），失败批不拖垮其余（failed 记批次开头供定位、toast 报），全部批失败才抛错；解析报错补解析器原文（显示问题所在）、截断输出补闭合抢救前段完整条目（utils.extractJson 共享件，监听/规划等全部调用方同步受益）；③刷新页面内容不丢（草稿与粘贴原文随设置留底，见上条——原「瞬态」设计反转）；④「入库选中/丢弃草稿」上移到草稿页头部（保存不再翻到内容最下面），底部按钮行删除；⑤两个「丢弃草稿」删一（留头部那个）。结构化提示词同轮加「紧凑 JSON 一整行」指令（全文随交付报告送审）。离线 42 项全绿、监听回归 61 绿、smoke 通过。待复验。
- **真机第三轮反馈一条修订：抓取面板勾选方向反转（2026-08-30 第四次交付，用户拍板）**——首轮⑤落成「默认全部勾上、不想发的取消勾选」，用户实测后点名「我引用两个清单抓了 10 条、只想用 1 条，竟要手动去掉 8 个勾——跟手动踢 8 条没区别；我要的是直接选哪一条」：把「选用」做成了「排除」，方向反了。当轮改为 kbSel 恒为数组、默认空（一条不勾）、勾上＝发送（**此轮的单一「默认全不勾」次日再被推翻，见第四轮——单一默认伺候不了并列的多种用法**）；第 2 步确认页在「勾了清单但一条没勾」时点名提醒（助手默认，可驳）。「踢」与「重抓」不变。待复验。
- **真机第四轮反馈一条修订：勾选改成显式「采用方式」二选一（2026-08-30 第五次交付，用户拍板）**——用户点名第三轮的「默认全不勾」弄死了另外两种用法：「有没有考虑过我全选的情况？还有一种可能性就是我要让模型从里面挑？你二极管是不？……那我还随机个屁啊」。全选／让模型从一把里挑／自己点名挑是**并列**用法，前两轮各做成对立的单一默认、每次弄死别的。现面板顶部「采用方式」分段选择器（复用世界书三段式同款 .pp-seg）：**全部采用**（默认，每轮向导重置；整把发送、规划模型自己挑——随机候选的主用法，覆盖「全选」与「让模型挑」两种意图）｜**自选**（勾选框只在自选下显示；从一条没勾开始、勾谁发谁，勾选随向导快照留底）。方式随快照保存、刷新不丢；旧快照兼容：有手勾的自动落在「自选」、没动过的落在「全部采用」。确认页空手提醒改不分方式的统一口径。「踢」「重抓」不变（自选下重抓的新一把默认不勾）。待复验。
- **真机第五轮反馈两条修订：放弃保存保留知识库材料＋冷却结算挪到正式上场（2026-08-29 第六次交付，用户拍板）**——用户实测：「用了一次的材料，那一轮只是草稿里使用，放弃保存回到第一步以后，那个材料并没有保留让我可以再次直接使用；而且我没有正式注入，它就进入了冷却期，这个冷却期我去知识库里还取消不掉」。两条根因：①「放弃保存」把抓到的条目/勾选/采用方式一并清了——丢弃现在只清生成结果（kbIds/kbSel/kbMode 全留，回第 1 步可原班材料直接再分析；按钮说明与 toast 同步点名）；②冷却在「分析成功」就结算——草稿还没被采用就烧了冷却。现改为：分析成功只快照发送帐（run.kbSentIds，随向导快照留底），结算（settleKbCharge）只在**确认采用**或**转为隐身注入**时执行，同一份草稿两条路都走只结一次；结算口径本身不变（全部条目 −1、空选照走、自报导选用过的重置上限、使用次数 +1），冷却计量从「生成次数」改「采用次数」（设置项更名「冷却生成次数」→「冷却采用次数」，内部键名 cooldownGens 不动）。配套：知识库页**冷却徽章可点清零**（clearCooldown，点一下立即恢复可抓，此前冷却没有任何取消出口）。待复验。
- **真机第六轮反馈一条修订：抓取从纯随机改轮换制（2026-08-29 第七次交付，用户拍板）**——用户拿 100 条清单实测 20 把（每把 5 条），统计上完全健康（助手验证过：62 条出现过 vs 理论期望 64.2、卡方 108 vs 期望 99±14、两万次模拟里 23.6% 会出现「某条 ≥5 次」——真随机的「疙瘩」长相本身没错），但用户拍板「我们随机肯定不能这么随机，我们要有冷却机制，所有条目随完一轮之前，不会出现重复条目」。现抓取＝**轮换制**：每张清单一条轮换队列（存清单对象 `queue`＝剩余未发条目 id、随设置持久化、全局共享跨聊天）；队列空了自动把全部**可用**（非冷却）条目洗牌成新一轮按序发——**全部条目各发一次之前绝不重复**，发完一轮自动重洗开下一轮（新一轮可与上一轮重复，轮边界允许）。配套细则（均助手默认，可驳）：①冷却交互——开轮时冷却中的条目不进本轮，轮中等到自己时才进冷却的跳过出本轮（冷却结束或下轮回归）；②重抓＝按队发下一批（被换掉的那把算已发过，要等下一轮才回来——顺带堵住「反复重抓刷同一条」）；③轮中删除的条目到轮到时跳过不炸；④手动新增条目下一轮才进队列；⑤一把跨轮边界（上轮剩 2＋新轮 3）时同一把内仍不重复（开新轮时排除本把已发过的）；⑥队列随条目全局共享（不绑聊天）。面板清单行加「本轮剩 N」计数（轮换进度看得见）。**两层防重复并存**：轮换管抓取层（一轮内不重复），采用冷却管采用层（选用过的 N 次采用内不抓）——机制独立、互不替代。待复验。
- **真机第七轮反馈七项方案（2026-08-29 锁定，2026-08-29 落码；用户批覆「大部分内容都没有问题」＋方案六单独加严）**——起因＝5 次带知识库的真机 roll 暴露六类问题：时间顺序盲区（当前上午 11 点仍排「先玩后吃/晚饭」）、连 roll 高度重合（放弃再分析＝纯盲 roll、重新生成＝复读锚定，两条路殊途同归）、指令遵循度低（点名多节点只给 3 个）、替 user 做行为说话（换模型连台词都代写）、礼物类想要全量投喂、性知识类想要全量但三档位世界书不适配。落点四散：知识库投喂方式见上方正文；世界书条目自选见 §6.10；规划行为四件（时间顺序/防复刻骨架/硬要求与节点下限/user 不可编排＋本地黄牌）见 §6.11。提示词全家（规划系统提示词、beats 字段说明、知识材料小节口径、随机事件/路人反应同款 user 条款）随落码交付报告全文送审。
- **真机第八轮反馈六项（2026-08-30 落码；①④⑤系用户逐条点名，③的思考复发根因系助手查证）**：①「新建清单」表单默认收起、点「新建清单」展开（投喂方式选择随会话记住，创建成功自动收起）；④知识库抓取面板按清单做折叠栏——**抽样清单默认展开（一把几条直接勾）、全量清单默认折叠**（整表本来就要发，不用逐条过目；踢/还原在展开后），折叠只动显示不动发送集、随面板关闭重置；⑤设置页两处密钥框加「小眼睛」显示/隐藏（只切输入框类型，不动存的值）；③分析/检查运行页五件套＋思考复发根治见 §6.12；⑥性能问答不落码（DeepSeek-V4-Flash-0731 关思考可否担纲规划——随交付报告作答）。
- **真机第十四轮反馈：采用方式改单张清单级＋发送集零条显式点名（2026-08-30 落码，用户点名「不存在单张清单能做指定的情况，传入逻辑错误」）**——翻车机理：多张抽样清单同场时，「自选」是面板顶部一个全局开关——用户给 A 清单逐条勾上、B 清单（约会地点）的条目一条没勾，B 就**整张静默掉队**（kbSel 里没有它的条目＝发送集为空），确认页细账只报有发送的清单、B 无声消失，输出里自然完全看不到。两件落码：①**采用方式单张清单化**——面板顶部的全局选择器撤掉，每张抽样清单的标题行各自带「全部采用｜自选」二选一（随向导快照留底）；裁决收口为纯函数 `kbSendPayload`（knowledge.js，面板/确认页/真实调用三处共用，离线直测）；旧向导快照的全局 kbMode 保留作各清单缺键时的回退值（旧快照行为不变）。②**确认页细账按清单点名、零发送标 ⚠**——逐张勾选中的清单各报一段（全量＝「全量 N 条」、抽样自选带「自选」标记逐条编号点名）；勾了清单却 0 条发送的（自选一条没勾/还没抓/全冷却或被踢）显式点名标 ⚠ 并给出回第 1 步的指引，不再无声。待复验。
- **真机第十五轮反馈：抽样口径软改硬＋结果页自报选用上屏（2026-08-30 落码，用户对清单条目与输出「完全不匹配」的对账结论逐条拍板）**——对账结论：清单确实发进去了（约会地点「自选」只勾 1 条＝只发动物园这 1 条；用餐地点 5 条全发；礼物清单全量 250 条随行），但抽样分组的软口径（「优先从中选用，没覆盖的方向可以自拟」）让模型按自己的偏好尖峰自拟了全部选材且自报空数组**完全合规**——规则管不住它。三件落码（用户原话：「他这个模型最后输出的选项必须从我有的这里面选，如果我只勾一条，就必须照着这一条编」）：①**口径统一改硬（不加档位不加开关）**——材料小节与规划系统提示词里，全量与抽样一律「凡涉及该清单领域的内容必须从该分组条目里选用、不得自拟同类」；抽样分组逐张带清单名（平铺混一组分不清哪条管哪类内容）；「保持条目核心特征」加了对照例（地点是酒吧就按酒吧写，不擅自改成餐厅——对的高空景观酒吧被写成餐厅的半采用病灶）；②**结果页「知识库选用」行**——模型自报的选用编号（knowledgeUsed，此前只喂冷却结算不显示）按分析时发送快照（kbSentIds）解析成「x-xx 首字段」原样上屏（knowledgeUsedLabels），报错编号也原样带出、一条没用显式点名；③**与构思点名的优先级写死＋第二遍对账加⑤**——构思或修改意见点名了该领域具体内容时点名赢、不硬换成清单条目（模型须在 risks 说明）；第二遍尾巴对账清单在①～④后追加⑤知识库口径（只在带知识材料时出现），「选材与 knowledgeUsed 都不动」改为「只在违反③点名或⑤口径时才换、换了同步改 knowledgeUsed」——此前第二遍永远不纠「该用清单而自拟」。新提示词三段（系统条款/小节标题与分组头/第二遍⑤）随交付报告全文送审。待复验。

### 6.10 世界书条目自选（2026-08-29 第七轮定稿并落码；2.0 骨架「世界书条目自选」的提前落地）

**动机**：性知识这类「照着写」的材料要的是**原文整条随行**，不是「选着用」——三档位（停用/关键词/常驻）完全不适配：关键词碰运气（场景里没出现关键词就带不出）、常驻进插件**全部**模型调用且不能按次控制（规划要用、打标不要用，没法表达）。用户在规划开始前也没法按次自选世界书。

**机制**（挂在剧情指导页第 1 步，与知识库抓取同款悬浮面板）：

- 第 1 步工具行新增「世界书自选」入口＋悬浮面板：**按书分组**列出条目（书名做分组头），勾哪条哪条**整条原文**随分析进材料；不看关键词/常驻/对话启用书单——勾选就是唯一的口径，禁用的书与条目照样能勾（自选＝点名，与检索状态无关）。
- **与检索命中去重，自选优先**：同一条目既被关键词检索带出又被自选勾中时，只出现在「自选世界书条目」小节，检索小节里不再重复出现。
- 勾选**随对话记忆保存**（chatdata 的 picks 块，与清单勾选同层），刷新/回第 1 步不丢；条目或书被删后勾选静默失效（发的时候找不到就不带）。
- **无冷却**——「照着写」的材料每次都该在场，冷却没有意义；知识库（选着用）才配冷却。
- 只进规划向导的分析调用（随机事件/路人反应/检查报告不自动带——它们的材料口径各自独立，不跟着向导走）。
- **面板交互（2026-08-30 第八轮加）**：按书折叠——默认全部收起，勾选时点「展开」才摊条目；搜检索词时命中的书自动展开（显式点过「收起」的仍尊重）。条目行**只显示名字**（条目标题；无标题显示「条目 N」），原文整条在行的悬浮提示里看——行面保持紧凑，发送的仍是整条原文。

### 6.11 规划生成的行为约束（2026-08-29 第七轮定稿并落码；提示词层＋两处本地机制）

真机反馈暴露的四类规划行为问题与对策，全部作用在 1.0 规划向导的分析调用上：

- **时间顺序三句**（提示词层）：①材料各小节与条目的排列顺序**不代表时间先后**（两张清单按序拼接被模型当时间线读是第七轮实测病灶）；②对话或用户构思里给出当前时间的，**按它排程**（上午 11 点就该排午饭前后，不排晚饭）；③条目带时段类字段（表头含「时段」或字段值含时间词）的按它排——表头可加「时段」列表达，无新机制。
- **防复刻骨架清单**（本地机制＋随行小节）：所有旧防重复条款都是往后看材料的（对话/记忆/历史摘要），**放弃的草稿哪边都不在**——这是连 roll 收敛的根因。现「放弃保存→再分析」视为重 roll，随行「近期草稿骨架」小节：每版一句话概括＋各节点阶段名，**最近 5 份为限，确认采用（或转隐身注入）后清空**；新规划的骨架不得与清单里任何一份高度雷同。跳转回第 1 步直接再分析（旧草稿还在）同样先入骨架账。「重新生成」保持按修改意见修订的语义，**修改意见为空时按换一版处理**（上一版随行但标注「换一版：不要沿用其骨架与安排」）。
- **构思框升硬要求＋节点下限**（提示词层＋设置项）：用户构思里的数量类要求（「多写些节点」）一律按**下限**落实，不许打折；用户没点名时，节点数下限进设置项「规划节点下限」（默认 5、不设上限——节点是将来监听挂载的判断点，示例数量会被模型当上限用，所以只写下限）。beats 数组说明里写明下限。
- **user 不可编排（四层）**：第一层系统提示词**三禁一豁免**——禁替 user 做动作、禁替 user 说台词（含「user 答应后」这类把 user 的回应写成既成事实的写法）、禁预设 user 的心理反应；豁免＝对话或构思里 user 已明说的意愿可当前提。第二层**条件式接口**是唯一合法写法——涉及 user 的部分一律写成「若 user X 则 Y」（可多接口可分支），且**每个节点的核心推进不依赖 user**（user 接口是挂件不是地基，删掉挂件节点仍成立）。第三层 beats.content 字段说明内嵌短版（贴生成点，项目先例＝risks/knowledgeUsed 都靠 schema 格子执行住）。第四层坏好对照示例各一组，配防复读护栏（学结构不学内容）。同类条款扫全：随机事件、路人反应提示词同款加「不替 user 写行为与台词」。**本地黄牌**（人工二检页）：本地正则扫规划文本里「user＋说/答应/承诺/主动/帮她」类句式，逐句亮牌提醒——只提醒不拦截，不阻止采用。

**第十轮修订（2026-08-30 凌晨真机反馈，同节追加）**——三件：

- **点名要求全面升硬要求**：构思与修改意见里点名的要求从「数量类」扩到**全部类别**——数量、价位或金额（候选条目带价格区间的以条目为准）、时间日期、由谁发起或由谁挑选（点名了发起权归谁就不得转手，尤其不得转给 user）、地点、身份资格。起因＝全量礼物清单（条目带「价格区间」列、随材料全文进模型）＋修改意见点名「礼物不低于七位数、由陆星棠挑选、注意浴室是今天」，产出仍是 user 提前安排、价位不明——第七轮只把数量类写成了硬约束，其余点名要求无任何条款兜着，模型按默认分布走。原「节点数量是硬要求」条款并入此条，节点下限（minBeats）条款保留原样。
- **事实一致性条款（新增）**：对话记录、记忆表格、历史摘要里已发生的事是既定事实——发生在哪天就是哪天（今天上午发生的事不得改写成第二天），后续排程只能从既定事实之后往后推；角色与 user 的既定设定（年龄、身份、能力、资格——从对话、人设卡、记忆表格读到的）是硬约束，规划不得安排设定不允许的事（如未成年开车）。起因＝记忆表格与上下文都有「今天上午」、规划仍把约会写成第二天，且出过未成年 user 开车的剧情——与时间顺序条款互补：时间顺序管「现在几点、往后怎么排」，事实一致性管「已经发生的事不许挪日子、设定不许破」。
- **黄牌展示改一行总述**：扫描逻辑不动（句式表原样），人工二检页只报「发现 N 处疑似替 user 编排的句子」＋一句合法写法提示，撤掉逐句清单（用户拍板「展示一个总体的输出就够了」）。

**第十一轮修订（2026-08-30 凌晨真机反馈，同节追加）**——两件：

- **思考标头自动重试（api 层，全部调用受益）**：deepseek 等推理模型经部分中转会把思考段直接写进正文、开头打「## 思考（Thinking）」类标头。chatCompletion 加外层包装：输出以思考标头开场＝思考混进了正文（本插件所有任务都要求 JSON 开场）——流式在标头刚露头时掐断（省 token）、非流式收完判定，命中即以原请求自动重试一次；重试的一发不再掐断（再掐可能把思考段后的完整内容丢在半路），仍带标头按原样返回＋toast 提示换模型或勾「关闭思考」。判定只认「#/思考或 Thinking/行尾」的独立标头行（looksLikeThinkingHeader 纯函数，「思考突破」这类普通标题不误伤）；重试请求服务商照常计费、onUsage 累进上层对账（planner 累加制），流式掐断那发收不到 usage——与手动中断同口径。
- **黄牌展示再修（纠正第十轮过头形态）**：用户澄清上轮「总体输出」指不要每处各开一张粗体标题卡（第九轮形态），不是只剩总数不说哪里——现合成一张卡：总数一行＋触发句子逐句列在同一张卡里（扫描逻辑两轮均未动）。

**第十二轮修订（2026-08-30 真机反馈「时间错位一直在」，同节追加）**——两件落地＋一件提案自驳：

- **时间锚外化（currentTime 字段）**：时间错位的机理＝相对时间（今天/明天/第二天）没有固定寄存的「现在」，模型写规划时被体裁惯性拽着把近事归档成「昨天」，且一处写错后续参照系整体漂移。现 plan 对象第一字段 `currentTime`：模型排程前先把「现在是什么时候」从对话/构思/记忆表格里读出来写在这里（材料无线索如实写「未指明」）——锚点外化到生成点上，后续相对时间全以它为参照。结果页新增「时间基准」行原样展示：模型把「现在」读错（读成明天）一眼可辨，用户改规划文本即可。规划文本（formatPlan）首行带「时间基准：X」（「未指明」不写）——随注入/采用一并带给扮演模型，主对话侧同样有锚。
- **两遍调用·第二遍对齐修改（用户拍板「第一次生成，第二次对齐要求修改」）**：第一遍照常生成；解析成功且设置开着时自动追加第二遍——同一份材料（含联网纪要，联网不重判、不重复计搜索费）＋第一遍 plan 草稿再发一次，系统提示词换成「对齐审校员」：只拿四类要求逐条对账（①时间基准重读＋相对时间对锚 ②既定事实与设定 ③点名硬要求 ④user 不可编排），**只改违反处、其余原样保留（校对不是重写，选材与 knowledgeUsed 不动）**，输出 `{plan, fixes[]}`；checks 不重查不重发（它是对对话的判断，不随 plan 修订而变）。合并规则：plan 取第二遍、checks 沿用第一遍，`fixes`（逐条改动清单）与 `alignState` 挂在结果上，结果页新增「第二遍对齐修改」行逐条展示。第二遍失败或被中断**不报废第一遍**：结果照常交付、alignState 如实标注（中断那半已生成的部分照常计费）。计费约 ×2（输入两遍全量材料＋两份输出；**本段计费与「系统提示词换成对齐审校员」的拼装方式已被下条第十三轮修订取代**——第二遍已尾巴化、计费约 ×1.2～1.4），运行页阶段标注「第二遍 · 对齐修改中」，两遍 token 实报合并上账单行；思考原文两遍拼接（【第二遍思考】分节）。设置页「生成后对齐修改（第二遍）」可关（默认开）。范围＝规划向导（含重新生成）；检查报告、随机事件、路人反应不接第二遍。
- **本地时间黄牌提案自驳（不落码）**：上轮交付时助手提案「本地扫输出里的昨天/第二天亮牌」，经分析撤回——规划是前瞻文档，「第二天」大量用于合法的未来排程（现在今天、明天约会被排成「第二天」完全正确），正则分不清「把今天的事挪到明天」与「把明天的事排在明天」，落地必成误报机器；真正能做语义对账的是第二遍模型调用（已落地），加一道正则黄牌只剩噪音。

**第十三轮修订（2026-08-30 用户拍板「×2 太贵，走前缀缓存」，同节追加）**——两件落地＋两个省钱方向记录在案未采纳：

- **第二遍尾巴化（共享前缀，吃前缀缓存）**：前缀缓存（DeepSeek/Anthropic 系，命中部分约一折）按请求**开头逐字节匹配**计费。第十二轮第二遍把 system 换成「对齐审校员」提示词——前缀从第一个字节分叉，缓存一分吃不到，全量材料两遍全价。现第二遍的 messages 与第一遍**逐字节相同**（system、全部材料、联网纪要都不动），审校指令改为「## 第二遍任务：对齐审校（覆盖上文的生成任务与输出要求）」小节追加在 user 消息末尾、其后附第一遍草稿——差异越晚出现、命中的前缀越长，第二遍的新增输入只剩这条尾巴。审校清单不再复述系统提示词里已有的规则（时间顺序/事实一致性/user 不可编排/点名要求系统里都在），只留四条对账要点＋「只改违反处、校对不是重写」口径＋输出 `{plan, fixes}`；尾巴显式声明「本次不输出 checks」以覆盖 system 末尾的输出结构要求。连带受益（无需改动自动获得）：思考标头掐断重试、坏 JSON 修复重试（parseModelJson 原 messages 后追加修复对话）本就是前缀不变的重发，同样走缓存价。诚实账：**缓存只折输入不折输出**——第二遍输出（整份 plan 重发＋fixes）无折扣，×2 修正为约 ×1.2～1.4（材料越大越接近 ×1.2）；服务商不做前缀缓存时本轮改动无害也无益（第二遍输入与旧版同量级，省掉的是审校系统提示词里复述规则的那几百字、多付的是与第一遍共用的大系统提示词，两相大致抵消）。
- **材料小节重排（跨调用也吃缓存）**：排序原则＝「**开头会变的小节垫底**」——缓存前缀在第一个变化字节处断掉，其后全部按未命中计价。三股开头会变的：最近对话记录（滑动窗口取最近 N 层，每轮聊天后窗口头就变——原排第 2 位，跨次分析的缓存全灭）、检索命中的世界书（按最近楼层重扫，内容随对话变）、知识库抽样分组（轮换制每把换新）。新序：角色摘要 → 自选世界书（勾选按对话存）→ 游戏玩法（按对话存）→ 记忆表格（只追加，前缀稳定）→ 进行中剧情 → 随机事件 → 路人反应 → 历史剧情摘要（只追加）→ 近期草稿骨架 → 知识库材料 → 检索命中世界书 → 最近对话记录 → 上一版规划/修改意见/用户构思 → 联网纪要（末尾追加，不动）。剧情→事件→反应的相邻序保留；系统提示词本就声明「排列顺序不代表时间先后」，语义无迁移。检查报告（runStoryReview）同款重排。收益场景：同一对话聊几层后再分析、重新生成（按意见修订/换一版）、两遍调用——稳定小节全部命中缓存价。
- **未采纳①：第二遍砍掉知识库材料（用户提案的另一半）**——与缓存互斥：砍掉中间小节＝前缀从那里断，知识库之后的材料全部回全价；且知识库留在前缀里只花一折钱，还保住第二遍「②既定事实与设定」的核对能力（设定类清单就在知识库里）。只有服务商完全不做缓存时它才有净收益，不为它维护双路径。
- **未采纳②：第二遍无改动时只输出 fixes、不重发 plan**——输出 token 是第二遍成本大头，此法能砍掉干净草稿的整份 plan 重发；但「有 fixes 无 plan」的坏输出会降级为失败回退（现校验要求 plan 必在），两遍制刚上线未经真机验证，健壮性不换这点钱。日后真机证明第二遍输出账单刺眼时可再启。

**第十四轮修订（2026-08-30 真机反馈「第二次校验报错率奇高，经常改不出第一遍存在的问题」，同节追加）**——两处加固（均助手默认，可驳）：

- **第二遍解析接入修复梯子**：此前第二遍是裸 `extractJson`——坏格式直接整遍判失败（toast「第二遍对齐修改失败」、保留第一遍），而其它结构化流（反应卡/报告卡/导入）早有 parseModelJson 的「带修复提示回炉一次」兜底。现第二遍同款：坏 JSON 自动把「原对话＋坏输出（assistant）＋修复提示（user）」再发一次（原对话逐字节在前，前缀缓存口径不变）；修不好或「解析成功但没有可用 plan」（如模型只回 fixes 不回完整 plan——第十二轮未采纳②预言过的坏形态）仍判失败、保留第一遍，全部实报账照累计。
- **第二遍输出上限 ×1.5**：第二遍输出＝整份 plan 重发＋fixes 清单，比第一遍只长不短；同一输出上限（默认 1500）下第二遍更容易撞顶截断成坏 JSON——这是「报错率奇高」的另一半病灶。现第二遍（含修复发）按设置值 ×1.5 发（默认 1500→2250），设置 0（不限/端点默认）保持 0 不动。
- **「改不出第一遍存在的问题」不在本轮两处之内**：那属于审校效果（模型在审校位报「草稿本就全对」或改不中要害），是提示词遵循度问题非代码 bug；开关（设置页「生成后对齐修改（第二遍）」，第十二轮已有）可整体关掉只跑第一遍，审校提示词的进一步收紧待真机样本再议（改动需重审）。


### 6.12 分析运行页的状态与中断（2026-08-30 第八轮定稿并落码）

真机反馈「剧情生成途中没有中断按钮、没有运行中状态、用时、报错留存、token 计数、模型名；模型又能思考了、一万多输出」——六件一起落，分析与检查报告两页共用同一套：

- **运行状态条**：模型名（当前主连接）· 用时（0.5s 秒表）· 思考计数 · 中断键。token 实报数字在调用结束才知道（流式要等末包 usage），结束后上横幅/结果页，不在运行中虚报。
- **中断**：AbortController 从运行页一路传到 fetch（联网判断的预跑调用发出去没法掐，只能不等它）；中断后页面停在半途输出上出「已中断」横幅，去路＝再试一次（原班材料）/回第 1 步。**切聊天自动中断**在途调用（回调已被聊天令牌作废，跑完只是白花计费）。计费口径如实交代：中断前已开始/完成的调用与已生成的部分，服务商照常计费。
- **报错留存**：出错不再自动跳页＋一闪而过的 toast——报错原文（限高滚动）留在运行页横幅里，半途输出也在；修订重试失败时旧结果仍在第 3 步没动。
- **账单上页**：完成的结果页/检查报告页头部常驻一行「模型 X · 用时 Ys · 输入 A/输出 B tokens（实报）」（联网判断的调用也计入；流式没回传 usage 就如实写「无实报」）。
- **思考复发根治（两层）**：①「关闭思考」参数从两家扩到四家（deepseek/GLM 认 `thinking`、Qwen 认 `enable_thinking`、OpenAI 系认 `reasoning_effort:'none'`、vLLM 部署认 `chat_template_kwargs.thinking:false`），且端点报 400/422 时按**报错点名的参数定向去除**重发（重试梯子：全量→去点名→只留最通用的 thinking→全去；失败的 400 不产 token）——旧版一刀切全部去参重发，等于把关闭思考一起丢掉、思考静默回来，这是第八轮「关了又想了一万多」的根因（V4-Flash 思考默认开、effort 默认 high）。真被全部去掉时弹警告点名。②流式思考增量实时计数上运行条（`reasoning_content` 增量累计）——「关闭思考」开着却在涨字＝端点没执行，界面直接标⚠，用户可当场中断止损。
- **密钥小眼睛**：设置页两处密钥框（大模型/搜索）加显示/隐藏切换，只切输入框类型不动存的值（核对粘贴用）。

**第九轮修订（2026-08-30 凌晨真机反馈，同页追加）**——反馈两件：「官 key（deepseek 官方便点）怎么也复发了思考」与「流式输出每来一个字就把滚动条顶回底部，翻不上去看已输出内容」：

- **官 key 复发根因坐实（不是官方关不掉思考）**：deepseek 官方 chat/completions 正是「严格点名陌生参数」型端点（不认的字段直接 400 `Unrecognized request argument supplied: X`，宽松忽略只在另一套 Responses 端点）——旧版发的两家参数里 `enable_thinking` 是 Qwen 方言，官方一眼认出是陌生人就 400，旧版处置＝一刀切全部去参重发，把真管用的 `thinking` 一起丢了。新版四家齐发＋定向去除本就治好这条路；本轮再把重试梯子上限从三发放宽到**四发**——官方端点倾向一次 400 只点一个名，四家里的三家方言要三轮点名才去完，三发上限会在中途报错断掉（失败的 400 不产 token，多一发不花钱）。离线 9.8 例：逐个点名三轮后第四发只剩 thinking 成功。
- **流式页贴底跟随**：旧版每个流式增量都 `scrollTop = scrollHeight` 拽回底部，翻上去看旧输出根本看不成。改为**在看的人本来就在最底下（距底 <48px）时才跟随滚动，翻上去了就不动**；流式框右下角出现「回到底部」浮键（点击跳回最新并恢复跟随）；运行页刚挂载（跳走再回来）force 定位到最新输出。
- **思考原文上屏**：第八轮思考只计数（运行条「思考 N 字」），原文无处可看。现 `onReasoning` 回调改为传思考全文，流式框里带「【思考 N 字】」段头排在「【正文】」之前——计数在涨时翻上去就能核对是不是真在思考（贴底跟随修好之前想看也看不到）。

**第十四轮修订（2026-08-30 真机反馈「流式传输巨卡、两轮剧情规划能把电脑卡死」，同页追加）**——卡死根因＝上屏方式，不是网络也不是流本身：此前每个 SSE 增量（每秒可达几十次）都把「思考＋正文」**全量累计文本**整段塞回输出框的 textContent（推理模型动辄上万字的思考流，等于每拍重建几万字的文本节点）并前后各读一次 `scrollHeight`（强制同步排版）——主线程被钉死，页面与整台机器跟着卡。两改（行为与视觉不变，纯工程修复）：①**入口节流**——增量统一走 `scheduleStreamView`，约每 120ms 合成一拍上屏（挂拍期间切聊天/重开向导由令牌自然作废）；②**增量上屏**——输出框拆成稳定子节点（思考计数/思考正文/正文），新字只追加尾巴（已上屏部分不重排），新全文对不上已上屏前缀时（第二遍重开/修复重试/掐断重发）才整体换一次。贴底跟随、回到底部键、思考段头等第九轮交互原样。
- **账单行补思考字数**：结果页/报告页头部的「模型 · 用时 · …」行追加「思考 N 字」（思考过必报；「关闭思考」开着且为 0 也报「思考 0 字」）——事后查「上次到底思考没有」不用一直盯着运行条。

**第十五轮修订（2026-08-30 真机反馈「清单条目与规划输出完全不匹配」，同页追加；口径改硬的主体见 §6.9 第十五轮）**——本页两件：①**第二遍对账清单加⑤知识库口径**——第二遍尾巴在对账要点①～④后追加「⑤知识库口径：凡规划涉及某清单领域的内容选材必须来自该清单条目、第一遍自拟了的换成小节条目并同步改报 plan.knowledgeUsed；构思/修改意见点名了具体内容的以点名为准」（只在带知识材料时出现）；原「走向、节点结构、选材与 plan.knowledgeUsed 都不动」改为「走向、节点结构不动；条目选材只在违反③点名要求或⑤知识库口径时才更换」——此前第二遍的合同里选材是免检区，「该用清单而自拟」这类问题永远不纠。②**结果页「知识库选用」行**——时间基准行下方新增，模型自报的选用编号按分析时发送快照解析成「x-xx 首字段」上屏（多报/错报编号原样带出、一条没用显式点名「这算违规」）——自报此前只喂冷却结算，用户对账「模型到底说它用了哪几条」无从看起。

**第十六轮修订（2026-08-30 真机反馈「关闭思考勾着、模型仍经常思考」，用户点名要改动：不管二次校验开不开，生成内容都要在第 2 步里保留，好原样报思考标头）**——本页两件：①**运行页第一遍原文冻结保留**——此前第二遍（对齐修改）一开跑，流式框里的第一遍正文被整体顶掉（第十四轮增量上屏的「对不上前缀就整体换掉」路径），第一遍若混着思考段就再也看不到。现 `onStage('align')` 到来时把第一遍全文冻结成独立段：框内结构变为【思考 N 字】＋【第一遍生成原文 · 共 N 字】（冻结不动）＋【第二遍 · 对齐修改输出】（实时流，第二遍重开/修复重试仍只动自己这段）；跳走再回来由模块态照常重建。②**确认页②「上次生成原文」折叠区**——`run.raw` 本就是第一遍原文（第二遍对齐修改只换 `result.plan`、不动 `raw`），且已随向导快照留底（刷新/切聊天不丢），此前却没有任何展示位。现确认页「联网搜索」行下、按钮行上方加默认收起的折叠区（提示词分块同款样式），展开即第一遍原始输出全文（原样不删改，模型把思考写进正文时在这里看得到原样、可直接复制去报）。二次校验关掉时该遍就是全部输出，同样进这个折叠区。用途口径：只供人查对，不参与任何解析与重试判定。

**第十七轮修订（2026-08-30 用户反馈「这一轮关闭思考似乎真的生效了」＋点名做开关分家，同页追加）**——①**思考开关分家**：`chatCompletion` 新增按次覆盖的 `thinkingOff` 参数（缺省跟设置页总开关——生成侧九类调用行为分毫不变），**监听发调点（listenerAttempt）恒传 true**：监听每轮都跑、开了思考成本会爆炸，规划要聪明可以开思考，两侧不再共用一个开关同进退。设置页「监听」区加一行说明、总开关悬浮补「只管生成侧」、空内容报错与参数被拒 toast 的措辞改中性（不再默认用户勾过设置项——监听调用没勾任何开关也会带这些参数）。②**用户环境记账**：DeepSeek 官方 API、基本不会变（§6.5）——方言适配以官方口径为第一优先。③取证通道保留：用户约好「关闭思考开着再出现思考就把内容发来」，第十六轮「上次生成原文」折叠区就是取证位。④另立**代码地图 docs/CODE_MAP.md**（缘由＝用户指出「每次一点小改动都要阅读很久」）：文件职责、五条核心链路、「改 X 先读 Y」路由，新增文件/链路须同提交更新。

## 7. 验证台账


**【实战】**（真实聊天里验证过）：

- 作者的编辑习惯本身（日常修正只在表尾两三行、大修节奏约三千层、久远内容一次维护即稳定）——长期使用源插件的真实习惯，先于本插件存在。

**【意图】**（设计意图，待第一次完整战役 / 首次大修验证后搬栏）：

- 镜像删除的降噪实效（删时空表 / 物品表后规划质量是否可感提升）；
- 名字标签 + 事件类型标签的群像卡筛法实际覆盖度；
- 指纹在大修下的表现 + 大修后处置流程（人工核验 + 重打标）；
- 事件注入按层数过期的手感（过期了剧情没演到怎么办）；
- 检查报告继承向导口径后的查重效果；
- 单元制的实际手感（跨工具导入、加工史徽章、生成草稿→立为单元→走向裁剪、第 1 步「插入单元」区勾选/查看/注入、满 3 手动清理）；
- 盲盒模式全套（四场景 + 模型自判公开 + 折叠劝退）；
- 监听全套手感（已落码：单位/轻量双模式、1.0 规划挂载、三级降级、排队闸、留痕悬浮窗、旋钮——离线测试台 61 项全绿，用户已初步实测；单位模式连跑、拔网降级不阻塞发送等深度项仍待验）；
- 长线规划管线全套（已落码 2026-08-30：骨架/具体化/修订/切章四步＋材料面板＋章挂监听执行；真机首轮使用后连续五轮反馈修至 2026-08-31——骨架就地编辑、流式实时状态、锚密度与章上限随预算、卷卡竖条根因修复、编辑框收起、展开块互斥、修订如实报账，明细见 §6.2 落码注记；界面复验与执行段〔挂章后节点逐轮点亮、接续下一章〕的深度验证仍待用户跑）；
- 知识库全套（已落码 2026-08-29：自定义表头导入、材料步抓取悬浮面板、用后冷却账——离线全绿；首轮真机实测通过，七轮真机修订（五条＋五条＋勾选反转＋采用方式二选一＋放弃保留材料/冷却挪到采用时结算＋抓取改轮换制＋第七轮七项：投喂方式抽样/全量、世界书条目自选、时间顺序、防复刻骨架、硬要求与节点下限、user 不可编排与本地黄牌）随第二至八次交付，第八轮运行页六件与第九轮运行页修订（贴底跟随/思考原文上屏/梯子四发，见 §6.12）随 f8b3853 之后两次交付，第十轮三件（点名要求全面硬化/事实一致性条款/黄牌一行总述，见 §6.11 第十轮修订）再次交付，第十一轮两件（思考标头自动重试、黄牌展示再修，见 §6.11 第十一轮修订）相继交付，第十二轮两件（时间锚外化 currentTime、两遍调用·第二遍对齐修改，见 §6.11 第十二轮修订）落地，第十三轮两件（第二遍尾巴化吃前缀缓存、材料排序稳头在前，见 §6.11 第十三轮修订）落地，第十四轮四件（采用方式单张清单级＋确认页零发送点名见 §6.9 第十四轮、第二遍修复梯子＋输出上限 ×1.5 见 §6.11 第十四轮、流式上屏节流见 §6.12 第十四轮）落地，第十五轮三件（抽样口径软改硬＋结果页自报选用上屏见 §6.9 第十五轮、第二遍对账加⑤知识库口径见 §6.12 第十五轮）落地，第十六轮两件（第一遍原文冻结＋确认页「上次生成原文」折叠区，见 §6.12 第十六轮）与第十七轮三件（思考开关分家、DeepSeek 官方环境记账、代码地图）相继落地（见 §6.5、§6.12 第十七轮），修订后界面、冷却体感、分批导入手感、轮换体感与第七轮各提示词遵循度待复验）。
