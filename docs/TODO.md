# 待办与待商榷

> 压缩会话/开新会话后从这份文件恢复上下文。概念与领域模型见 docs/DESIGN.md（**动功能前先读它**）。
> 交付状态：T1✅ T2✅ T3✅ T4✅ T6✅ T7✅；B1/B2/B3✅、D1✅、D2/D3✅（2026-08-26 用户开口「按 TODO 清单执行」，全部同轮交付）；E1✅（2026-08-25 用户逐条点名的界面减负 25 条，同轮交付，明细见 DEVELOPMENT_PLAN §12 当日行）。**清单清空**。

## 工作约定（长期有效）

- 非必要不在用户的浏览器里验证功能（用户环境有敏感内容），由用户 Ctrl+F5 自测。
- 推送失败不重试，一次为限；失败的提交等下一轮推送顺带带上。
- 交付管线：改完 → `node --check`（复制到 %TEMP%\*.mjs 逐个查）→ `git add -A && git commit` → 安装副本 `git pull` 本地仓库 → 单次 `git push`。
- 文档写法面向「想干什么 → 去哪里」，不写机制级细节（用户反馈过"无从下手"）。
- **记录纪律（2026-08-26 立）**：写进本文件的条目必须标清来源——「用户拍板」还是「助手自查/提案」。助手提案不得写成"已定路线图"；待用户裁决的积压项要在交付报告里念出来，不靠文件默默转达。
- **清单实时性与旧数据对账（2026-08-26 立，用户口谕）**：任务状态一变（拍板/交付/砍掉）**当场**更新本文件，不等下次交付顺带；任何场合读到与现状不符的旧记录（文档、路线图、代码注释、状态标记），当场指出并修正——改不了的（提交历史这类）在交付报告里点名，不许视而不见。

## A. 任务清单（2026-08-25 与用户对口径定下；概念背景见 docs/DESIGN.md）

1. **T1 ✅ DESIGN.md**：领域模型 + 数据生命周期总账 + 验证两栏制（2026-08-25 交付）。此后动功能前先读它，概念级变更与代码同一提交。
2. **T2 ✅ 随机事件独立入口 + 单元制**（2026-08-25 交付，概念记录在 DESIGN.md 2.5，实现取舍见 DEVELOPMENT_PLAN §12 当日行）：
   - **入口与向导**：随机事件与路人反应同为第 1 步入口键 → 各自悬浮面板（三路掷骰已搬进事件面板，掷骰入口唯一化，事件页签保持库/维度管理）；向导四步（材料→确认→生成→结果），步骤条重编号。
   - **单元制**：产物 = 单元（每工具暂存 ≤3，满 3 禁生成先手删）；跨工具导入 = 唯一影响通道，只能导入「加工史里没有本工具」的单元（同工具回流禁 / 两标满禁结构性成立）；徽章 = 加工史（卡头左 = 先、右 = 后）；导入不消耗（可再导入重 roll / 可自己注入），导入产物 = 带累积徽章的新单元；单元不论徽章数直接勾「参与规划」（2026-08-26 E7 修订：勾选移到第 1 步「插入单元」勾选区，卡上无此开关；E8 再修订：单元＝带标签纯文本提示词块（用户定则），插入单元区扩为查看/勾选/注入/删除/清空一站式，两面板只管生成）；采纳规划后**不清池**，清空键在「插入单元」区各组头部（只清本工具名下）。
   - **数据与接线**：run.eventText/run.reaction 标量 → 单元列表（chatdata 新增 units 块）；向导快照 v2、旧 v1 快照恢复时归一化（攒到一半的事件卡/反应卡自动转成单元）；注入按单元走（注入带 unitId 对账）。
   - **材料口径**：进行中剧情 + 常规材料照旧自动带；**已生效注入不自动进工具生成（防双算）**——跨工具影响只走显式导入；事件/反应/规划三功能共用第 1 步确认的同一套材料模板（两工具生成器都吃 wizardMaterials + importedUnits）；注入模板序固定：剧情 → 事件 → 反应（planner 材料小节排序，无旋钮）。
3. **T3 ✅ 输出契约加固**（2026-08-25 交付）：api.js 共享 parseModelJson——先走 utils.extractJson 本地解析（剥围栏/定位/格式伤修复，原已存在），修不回时把坏输出作为 assistant 消息回传、附一份不感知 schema 的共享修复提示（内容不变只修格式，截断则精简收尾）重发一次原请求，再失败才抛。接入反应卡 / 报告卡 / 自动打标 / 事件三生成器与建库（同族顺手）。联网判断刻意不接（坏了按「不需要」处理，不多花调用）；规划分析不在本条（失败时 err.raw 已把原文带给界面可人工打捞）。
4. **T4 ✅ 镜像定位 UI 说破**（2026-08-26 交付）：记忆表格页镜像分组标题悬浮一句话（镜像只喂规划与检查，主对话模型看原表、删行不影响扮演侧记忆，修扮演侧事实去源插件改）。最小保留，用户可随时砍。
5. ~~T5 整表重写绊线~~ 已砍：大修含改写的代价用户自担（人工核验 + 重打标纳入大修流程，见 DESIGN.md 2.3）。
6. **T6 ✅ 参与召回开关退役**（2026-08-25 交付）：向导勾选足够（用户点名多余）；**召回列保留**（剔除无用列省 token，无更便捷替代）。记忆表格页开关与接线删除，buildMemoryContext / 档位统计 / 向导三处表清单去闸门（表进不进材料只由档位说了算），旧值读取时清残留。
7. **T7 ✅ 检查报告继承向导口径**（2026-08-25 交付）：runStoryReview 按第 1 步选的表/标签召回（单人卡全量无感；群像卡只查同类重复，兜底=指导意见打回）。口径来源 = 对话记忆的 picks 块（第 1 步每次勾选立即写回，与「按建议重写」同一份，检查与重写天然同口径）；记忆小节标题同批注明口径。

节奏共识（2026-08-25）：T2 讨论完 → T3/T6/T7 小修 → 冻结新功能，跑完第一次完整战役 + 首次三千层大修 → 回填 DESIGN.md §7 验证台账 → 自动监听/长线规划只设计不实现（DESIGN.md §6）。

## 2026-08-23 本轮已完成（备查）

1. 上下文收集去重：抽成 context.js 的 `collectPlanningContext()`，planner 3 处 / reactions / randomEvents 统一调用。
2. AI 打标批间容错：失败批次跳过继续、结束汇总失败行数（不勾「覆盖」时再点一次只补漏的行）。
3. 世界书「常驻」开关：条目勾常驻 → 不看关键词恒带出、排最前、不占条数上限但优先吃字符预算；酒馆 JSON 的 constant 导入时继承；检索测试结果标「（常驻）」。

## B. 小修（2026-08-26 逐条裁决；同日用户开口「按 TODO 清单执行」，三条已全部交付）

1. **B1 ✅（2026-08-26 交付）滑动/重新生成会加速注入老化**：计层改按**聊天楼层净增**（用户拍板精确修法）——注入时记下当时楼数（floorBase 基线，楼数 = 角色回复数、user 楼不计），此后已过层数 = 当前楼数 − 基线（夹 ≥0）；滑动/重新生成只替换最后一条回复、楼数不变不吃层；删楼层楼数只减、推导不为负；反应换段/收束提示/到期撤下随新层数自动对齐；老注入首次推进时按现有 age 折算基线、剩余层数不跳变（基线可为负不夹 0，夹了反而放大已过层数）。概念级变更，DESIGN.md §3 注入生命周期同一提交同步。
2. **B2 ✅（2026-08-26 交付）AI 自主掷骰维度名失配**：回传维度名先去空格再精确匹配，仍对不上维持现状记空。
3. **B3 ✅（2026-08-26 交付）空聊天保护**：事件面板「大模型随机 / 掷骰」与反应面板「生成反应卡」入口直接拦截提示，不发模型调用。

## C. 退役记录（2026-08-26 查账裁决：原 C 区系助手自查与提案冒充「已定路线图」，逐条清算）

1. ~~随机事件自动触发~~ **暂否（用户裁决）**：出自早期助手阶段计划、未经用户确认；用户对自动「反向触发」的近期表态也是暂缓（DESIGN.md §6）。
2. ~~场景标签分组注入~~ **删除**：查无对话记录，系助手自造条目。
3. ~~规划结果回写记忆表格~~ **删除（用户确认记岔）**：用户原话诉求 =「总结剧情时实际演过的内容自动入表」，由现有流程覆盖——原生总结写进原表 → 「同步记忆表格」一键并进镜像（插件侧为手动同步；要全自动将来单独立项）。
4. ~~标签词表导入导出~~ **删除（用户 2026-08-26 手动确认不需要）**：设置页「导出备份」已含词表，整体备份够用。

## D. 已批/已立案新项（2026-08-26 同轮全部交付）

1. **D1 ✅ 游戏玩法条目编辑（用户 2026-08-26 拍板）**：条目卡新增铅笔编辑键——卡内就地展开编辑框（名称 / 触发词 / 注入深度 / 常驻 / 正文），保存即回写并按新参数重扫注入，取消收起；入口在底部「游戏玩法」工具区条目处，不进向导运行区。
2. **D2 ✅ 用户面文档过时**：README 与《随机事件说明书》过时段重写——向导四步、双工具入口与单元制、材料同源（反应区独立勾选描述删除）、注入计层新口径、游戏玩法可编辑、开发文档指路改 DESIGN.md。
3. **D3 ✅ 开发计划前半部过时**（按记录在案的推荐执行，未大改）：§1–§11 挂存档横幅（权威已移交 DESIGN.md，§12/§13 之外停维护）；清除未立项 / 已否目标（向量语义检索、i18n、场景标签分组；自动触发改记「已暂否」）；Phase 3/4/5 状态对齐现状；randomEvents.js / tab-events.js 两处「掷骰在向导第 2 步」过时注释更正。manifest 版本号维持不动（用户未开口）。

## E. 界面减负 25 条（2026-08-25 用户对照截图逐条点名，同轮交付）

1. **E1 ✅ 界面减负包（全部用户拍板）**：①注释砍短 + 占位符全去——世界书纯文本导入编辑框、第 1 步构思方向、事件页三个折叠标题与补充说明、玩法区（触发词/常驻/内容/AI 思路）、设置页（供应商方案/试搜），砍掉的说明一律进 title 悬浮；②「书名」改「世界书名称」、「AI 咨询」改「AI 玩法创作」、另附最新行改「标签」启用时每表另附最新 N 行；③第 1 步材料概览统计行从页面撤下，移进「查看完整提示词」弹窗开头；④玩法表单触发词留空自动勾上常驻（有触发词时由用户自定）；⑤设置页四个大区块（大模型连接/联网搜索/高级设置/数据备份与搬家）折叠化，大模型连接默认展开、其余收起；⑥「分析前联网判断」勾选框隐身查证——根因是酒馆自绘勾选框的主题配色在深底主题下与背景同色，插件侧统一补描边根治（§13 已立案）。
2. **E2 ✅ 联网开关正名 + 备份区正名（2026-08-26 用户指正，全部用户拍板）**：①联网勾选框正名「启用联网搜索」——E1 ⑥ 查证结论修正：框从没被删（c59cf6a 诞生即在原位，原名「允许模型自主调用」），01880c2 更名「分析前联网判断」才令用户对不上号，深底隐身是叠加因素（描边修复仍有效）；②「一键导出全部数据」→「导出备份」、「过户到当前聊天」→「备份继承」并与「导入备份」并列同一按钮行，源聊天下拉移到按钮行上方；README 与代码注释同步。
3. **E3 ✅ 联网开关拆双（2026-08-26 用户拍板）**：原一体开关拆成两个并行开关——「启用联网搜索」管总闸（关 = 完全不联网）、「模型搜索前判断」管口径（开 = 先轻量判断要不要搜；关 = 每次直接检索，轻量调用换「直接给关键词」提示词、必搜不判）；设置键 toolMode → enabled + preJudge（旧值迁移、残留清删），总开关关时判断开关置灰；账单 toast / 预览提示 / 确认页状态行按两模式分文案，README 同步。
4. **E4 ✅ 界面整改 12 条（2026-08-26 用户对照截图逐条点名，全部用户拍板）**：①「生效中的隐身注入」搬家——设置页灰框区 → 剧情指导页底部工具区（游戏玩法折叠区之后），去括号，注入增删改（含到期自动撤下）同页即时刷新；②记忆表格页标题正名——「镜像剧情表格」「原表库 · 自动备份」「标签词表」（去掉的括号内容全部进 title）；③打标区域去括号 + 全选框（勾上=全勾、再点=全不勾、子勾选取消则全选勾自动消失）；④第 2 步确认页只显插入单元（材料清单在提示词预览里已有），预览的单元数改常显（0 也显示）；⑤向导按钮正名「开始分析 / 重新生成 / 放弃保存」+ 修改意见框去占位符（说明进 title）+「转为隐身注入」悬浮说明重写（讲清与「确认采用」的区别）。明细见 DEVELOPMENT_PLAN §12 当日行。
5. **E5 ✅ 清空误报双修（2026-08-26 用户报障：记忆表格完好却报「疑似被清空」）**：立案根因 = 重载/切聊天初期大聊天还在加载（ctx.chat 空、chatMetadata 未就绪）而原表库双层存储即时可读，开页签自动同步读到「空表」误判；挂死根因 = 同构分支提前返回不清 wipeAlert，真表恢复后警报不消失。修法 = syncMemory 加就绪门槛（chat 未加载且非 force 直接放弃本轮）+ 同构时清既有误报。用户侧解除方式：Ctrl+F5 后等聊天加载完，点一次「同步记忆表格」。明细见 DEVELOPMENT_PLAN §12 当日行。
6. **E8 ✅ 单元区外置 + 意见走模型 + 反应对齐草稿制（2026-08-26 用户六条点名 + 三点拍板）**：①「或自己给意见」搬回面板顶部（E7 把整块搬去底部是助手自作主张、认账），删「本地零调用直接立单元」路径——改「按意见生成」键走大模型（意见=方向不是剧本，产物同样先出草稿，mode='opinion'）；②意见与随机**严格二选一**（拍板①）：意见框有字时掷骰/大模型随机/导入勾选区全部灰置（pp-off），清空恢复；③「参考事件库」勾选删除（拍板②）：大模型随机**无条件**把事件库启用条目列为「防复刻清单」随行（告诉模型避开库里已有事件，不做勾选），掷骰-维度随机分支不再看库，eventOpts 退役清残留；④单元＝**带标签的纯文本提示词块**（拍板③定则，作为后续一切修改的准则）——两工具面板删池卡与清空暂存键、只留草稿卡；第 1 步「插入单元」区改造为**单元区**：按工具分组（组头带本组清空键）、行=勾选框+完整加工史双徽章+可点开的单元名，展开看提示词全文（unit.text 纯文本块），转注入/层数/楼层预算/删除全在展开态；顺手修「插入单元行只画 u.tool 单标、加工史不显示」的 bug（用户报「导入事件再生成只见一个反标」的实锤位置）；⑤立卡走向裁剪（拍板③接受后果）：草稿三选一或不选，没选=选项全砍、选了=只留那一个，入池即定稿不可改选；⑥反应面板草稿制对齐（生成→草稿→立为单元/丢弃）+ 模型顺给**浓缩短标题**作单元名（原拿即时口径开头硬切 40 字）；计费表更新（按意见生成=1 调用，「意见立卡 0 调用」行删除）。DESIGN §2.5（定则+三处修订）/§2.6/§4/§7、README、随机事件说明书同一提交同步。
8. **E9 ✅ 反应卡显示恢复（2026-08-26 用户点名：「你为什么把原来路人反应的输出规则也给替换了？现在输出的文本根本就不是原来的文本……我根本就没有要求你对这一部分进行任何更改」）**：E8 把「单元＝纯文本提示词块」定则过度套用到了**显示层**——反应草稿卡与「插入单元」区展开卡被换成 composeReactionText 组装后的整块纯文本，E7 的结构化卡面（显著性星级 / 即时口径 / 余波口径 / 底线分栏 / 楼层预算 / 注入正文预览）全丢。修法（事件侧不动）：从 0d8541d 逐字取回 E7 字段卡面，抽成 rxCardFieldsHtml + wireRxCardFields 供草稿卡与单元区展开卡共用，草稿期即可改楼层预算与正文、改动随立卡入池；unit.text 组装全文本身没动（注入与随分析发送仍走它）。教训写进 DESIGN §2.5 澄清括注：定则管**材料语义**，卡面显示保留各工具生成结果的原有形态；说明书 §0/§1.3/§5.2/§5.5 同步。
7. **E7 ✅ 单元制五件套（2026-08-26 用户逐条点名，全部用户拍板）**：①入口键「随机事件（暂存 1）」去掉「暂存」二字只留数字（路人反应同款同步）；②「顺带出预览剧情」删除——查实它只作用于「大模型随机」与掷骰的维度自由分支（掷到条目/AI 自主永远无差别），且模型还常不回 preview 字段，用户勾与不勾无可见变化，全链撤除（payload.preview 读回时清残留）；③「立为单元」重排——大模型随机/掷骰改产**草稿卡**（池顶带「草稿」标记，动作行＝立为单元/丢弃，入池不要求写意见；再生成换掉草稿，随聊天存），「或自己给意见」+「立为单元」整块搬到面板最底部（暂存池之下，E8 已纠正回顶部）；④「参与规划」从两工具卡上撤下——单元=整块提示词，参不参与是规划侧的事：第 1 步入口键下方新增「插入单元」勾选区（读写 unit.inPlan，已注入反应单元灰显防双算，面板变动回来重建）；⑤排版「宁宽勿挤」——悬浮查看器 900→1100、查看器补 .pp-option 两行式排版作用域（根因=T2 把面板搬进查看器后没补 .pp-viewer 作用域，走向选项退回酒馆 menu_button 单字竖排）、menu_button 补 width:auto；铁则写进 §13 与记忆。DESIGN.md §2.5/§2.6/§4/§7、README、随机事件说明书同一提交同步。
6. **E6 ✅ 镜像「添加行」移除（2026-08-26 用户点名：「什么时候做的？删掉好了」）**：功能出自 3687de3（记忆表格v2），DESIGN §2.2 镜像=策展层从未立过「添加」（以删为主、改单元格保留）——每表下方按钮、表体顶部临时编辑行（newRowEditorHtml）、addMirrorRow、bindRowEditor 的 add 双分支全撤，空表提示改「没有行」；存量手加行不删不迁移（照常编辑/删除，仅无新入口）。明细见 DEVELOPMENT_PLAN §12 当日行。
9. **E10 ✅ 跨工具导入＝既定方向（2026-08-26 用户拍板口径 + 助手措辞设计）**：用户报「事件单元导进路人反应好像没起关联」（反向看着有效）；查证＝两边接线对称，差别在任务指令——反应侧「从最近对话里找最引人注目的事」压过导入、事件侧任务开放且反应卡正文浓。用户拍板：「既然做了导入，这个工具生成的东西就应该跟这个单元有关系」，构思「将暂存的剧情视为将要且一定要发生的方向，围绕上下文情景以及暂存剧情的发展来构建」，措辞交助手设计（定名**既定方向**）。落地：reactions.js 系统提示改分流（导入了事件＝锁定它，没导入才从最近对话找）+ 任务小节按有无导入分流；randomEvents.js contextSections 导入小节改「视为已定局的世界状态，新事件必须顺着它发展或正面咬合，不能无关不能矛盾，但出下一件事不复写同一件事」；界面「导入XX单元做参考」全部改名「导入XX单元做既定方向」（两面板勾选区标签 + 入口键/生成键悬浮说明同步）；DESIGN §2.5 立定则、说明书 §0/§1.1/§1.4/§5.2/§7、README 同一提交同步。「按意见生成」不带导入（E8 拍板的独占材料位）不动。
10. **E11 ✅ 导入单选 + 导入产物正文自带前因（2026-08-26 用户两点拍板）**：①「生成的单元在另一个工具里可以同时勾多项，这个不行，设计成最多只能勾选一项」——两面板导入选材改互斥单选（勾了新的自动替掉旧的），悬浮说明同步；②「有两个标签的单元内容只会显示最后出来的工具生成的内容——原始单元删了岂不丢前因后果？输入进去也没法演绎第一个标签的内容」——导入产物正文把源单元内容原样嵌在开头作「前因」小节（存 payload.origin）：事件单元带【前因｜来自路人反应单元……】、反应卡在【路人反应校准｜…】头后带「## 前因（来自导入的随机事件单元……）」，随分析发送、转隐身注入、走向裁剪重算、注入到期逐层重算全带着——原始单元删了链条也在。既有双徽章单元不迁移（维持无前因旧形态），新产物起带。DESIGN §2.5 E11 定则、说明书 §0/§1/§1.2/§1.3/§5.2/§7、README 同一提交同步；明细见 DEVELOPMENT_PLAN §12 当日行。
11. **E12 ✅ 注释与步骤精简六件套（2026-08-26 用户逐条点名，全部用户拍板）**：①预览弹窗「全部展开」键删除（展开/收起逐块点块头，检索自动展开命中的块照旧）；②第 2 步确认页重排——材料细账第 1 步已确认过不再重复，改逐行竖排四行：插入单元·随机事件（**单元名称**名单，不再显示数量）、插入单元·路人反应、玩法（**条目名称**名单）、联网搜索（只写「开/关」，括号里的判断注释删掉进 title）；记忆表格/进行中剧情行撤下（第 1 步「查看完整提示词」弹窗里已有）；③跳转条四步并三步——「③ 生成」撤下（闲时点它也只是落回②，没有自己的页面），「④ 结果」改「③ 结果」，分析实时输出页保留在状态机里不占跳转条一格（开始分析自动进、在途点②回来且②保持高亮、完账自动跳③），running 页标题去掉步骤号；④底部工具区（事件库设置/AI建库/游戏玩法/隐身注入）只在空闲页与第 1 步显示，确认/分析中/结果/检查报告运行页自动让位；⑤「或自己给意见（有字 = 按意见生成，清空 = 随机两键）」改名「指导意见」（括号说明进 title）；⑥草稿卡「走向选项（三选一或不选；都不选＝…裁掉）」只留「走向选项」四字（括号说明进 title）。代码内全部「第 4 步/④/四步」引用同步更正（含「分析已完成点④二检查看」toast 改③结果）；README 功能表行、说明书 §1、DEVELOPMENT_PLAN §12 当日行同一提交同步。
12. **E13 ✅ 顶栏收起修复 + 内嵌区互斥 + 预设区减负（2026-08-27 用户五点逐条点名，全部用户拍板）**：①世界书页「检索测试」「回收站」原先能同时展开前后叠着——改互斥切换：点开一个自动收起另一个，再点同一个键收起；同排的「导入纯文本」编辑框属同类叠层面板（点名同类扫全）一并纳入互斥组。②记忆表格页「打标签」「备份与恢复」同款互斥切换。③设置页预设条目的上移/下移箭头删除（用户：没有上下调整的必要）——预设顺序从此＝新建先后，拼接按列表顺序的语义不变。④供应商方案「存当前」改名「保存」（悬浮说明不动）。⑤开着剧情规划器时点酒馆顶栏按钮（用户设置/背景等）自动收起——该功能代码早就有，但选择器 #top-bar 匹配的是酒馆的空占位 div，真按钮全在 #top-settings-holder 里所以从未生效；修正选择器（补 #top-settings-holder 与 #options 汉堡菜单弹层）后生效。明细见 DEVELOPMENT_PLAN §12 当日行。
 13. **E14 ✅ 全量体检八项修复（2026-08-26 用户拍板「全量检验」；8 项修复为助手提案，其中 ③④ 为过期文案的事实修正）**：①reviewStory 补并发闸——检查/分析共用流式页与 token，混跑会静默作废一次计费调用（补 analyzeBusy 闸 + finally 释放 + 等待提示）；②玩法导入路径深度 0 被 `|| 6` 吞（改 Number.isFinite 判定 + 0-16 夹取，同添加表单口径）；③备份区悬浮说明「最多 20 份」→「最多 3 份」（用户可见改名，实上限一直是 3）；④README 快速上手「四步向导」→「三步向导」（E12 漏改，用户可见）；⑤lorebook addEntry 撤 tags 死参数（标签筛选下线后的僵尸字段）；⑥settings customPrompt 迁移成预设后残留键补 delete；⑦chatdata takeLegacy 改扫全部五个旧键（reaction 块退役后其旧键原本永远迁不走）；⑧style.css 删两个孤儿块（召回预览输出 / 旧版扩散链，全类名扫描 JS 零引用）。明细见 DEVELOPMENT_PLAN §12 当日行。开放项（事件冷却「剩 X 层」显示、注入计层只涨不跌、manifest 版本号）待用户拍板，未立项。
 14. **E15 ✅ 1.0.0 正式交付（2026-08-27 用户拍板「开放项我看也没有什么需要修改的，1.0 版本可以正式交付了」）**：manifest version 0.1.0 → 1.0.0；开放项三条全部关闭——冷却显示维持现状、计层口径维持现状、版本号升 1.0.0。E1-E14 全部交付，插件进入实战使用阶段；后续等首次完整战役 + 首次三千层大修回填 DESIGN §7 验证台账（新功能冻结口径不变）。
