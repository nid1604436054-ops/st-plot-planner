# 待办与待商榷

> 2026-08-23 全量代码审查后的遗留清单。压缩会话/开新会话后从这份文件恢复上下文。
> 交付状态：本地仓库与酒馆安装副本 = 3c6d4cc；GitHub 停在 2719e2e（3c6d4cc 推送时网络失败未重试，下次成功的推送自动带上）。

## 工作约定（长期有效）

- 非必要不在用户的浏览器里验证功能（用户环境有敏感内容），由用户 Ctrl+F5 自测。
- 推送失败不重试，一次为限；失败的提交等下一轮推送顺带带上。
- 交付管线：改完 → `node --check`（复制到 %TEMP%\*.mjs 逐个查）→ `git add -A && git commit` → 安装副本 `git pull` 本地仓库 → 单次 `git push`。
- 文档写法面向「想干什么 → 去哪里」，不写机制级细节（用户反馈过"无从下手"）。

## A. 优化项（已确认值得做，尚未做）

1. **上下文收集去重（优先，建议单独一轮）**：`collectRecentChat + slice(scanDepth) + scanLorebooks` 这套组合在 5 处各写一遍——planner.js 的 collectStats / buildGuidanceMessages / runStoryReview、reactions.js 的 generateReactionCard、randomEvents.js 的 contextSections。抽一个共享函数，以后改检索口径只动一处。
2. **AI 打标批间容错**：某批调用失败会中断剩余批次（落盘问题已在 3c6d4cc 修掉，中断没修）。可改成跳过失败批次继续、最后汇总报告。

## B. 待商榷项（动手前需用户拍板方向）

1. **世界书"常驻"语义**：现状只有导入提醒（无关键词条目导入时 toastr 提醒补关键词）。要不要给条目加真正的「常驻」开关（检索时恒带出，对齐酒馆原生 constant）？中等改动。不做的替代方案：角色名当关键词 = 准常驻（几乎每层命中）。
2. **swipe 加速反应注入老化**：每次滑动/重新生成都触发 MESSAGE_RECEIVED → age+1，卡片会跳层。精确修法是按 chat 长度差计层，但动注入生命周期语义；保持现状只是滑几次加速收束。
3. **AI 自主掷骰维度名失配**：模型回的维度名对不上清单时 recent 记空、密度暂停规则对该次失效。可加 trim/模糊匹配。低优先级。
4. **空聊天保护**：生成反应卡/随机事件在空聊天时照样发模型，浪费一次调用。入口处加拦截即可，很小，之前定性"知道即可"。
5. **游戏玩法触发词扫描窗口**：store.js 的 storageItemsInEffect 写死最近 20 层，不吃设置页 scanDepth。统一口径还是保持写死，待定。

## C. 更早轮次定下的路线图（未经明确要求不动）

1. Phase 3 随机事件自动触发：MESSAGE_RECEIVED 钩子按概率/冷却自动掷骰（条目的 lastFloor 冷却字段已埋好，当前只有向导手动掷）。
2. 场景标签分组注入（injection groups）。
3. 规划结果回写记忆表格。
4. 标签词表导入导出。
5. 游戏玩法条目编辑（当前只有添加 + 删除，改内容得删了重加）。
