# 待办与待商榷

> 压缩会话/开新会话后从这份文件恢复上下文。概念与领域模型见 docs/DESIGN.md（**动功能前先读它**）。
> 交付状态：召回设置退役+全选框（ee6dd4b）与本 T 清单（本次提交）已推送。

## 工作约定（长期有效）

- 非必要不在用户的浏览器里验证功能（用户环境有敏感内容），由用户 Ctrl+F5 自测。
- 推送失败不重试，一次为限；失败的提交等下一轮推送顺带带上。
- 交付管线：改完 → `node --check`（复制到 %TEMP%\*.mjs 逐个查）→ `git add -A && git commit` → 安装副本 `git pull` 本地仓库 → 单次 `git push`。
- 文档写法面向「想干什么 → 去哪里」，不写机制级细节（用户反馈过"无从下手"）。

## A. 任务清单（2026-08-25 与用户对口径定下；概念背景见 docs/DESIGN.md）

1. **T1 ✅ DESIGN.md**：领域模型 + 数据生命周期总账 + 验证两栏制（本次交付）。此后动功能前先读它，概念级变更与代码同一提交。
2. **T2 随机事件独立入口**：改成第 1 步的独立入口键（同路人反应悬浮面板模式），向导收窄为 材料→规划→采纳 主线；生成链接对称读取规则——生成事件/反应时自动把生效中的规划/事件/反应放进材料，注入模板序固定（剧情→事件→反应）。**动工前先与用户过一遍设计**。
3. **T3 输出契约加固**：api.js 共享 parseModelJson（剥代码围栏 + 定位 JSON + 容错），解析失败带修复提示重试一次；接入反应卡 / 报告卡 / 自动打标 / 事件选项四个结构化流。
4. **T4 镜像定位 UI 说破**：记忆表格页一句悬浮提示（镜像只喂规划与检查，主对话模型看原表）。最小保留，用户可随时砍。
5. ~~T5 整表重写绊线~~ 已砍：大修含改写的代价用户自担（人工核验 + 重打标纳入大修流程，见 DESIGN.md 2.3）。
6. **T6 参与召回开关退役**：向导勾选足够（用户点名多余）；**召回列保留**（剔除无用列省 token，无更便捷替代）。
7. **T7 检查报告继承向导口径**：runStoryReview 按第 1 步选的表/标签召回（单人卡全量无感；群像卡只查同类重复，兜底=指导意见打回）。

节奏共识（2026-08-25）：T2 讨论完 → T3/T6/T7 小修 → 冻结新功能，跑完第一次完整战役 + 首次三千层大修 → 回填 DESIGN.md §7 验证台账 → 自动监听/长线规划只设计不实现（DESIGN.md §6）。

## 2026-08-23 本轮已完成（备查）

1. 上下文收集去重：抽成 context.js 的 `collectPlanningContext()`，planner 3 处 / reactions / randomEvents 统一调用。
2. AI 打标批间容错：失败批次跳过继续、结束汇总失败行数（不勾「覆盖」时再点一次只补漏的行）。
3. 世界书「常驻」开关：条目勾常驻 → 不看关键词恒带出、排最前、不占条数上限但优先吃字符预算；酒馆 JSON 的 constant 导入时继承；检索测试结果标「（常驻）」。

## B. 待商榷项（动手前需用户拍板方向）

1. **swipe 加速反应注入老化**：每次滑动/重新生成都触发 MESSAGE_RECEIVED → age+1，卡片会跳层。精确修法是按 chat 长度差计层，但动注入生命周期语义；保持现状只是滑几次加速收束。
2. **AI 自主掷骰维度名失配**：模型回的维度名对不上清单时 recent 记空、密度暂停规则对该次失效。可加 trim/模糊匹配。低优先级。
3. **空聊天保护**：生成反应卡/随机事件在空聊天时照样发模型，浪费一次调用。入口处加拦截即可，很小，之前定性"知道即可"。

（B4 游戏玩法触发词扫描窗口已于 2026-08-24 结案：不并 scanDepth，独立设置项放进游戏玩法折叠区，见 DEVELOPMENT_PLAN 当日记录。）

## C. 更早轮次定下的路线图（未经明确要求不动）

1. Phase 3 随机事件自动触发：MESSAGE_RECEIVED 钩子按概率/冷却自动掷骰（条目的 lastFloor 冷却字段已埋好，当前只有向导手动掷）。
2. 场景标签分组注入（injection groups）。
3. 规划结果回写记忆表格。
4. 标签词表导入导出。
5. 游戏玩法条目编辑（当前只有添加 + 删除，改内容得删了重加）。
