# 剧情规划器（Plot Planner）开发方案

> 本文档是项目的基准文档（single source of truth）。后续所有开发以本文为准；
> 涉及架构、数据结构的重大变更，先改本文再改代码，并在第 12 节记录变更。
>
> 状态标记：✅ 可用 · 🔶 部分实现 · ⬜ 未开始（对照第 8 节路线图）

---

## 1. 项目概述

一个 SillyTavern（酒馆）前端扩展，围绕「剧情编排」提供五个功能板块，
通过 GitHub 仓库分发，自带一条**独立于主界面的大模型 API 通道**。

核心价值：

- **独立通道**：规划、事件生成等调用走插件自配的 API，不占用主对话连接的算力，也不向主对话提示词追加 token。
- **按需检索**：世界书不全量输入，先判断「当前剧情实际用到哪些条目」，只把命中条目 + 必要上下文喂给模型。
- **幕后编排**：隐身注入把幕后内容写进主对话提示词（模型可见、聊天界面不渲染），解决「让模型输出其他角色 → 用户全知视角；不让输出 → 模型缺上下文瞎写」的矛盾。需要连用户一起瞒的内容（如对手手牌）并入剧情规划的「隐藏剧本」设计。
- **轻量依赖**：纯前端 ES Module，零构建步骤，无第三方库。

## 2. 术语表

| 术语 | 含义 |
|---|---|
| 主对话 / 主 API | 酒馆界面里角色扮演所用的连接与提示词 |
| 规划调用 | 插件通过自带 API 通道发起的调用（剧情指导、检查报告、随机事件、路人反应、AI 建库） |
| 世界书（Lorebook） | 条目集合，每条含触发词与设定内容 |
| 检索 | 扫描最近若干层消息，按关键词/正则判断哪些条目被「用到」 |
| 幕后内容 / 隐藏剧本 | 注入给模型、但不出现在聊天界面里的剧情安排或设定 |
| 隐身注入 | 内容写进主对话提示词：模型可见、聊天界面不渲染；由剧情规划 / 随机事件 / 路人反应产生，设置页底部「生效中的隐身注入」统一管理 |
| 密封注入（已移除） | 2026-08 下线：内容对用户也不可见的注入模式。隐藏信息需求并入剧情规划的隐藏剧本；历史密封条目仍保存在设置里，列表只显示指纹 |
| 指纹 | 密封内容只展示「字数 + 校验值」，不暴露正文（现仅用于展示历史密封条目） |

## 3. 已确认的关键决策（2026-08-22）

| 决策点 | 结论 | 备注 |
|---|---|---|
| 分发方式 | GitHub 仓库 + 标准 `manifest.json` | 扩展面板「Install extension」粘贴仓库地址安装 |
| API 通道 | **纯前端直连** OpenAI 兼容接口 | 浏览器直接 fetch；服务商需支持 CORS，遇到再换中转/本地网关；服务端插件代理仅作远期备选 |
| 世界书检索用途 | **只喂给规划调用** | 主对话的世界书照旧走酒馆原生机制，互不干扰 |
| 检索方式 | **关键词/正则**起步 | 预留向量语义检索接口（二期以后） |
| 隐身注入 | **并入各功能 + 设置页管理** | 独立页签已移除（2026-08）：注入由剧情规划 / 随机事件 / 路人反应产生，设置页底部统一查看与撤下；密封模式移除，隐藏信息并入剧情规划 |
| 世界书来源 | 酒馆原生 JSON + 纯文本导入 | 纯文本按第 5 节 M1 的格式约定切分 |

## 4. 总体架构

```
┌ 酒馆页面 ──────────────────────────────────────────────┐
│                                                        │
│  主对话提示词 ←─ setExtensionPrompt ←─ M4 隐身注入       │
│        ↑                                M5 游戏玩法     │
│   （主 API 生成时读到幕后内容，聊天界面不渲染）              │
│                                                        │
│  ┌ 剧情规划器（右侧抽屉） ────────────────────────────┐  │
│  │  M2 剧情指导          M3 随机事件                   │  │
│  │      └──── 共用管线 ────┘                          │  │
│  │  context.js（聊天/角色卡上下文）                      │  │
│  │  lorebook.js（世界书检索）                           │  │
│  │  api.js（独立 OpenAI 兼容通道） ──→ 外部大模型        │  │
│  └──────────────────────────────────────────────────┘  │
└────────────────────────────────────────────────────────┘
```

一次典型工作流：**检索世界书 → 规划 / 随机事件 → 用户审核 → 隐身注入 → 主 API 带着幕后状态继续扮演**。

兼容性封装原则：对酒馆内部 API 的依赖集中在少数文件
（`js/context.js`、`js/injection.js`、`js/store.js`、`js/settings.js`、`index.js`），
酒馆版本变动时只改这些封装点，业务模块不直接碰酒馆 API。

导入约定（ST 1.15.0 实测，务必遵守）：对酒馆内部模块**一律使用绝对路径导入**——
`/script.js`（setExtensionPrompt、extension_prompt_types/roles、eventSource、event_types、saveSettingsDebounced）、
`/scripts/extensions.js`（extension_settings）；不要使用相对层级（`../../…`），扩展目录深度一旦和假设不符就会 404，
表现为扩展加载失败 `[object Event]`。`getContext` 通过全局 `SillyTavern.getContext()` 获取
（1.15 的实现位于 scripts/st-context.js，context 对象含 chat / chatId / getCurrentChatId）。

## 5. 模块详细设计

### M0 基础：设置与独立 API 通道 ✅

**职责**：manifest 分发、设置持久化、OpenAI 兼容调用封装（含 SSE 流式）。

设置存储在 `extension_settings['plot-planner']`，结构：

```jsonc
{
  "api": {
    "baseUrl": "",        // OpenAI 兼容根地址，如 https://api.openai.com/v1
    "apiKey": "",
    "model": "",
    "temperature": 0.7,
    "maxTokens": 1500
  },
  "retrieval": {
    "scanDepth": 20,      // 世界书检索扫描最近多少层消息
    "maxEntries": 10,     // 单次检索最多带出条目数
    "maxChars": 6000,     // 检索结果拼装字符上限（控制规划调用输入规模）
    "contextLayers": 30   // 规划调用携带的最近对话层数
  },
  "lorebooks": [],        // M1
  "injections": [],       // M4
  "storageItems": [],     // M5 游戏玩法条目
  "eventRules": []        // M3
}
```

`api.js` 对外只暴露：

- `chatCompletion({ messages, temperature?, maxTokens?, signal?, onDelta? }) → Promise<string>`
  - `onDelta` 存在时走 SSE 流式，逐步回调累计文本（供后续 UI 打字机效果）。
- `testConnection() → Promise<string>`：发一条 ping/pong 验证配置。
- 错误统一抛 `ApiError`（含 status/body），UI 层 `toastr.error` 呈现。

**CORS 注意**：浏览器直连要求服务商允许跨域。OpenAI 及多数中转支持；
不支持时的对策：换服务商 / 中转 / 本地网关（如 one-api）。
报错信息里已提示这一原因，避免误判为插件缺陷。

**密钥安全**：密钥存于酒馆设置（浏览器 + 服务器 settings.json），仅本插件使用；
不做任何遥测或上传。

### M1 世界书导入与按需检索 ✅

**职责**：导入世界书（酒馆 JSON / 纯文本）到插件自有库；按需检索命中条目；
检索结果**只**用于 M2/M3 的规划调用。

数据结构：

```ts
interface Lorebook { id: string; name: string; enabled: boolean; source: 'st-json' | 'plain-text'; entries: LoreEntry[] }
interface LoreEntry {
  uid: number; comment: string;     // 条目名
  keys: string[];                   // 触发词（子串匹配，大小写不敏感）
  content: string;
  constant: boolean;                // 常驻：不看关键词恒带出（对齐酒馆 constant）
  disabled: boolean;
}
```

酒馆原生 JSON 字段映射（次要关键词 / 正则等高级字段不采用）：

| 酒馆字段 | 本插件字段 | 说明 |
|---|---|---|
| `key` | `keys` | 触发词数组 |
| `content` | `content` | 条目正文 |
| `comment` | `comment` | 条目名 |
| `constant` | `constant` | 常驻 |
| `disable` | `disabled` | 停用 |

纯文本导入：一次粘贴的整块就是**一条**条目（书名 / 关键词在导入框单独填），
不做切块解析；常驻在条目列表里勾选。

检索算法（`scanLorebooks(scanText)`）：

```
输入：最近 scanDepth 层消息拼成的文本（小写化）
对每个启用书籍的每个非停用且内容非空的条目：
  命中 ⟸ constant ∨ 任一关键词为文本子串
输出：常驻条目按 uid 升序排最前（恒带出，不占 maxEntries 名额，
      但优先消耗 maxChars 预算），关键词命中按 uid 升序排后、截 maxEntries 条；
      拼装总字符不超过 maxChars（超出截断并加省略号）
```

**简化点（有意为之）**：次关键词只实现「AND ANY」；
不做递归扫描、矢量深度、时间/概率激活等原生高级特性——插件检索只服务规划调用，够用优先。

UI：世界书页签 = 导入按钮 ×2 + 书籍列表（启用/删除）+「检索测试」（用当前聊天即时验证命中）。

### M2 剧情指导（手动触发）🔶

**职责**：点击「开始分析」→ 收集上下文 + 检索 → 调用独立 API →
输出 **OOC 检测** + **剧情规划（隐藏剧本）**；支持人工编辑与「按修改意见重写」迭代；
产出可一键转为隐身注入。定位是**给固定场景预编排幕后剧本**。

输入构成（拼进 user 消息）：

| 部分 | 来源 | 预算 |
|---|---|---|
| 角色设定摘要 | 角色卡 name/description/personality/scenario | ≤800 字 |
| 最近对话 | `contextLayers` 层 | 可配 |
| 世界书命中 | `scanLorebooks` | `maxEntries`/`maxChars` |
| 上一版规划 + 修改意见 | 迭代时携带 | 全量 |
| 用户补充说明 | 输入框 | 用户自控 |

输出 JSON schema（系统提示词中约定，`extractJson` 容错解析）：

```jsonc
{
  "ooc": {
    "found": true,
    "items": [ { "aspect": "性格|事实|关系|世界观|口吻", "evidence": "对话依据", "severity": "轻微|中等|严重", "fix": "修正建议" } ]
  },
  "plan": {
    "summary": "一句话概括接下来的走向",
    "beats": [ { "stage": "阶段名", "content": "该阶段幕后剧情安排" } ],
    "risks": ["可能跑偏的点"]
  }
}
```

迭代：再次调用时携带 `previousPlan`（当前编辑框内容）与 `revisionNote`（修改意见），
要求模型在原方案基础上修订。

转注入：编辑框最终文本 → `addInjection({ mode:'open', source:'planner' })`，深度默认 4。

**待办**：提示词需用真实剧情实测调优；OOC 严重度阈值与呈现方式。

### M3 随机事件 🔶

**职责**：与 M2 共用管线，但定位不同——不是给固定场景预编排，
而是**开放情境下的动态遭遇**（「走在路上」式），带随机性。

数据结构：

```ts
interface EventRule {
  id: string; name: string; enabled: boolean;
  probability: number;      // 触发概率 0-1（与权重相乘参与抽取）
  weight: number;           // 加权权重（与概率相乘）
  cooldownLayers: number;   // 冷却：间隔多少层消息才可能再次触发
  promptHint: string;       // 事件方向提示，喂给生成提示词
}
```

掷骰算法（2026-08 三板块改版）：先在勾选且有货的掷骰板块（事件条目 / 维度随机 / AI 自主）
里按板块权重抽一个，再走该板块——条目板块内合格条目按 `weight × probability` 加权随机取一
（必出一条，不会因概率未中而空手）；随机板块按维度权重抽方向；AI 自主板块把维度清单交给
模型自选（挑选与生成同一次调用完成，输出附 `dimension` 字段）。板块权重存
`settings.events.branches`（旧 `libraryRatio` 占比自动换算迁移，AI 自主默认不勾）。
冷却：记录 `lastTriggerLayer`，当前消息层数差小于 `cooldownLayers` 则不入池
（自动判定挂 `MESSAGE_RECEIVED` 钩子，Phase 3 实现；当前为手动掷骰）。

生成输出 schema：

```jsonc
{ "title": "事件标题", "description": "遭遇描述（150字内）", "options": [ { "label": "选项名", "hint": "选后的幕后走向提示" } ] }
```

选项处理：选中后组装为明盘注入（事件描述 + 选定走向 + 幕后提示），
默认 `expires: { type:'layers', layers: 20 }`，即 20 层后自动过期撤销。

### M4 隐身注入 🔶

**职责**：把幕后内容注入主对话提示词——**模型可见、聊天界面不渲染**。

注入没有独立页签（2026-08 移除）：产生入口在各功能里——
M2 剧情指导（确认采用自动绑定的剧情注入 / 手动「转为隐身注入」）、
M3 随机事件（事件走向转注入、路人反应卡）；管理入口在设置页底部
「生效中的隐身注入」折叠区（查看 / 停用 / 删除 / 提前撤下）。
原「密封」模式已移除——需要瞒着用户的内容（如对手手牌）并入 M2 的隐藏剧本设计。

**进规划通道**：生效中的反应卡注入（`source='reaction'`）由 `activeReactionInjections()`
筛出（enabled + 当前聊天判定），作为「路人反应」小节自动附带进剧情规划分析与检查报告的
提示词；附带的正文就是主对话提示词里逐层换段的同一份文本——规划/检查模型与主对话模型
看到同一口径。

数据结构：

```ts
interface InjectionItem {
  id: string; label: string;
  mode: 'open' | 'sealed';        // sealed 仅历史条目可能出现
  content: string;
  depth: number; role: 'system' | 'user';
  scope: 'chat' | 'global';      // chat：绑定创建时的 chatId
  chatId?: number;
  enabled: boolean;
  source: 'manual' | 'planner' | 'event' | 'story' | 'reaction';  // manual 仅历史条目
  createdAt: number;
  expires: { type: 'never' | 'layers'; layers?: number };
  age?: number;                  // 已经历的层数（layers 过期用）
  reaction?: object;             // source=reaction：逐层换段所需的反应卡数据
}
```

实现：封装 `setExtensionPrompt('pp:<id>', content, IN_PROMPT, depth, false, role)`；
撤销 = 置空同 key。枚举值从 `extensions.js` 导入并做缺省回退（兼容层集中在 `injection.js`）。

生命周期：

- `CHAT_CHANGED`：撤销不属于当前聊天的 `scope:'chat'` 注入，重放其余启用的。
- `MESSAGE_RECEIVED`：`expires.type==='layers'` 的条目 `age+1`，达到 `layers` 即停用并撤销。
- M5 储存条目共用同一机制，键空间 `pps:` 隔离。

**历史密封条目**：仍保存在设置里不丢；设置页列表只显示指纹，不渲染正文。
导出/备份设置会包含其内容——README 已提醒。

**待办**：场景标签分组（一个场景一组注入、结束时整组撤销）、注入深度可视化预览。

### M5 游戏玩法（原「储存空间」）🔶

**职责**：一次性内容（游戏规则、地图、跑团数值等）的条目库，
按触发条件自动注入主对话，**让这类内容不必专门写成世界书**。
UI 挂在剧情指导页最底部的折叠区（tab-storage.js 的 `renderStorageTools`）。

```ts
interface StorageItem { id: string; name: string; keys: string[]; constant: boolean; depth: number; content: string; enabled: boolean }
```

注入逻辑：`CHAT_CHANGED` / `MESSAGE_RECEIVED` 时扫描最近 20 层消息，
`constant` 或任一 `keys` 命中 → `setExtensionPrompt('pps:<id>', ...)`，否则清空该键。
支持整个库导入/导出 JSON、手动「按当前剧情重放」。

**进规划通道**：生效判定抽成 `storageItemsInEffect()`（与注入同一套），
分步向导第 1 步默认勾选生效条目，勾选的作为「游戏玩法」小节
随 `runPlotGuidance` 发给规划模型；检查报告（`runStoryReview`）自动附带
当前生效条目（检查执行情况时对照）——规划、报告与主对话看到同一批规则。

**待办**：条目编辑（当前为添加 + 删除）、按条目预览命中情况。

## 6. 数据模型汇总

见第 5 节各模块的 TypeScript 定义；所有持久化数据都在
`extension_settings['plot-planner']` 之下，随酒馆设置一起保存（`saveSettingsDebounced`）。

## 7. UI 设计

设置区块（酒馆「扩展」面板内，inline-drawer 折叠）：

```
┌ 剧情规划器 ▾ ────────────────────────┐
│ API 地址（OpenAI 兼容，含 /v1）        │
│ [ https://……                    ]    │
│ API 密钥 [••••]   模型 [        ]    │
│ 温度 [0.7]  单次上限 tokens [1500]    │
│ ── 世界书检索 ──                       │
│ 扫描层数 [20] 最多条目 [10] 字数 [6000]│
│ 规划携带对话层数 [30]                  │
│ [测试连接] [打开剧情规划器]             │
└──────────────────────────────────────┘
```

主面板：右侧抽屉（`min(560px, 94vw)`），四个页签（随机事件与游戏玩法并入剧情指导页下部）：

```
┌ 剧情规划器                      [✕] ┐
│ [世界书][记忆表格][剧情指导][设置] │
│ ┌ 页签内容（滚动区） ───────────────┐ │
│ │ ……                              │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

交互约定：耗时操作按钮点击后进入「规划中……」状态文案；
所有错误 `toastr.error` 弹出；历史密封条目任何位置只显示指纹。

## 8. 开发路线图

| 阶段 | 内容 | 验收标准 | 状态 |
|---|---|---|---|
| Phase 0 骨架 | manifest / 设置面板 / API 通道（含流式）/ 抽屉框架 / 事件挂接 | 扩展可安装启用；测试连接通过；抽屉可开关 | ✅ |
| Phase 1 世界书 | 双格式导入、启停删、检索测试 | 导入真实世界书 JSON ≥1 本；检索测试命中符合预期 | ✅ |
| Phase 2 剧情指导 | 分析管线、迭代重写、转注入 | 在真实聊天中产出结构化 OOC+规划；编辑后注入生效（主 API 回复体现幕后走向且界面无显示） | 🔶 管线可用，提示词待实测调优 |
| Phase 3 注入成熟 | 生命周期、设置页列表管理 | 切聊天自动清理；层数过期自动撤销；设置页可提前撤下 | ✅ 独立页签与密封模式已移除（并入各功能 / 剧情规划隐藏剧本）；场景标签 ⬜ |
| Phase 4 随机事件 | 规则管理、掷骰、生成、选项转注入、自动判定+冷却 | 手动掷骰全流程可用；自动判定挂消息钩子 | 🔶 手动全流程可用；自动判定 ⬜ |
| Phase 5 游戏玩法（原储存空间） | 条目库、触发注入、导入导出、生效条目进规划 | 添加规则条目后关键词出现时主 API 行为变化；向导第 1 步勾选后完整提示词含该条目 | 🔶 可用（UI 并入剧情指导页底部）；条目编辑 ⬜ |
| Phase 6 发布打磨 | 提示词调优、i18n、README、版本发布 v0.1 | 从 GitHub 安装可用；文档完整 | ⬜ |

## 9. 风险与对策

| 风险 | 影响 | 对策 |
|---|---|---|
| 服务商不支持 CORS | 直连失败 | 报错信息已提示；换支持跨域的服务商/中转/本地 one-api；远期做服务端插件代理（决策已排除为首选） |
| 酒馆版本 API 变动 | 扩展失效 | ST API 依赖集中在 5 个封装文件；manifest 声明版本；升级酒馆后跑一遍测试清单 |
| 密钥泄露 | 资损 | 密钥仅存本地设置，无遥测；README 说明 |
| 规划调用输入超限 | 费用/截断 | `maxEntries` + `maxChars` + `contextLayers` 三重预算控制 |
| 历史密封条目意外暴露 | 剧透 | UI 只显示指纹；不进聊天记录；导出设置时的行为已文档化 |
| 模型不按 schema 输出 JSON | 解析失败 | `extractJson` 容错（剥围栏/截取大括号）；失败时展示原始输出供人工处理 |

## 10. 文件结构

```
st-plot-planner/
├── manifest.json              # 酒馆扩展清单（display_name/js/css/version…）
├── index.js                   # 入口：装配抽屉与魔法棒入口，挂接聊天事件
├── css/style.css              # 抽屉与组件样式（尽量用酒馆主题变量）
├── js/
│   ├── settings.js            # 设置单例：extension_settings['plot-planner'] 读写
│   ├── api.js                 # M0 独立通道：OpenAI 兼容 chat/completions（含 SSE；全局预设在此出口统一附加）
│   ├── context.js             # 兼容层：SillyTavern.getContext() 唯一依赖点
│   ├── lorebook.js            # M1 世界书：导入（双格式）+ 关键词检索（关键词 + 常驻；条目标签已退役）
│   ├── planner.js             # M2 剧情指导：分析 + 检查报告提示词组装
│   ├── materials.js           # 共用材料小节构建（materialSections/reactionPicks；预设拼装已随全局化退役，归 api.js）
│   ├── randomEvents.js        # M3 随机事件：维度/条目/掷骰管线、生成
│   ├── reactions.js           # 路人反应卡：口径式生成（材料随反应区自己的勾选）+ 注入正文组装
│   ├── injection.js           # M4 隐身注入：setExtensionPrompt 封装与生命周期
│   ├── story.js               # 进行中剧情 + 历史归档（存 chatMetadata）
│   ├── memoryTable.js         # 记忆表格：原表同步/镜像/打标/召回
│   ├── store.js               # M5 游戏玩法：条目库与触发注入（生效判定供向导勾选共用）
│   ├── utils.js               # escapeHtml / clamp / extractJson / 指纹 / 文件读写
│   └── ui/
│       ├── drawer.js          # 顶部下拉抽屉与页签框架
│       ├── wandMenu.js        # 魔法棒菜单入口
│       └── tabs/
│           ├── tab-worldbook.js
│           ├── tab-memory.js
│           ├── tab-guidance.js
│           ├── tab-events.js          # 随机事件工具区（路人反应/事件库设置/AI建库，挂剧情指导页；掷骰在向导第 2 步）
│           ├── tab-storage.js         # 游戏玩法工具区（条目库/触发注入/导入导出，挂剧情指导页最底部）
│           └── tab-settings.js
├── docs/DEVELOPMENT_PLAN.md   # 本文档
└── README.md
```

## 11. 测试清单（手动验收）

1. 本地安装：文件夹放入 `public/scripts/extensions/third-party/` → 重启 → 扩展面板出现「剧情规划器」并可用。
2. 设置：填 API 后「测试连接」返回 pong；错误密钥给出可读报错。
3. 世界书：导入酒馆 JSON（条目数正确；原生常驻条目自动带上常驻标记，无关键词也恒带出）；既没关键词又没常驻的条目导入时提醒；导入纯文本（整块作为一条条目，关键词可选）；检索测试在含关键词的聊天中命中、不含时不命中，勾「常驻」的条目恒出现并标「（常驻）」。
4. 剧情指导：有 ≥5 层对话的聊天中「开始分析」→ 返回 OOC+规划 JSON 并正常渲染；修改意见重写后内容变化；「转为隐身注入」后设置页「生效中的隐身注入」出现新条目。
5. 注入生效验证：开启酒馆的提示词查看（或观察 token 数变化）确认幕后内容已进主提示词、聊天界面无显示；继续对话主 AI 行为体现幕后走向。
6. 生命周期：切换聊天后 scope=chat 的注入被清理；层数过期的注入自动停用；设置页可提前撤下。
7. 游戏玩法：添加带触发词的规则条目 → 对话提到关键词后主 AI 行为变化；导入导出往返一致；向导第 1 步该条目默认勾选，「查看完整提示词」里含其内容，「检查当前剧情」的报告同样附带生效条目。
8. 路人反应：不填任何事件描述直接「生成反应卡」→ 模型从最近对话认出事件并出卡（材料在反应区自己的「材料勾选」里勾，预设全局自动附带）；确认注入后到期自动撤下（一层 = 一条角色回复）；生效期间向导「查看完整提示词」与检查报告里出现「路人反应」小节，到期后不再附带。

## 12. 变更记录

| 日期 | 变更 |
|---|---|
| 2026-08-22 | 初版：确认五模块方案与关键决策；完成 Phase 0 骨架与 Phase 1-5 的基础实现 |
| 2026-08-22 | 修复 1.15.0 加载失败（[object Event]）：ST 内部导入由相对层级改为绝对路径；符号位置以 1.15.0 实测为准（setExtensionPrompt/枚举/事件由 script.js 再导出，extension_settings 在 extensions.js，getContext 走全局 SillyTavern）；兼容基线定为 1.15.0 |
| 2026-08-23 | 移除「隐身注入」独立页签：注入产生并入剧情指导 / 随机事件 / 路人反应各入口，管理收进设置页底部折叠区；密封模式移除（隐藏信息并入剧情规划隐藏剧本），历史密封条目保留在设置中、列表只显示指纹 |
| 2026-08-23 | 掷骰不再空手：走库时删除「触发概率独立过筛、可能全灭」环节，概率并入权重相乘抽取，必定抽出一条 |
| 2026-08-23 | 掷骰三板块并行（事件条目 / 维度随机 / AI 自主）：板块开关旁直接填权重，每次掷骰先按权重抽板块；新增 AI 自主生成调用（模型从维度清单自选，一次调用完成挑选+生成）；旧「事件库占比」换算迁移；AI 建库从「事件库设置」拎出为独立折叠区（不参与掷骰） |
| 2026-08-23 | 「随机事件」页签并入「剧情指导」页（抽屉六页签→五）：掷骰 / 路人反应 / 事件库设置 / AI 建库成为剧情指导页下部工具区（tab-events.js 改为可挂载的 renderEventsTools）；「参考事件库 / 顺带出预览剧情」勾选改为向导第 2 步与工具区共用同一份状态（自由分支提示词两侧一致）；向导掷骰与大模型随机补防连点锁；聊天切换时事件卡与反应卡随向导一并清空 |
| 2026-08-23 | 掷骰入口收敛为向导第 2 步唯一入口：删除剧情指导页下部的独立「掷骰生成事件」区（与第 2 步完全重复），事件卡新增「转为隐身注入」按钮承接原注入能力（深度 4 / system / 20 层过期）；「参考事件库 / 顺带出预览剧情」回归向导本地状态（撤销 eventsPrefs 共享）；下部工具区只留路人反应校准 + 事件库设置 / AI 建库折叠区 |
| 2026-08-23 | 「储存空间」改名「游戏玩法」并入剧情指导页最底部折叠区（抽屉五页签→四，tab-storage.js 改为可挂载的 renderStorageTools）；生效判定抽成 storageItemsInEffect（与主对话注入同一套），向导第 1 步新增游戏玩法勾选（默认勾当前生效条目，标「生效中」），勾选项作为「游戏玩法」小节随 runPlotGuidance 发送（「查看完整提示词」同步可见）；顺带修复条目导入路径缺 save 导入导致的 ReferenceError |
| 2026-08-23 | 检查报告（runStoryReview）自动附带当前生效的游戏玩法条目（检查执行情况时对照）；游戏玩法折叠区改挂进底部折叠区容器与「事件库设置」「AI 建库」并列（原先独立挂载点跨 flex 边距不合并，间距是其他折叠区间距的两倍） |
| 2026-08-23 | 路人反应校准去掉手填事件：引人注目的事一律由模型从最近对话认出，「补充说明」更名「指导意见」成为唯一输入框；生效中的反应卡注入自动附带进剧情规划与检查报告的提示词（activeReactionInjections + reactionSection，与主对话同一份逐层换段文本，规划/检查与正文同一口径） |
| 2026-08-23 | 上下文收集去重：collectRecentChat + slice(scanDepth) + scanLorebooks 组合抽成 context.js 的 collectPlanningContext，planner（collectStats/buildGuidanceMessages/runStoryReview）、reactions、randomEvents 共 5 处统一口径；AI 打标批间容错（失败批次跳过继续、结束汇总失败行数，不勾覆盖时再点一次只补漏） |
| 2026-08-23 | 世界书条目新增「常驻」（对齐酒馆原生 constant）：勾常驻不看关键词恒带出、排在命中前面、不占 maxEntries 名额但优先消耗 maxChars 预算；条目行加常驻勾选框、检索测试结果带「（常驻）」标记；酒馆 JSON 的 constant 字段导入时继承，导入提醒改为只点破「既无关键词也无常驻」的死条目 |
| 2026-08-23 | 全量代码审查修复：事件库默认条目只种一次（seeded 标记，删空库不再复活）；反应卡扩散链楼层空档归入已开始的最近一段、早于首段用第一段（原先直落末段，首层就会注入收束文案）；「确认采用」后连第 2 步事件闸口状态一并清空（旧事件卡不再带进下一轮）；AI 打标每批落盘（中途失败已打标签不丢）；游戏玩法深度 0 不再被吞成 6（0=紧贴上下文末尾为合法值）；世界书 JSON 导入检测无关键词条目并提醒（常驻条目不再无声变死条目）；魔法棒菜单轮询设上限；文档勘误（纯文本导入早已整块一条、README 去掉不存在的"正则"检索） |
| 2026-08-23 | 界面瘦身：按用户指定清单删除各页说明性灰字共 16 处（记忆表格镜像/原表库空态、剧情状态条与向导说明、归档空态、路人反应说明、掷骰板块/事件条目/事件维度/AI 建库/游戏玩法说明、设置页大模型/联网搜索/高级设置/隐身注入说明）；「指导意见」标签保留字段名只删括号内说明；「暂无归档」删除后归档数量提示（另有 N 条归档）保留 |
| 2026-08-23 | 分步向导界面减负：修复勾选框与文字上下堆叠（酒馆全局把 checkbox 设为块级 grid 所致，抽屉内改回行内排布）；向导各步说明性灰字与括号补充大删（细节转入悬停提示，分析中/检查中只剩标题）；第 1 步材料统计去边框盒改单行、「查看完整提示词」上移到标题行、无可召回表时记忆区块整个不渲染、玩法「生效中」灰字改徽标、构思与修改意见的说明转输入框 placeholder；第 2 步去长说明改标题「随机事件（可选）」；按钮组上方加发丝线、勾选区间距加大 |
| 2026-08-24 | 修复 formatChatLog 未导入（报错「formatChatLog is not defined」）：抽 collectPlanningContext 时 planner/reactions 的 import 行漏带该函数，影响「查看完整提示词」预览、规划分析、检查报告、路人反应生成四条路径；顺带全量核对 context.js 各导出在其他文件的使用与导入均配套，无同类漏网 |
| 2026-08-24 | 模型输出解析失败可自排查：extractJson 报错附原始输出片段（找不到 JSON 给开头、解析失败给结尾并提示截断可能），runPlotGuidance/runStoryReview 解析失败时把原始输出附到错误对象；API 层正文为空时兜底取 reasoning_content/reasoning（部分推理模型/中转把内容放思考字段），仍为空报「思考耗光单次上限 tokens」的可操作提示——针对掷骰成功但分析报「未找到 JSON」的场景 |
| 2026-08-24 | JSON 格式伤自动修复：extractJson 解析失败后先跑修复管线再试（值内未转义英文引号补转义——只看同一行内引号后是否紧跟文字判断内文/收尾、值内裸换行/制表转义、换行分隔的属性缺逗号补上、尾逗号删除），离线 7 类用例全过；7 处要求输出 JSON 的提示词（规划/检查/三类事件生成/AI 建库/路人反应）统一加「值内不用英文双引号、不换行」规范——针对随机事件生成报「Expected ',' or '}' after property value」（模型在中文值里放了未转义英文引号，非截断） |
| 2026-08-24 | 空内容报错带证据：pickContent 兼容分段数组正文（content:[{type:'text'}]），取不到正文时把 finish_reason、completion_tokens、消息原文字段亮进报错——length=思考耗尽上限、content_filter/sensitive=服务商安全过滤、stop 无正文=疑似过滤或回答在不认识字段（把报错发维护者即可适配）——针对「9984 tokens 仍报空内容」（长度不是唯一原因） |
| 2026-08-24 | 新增「关闭思考」开关（设置 → 高级设置，默认关）：勾上后请求体附带 GLM 系 thinking:{type:'disabled'} 与 Qwen 系 enable_thinking:false，端点对陌生参数报 400/422 时自动去参重发一次——针对推理模型把全部「单次上限 tokens」耗在隐藏思考上（completion_tokens 打满、content 空、无思考字段），调大上限救不了的场景 |
| 2026-08-23 | 向导第 1 步「查看完整提示词」预览顶部新增 token 统计（utils.js 抽 estimateTokens：中日韩全角字符≈1、英文数字≈4字符=1 粗估，离线 7 用例全过），显示合计及系统/用户消息分项、悬停说明估法，并点破「输入不占单次上限 tokens」——方便在发起分析前看清输入规模（检索限制全 0/不限时输入容易超大） |
| 2026-08-24 | 联网搜索往返账单透明化：chatCompletionWithTools 逐轮累计服务商返回的真实 usage（请求次数/输入/输出 tokens）随结果返回，分析与检查结束后 toastr 汇报「往返共 N 次请求（每次重发全部材料），累计输入约 X tokens」；向导预览与「分析前确认」页标注联网搜索的累加提示——针对单次材料实际约 3.5万 tokens、服务商后台却见 19万 总输入的对账困惑（预览粗估 5.7万 偏保守约 1.6 倍，实际偏差方向不会让单次请求超过预估值；19万 = 单次 × 工具循环最多 5 次请求的累计） |
| 2026-08-24 | 材料规模可精确对账：buildGuidanceMessages 返回逐小节精确字数（sections，字符数非 token 估算），预览头部新增「材料共 X 字」与「材料构成」（角色摘要/最近对话/世界书命中/记忆表格…各节字数，悬停说明世界书一节只含关键词命中或常驻条目——排查「世界书三万五千字为何没全进提示词」）；chatCompletion 增 onUsage 回传实报 usage，分析与检查（含单次调用）结束一律 toastr 报「输入 X · 输出 Y tokens（服务商实报）」；空内容 finish_reason=length 报错补强：点破计费口径「输出 tokens 通常含思考」、消息原文无思考字段即端点扣下了已生成内容，「关闭思考」勾上仍报错时提示端点多半没真正执行参数——针对「9984 是不含思维链的真实输出已计费」的质疑（若真生成了 9984 tokens 正文，content 里应有被截断的半截文本而非空串）；全查四层检索限制 0=不限实现无丢材料路径 |
| 2026-08-24 | ①随机事件材料与第 1 步全同步：抽 materialSections 共用（角色摘要/对话/世界书命中/记忆表格/游戏玩法/路人反应/进行中剧情/历史摘要同一批），向导第 2 步三类事件生成（条目/自由/AI 自主）带第 1 步的本次材料选择，预设同样拼进系统提示词（带 JSON 格式保护语）——原先掷骰只带对话+世界书命中，与预览对不上账；②失败也报真实账单：分析与检查调用失败时照样 toastr 实报输入/输出（chatCompletionWithTools 把累计 usage/searchLogs 附到错误上，单次路径 catch 后补报），pickContent 空内容证据加「输入实报 X tokens」——空内容报错时也能对账；③清空归档 toast 点破「进行中剧情不受影响，别再用它清提示词，要停用点结束剧情」——针对「剧情注入清不掉」（核查两条删除路径均有效：结束剧情删注入+置空 activeId、设置页隐身注入删除过滤+撤销，残留多半是进行中剧情本身或记忆表格/世界书内容，看预览材料构成即知在哪节） |
| 2026-08-24 | 修复扩展整体消失（魔法棒里找不到）：上一轮 randomEvents.js 新增导入 planner.js 的 assemblePresets，但该函数一直只是模块内部函数、漏加 export——浏览器 ES 模块在链接阶段直接报「does not provide an export named 'assemblePresets'」，index.js 一路引到它、整个扩展加载失败。node --check 只查单文件语法、查不出跨文件断链，本次补 export，并用链接检查脚本全量扫描所有具名导入/导出匹配（确认仅此一处断链），今后交付前例行运行 |
| 2026-08-24 | 修复开搜索工具后输入按请求次数翻倍计费：旧实现把全套材料（数万 token）交给 web_search 工具循环，tools 协议要求每搜一轮原样重发全部 messages，3 轮就是 4~5 倍材料费。现拆两段——①检索助手子调用：只发千字级剧情简报（角色摘要 200 字/进行中剧情节选 500 字/历史摘要/事件节选/构思/最近 6 层对话节选 800 字），模型在这一小份上自主检索（轮次上限 2），汇总成不超过 300 字的检索纪要；②正式分析：全套材料只发一次（不带工具），纪要作为「联网检索纪要」小节附进。检索失败不拦分析（警告后继续，已产生的检索计费照实累计）；账单 toast 改为「联网检索 N 次轻量请求（只发剧情简报，全套材料只发 1 次）+ 合计输入/输出（实报）」；设置开关与向导预览的说明文字同步更新 |
| 2026-08-24 | 修「开搜索就逢开必搜」：上一版检索助手的角色设定＋工具挂在请求里，模型不管剧情需不需要都会搜。改为判断与执行分离——①判断调用：无工具的轻量请求（千字简报），提示词明确「默认判不需要，联网是例外而非例行步骤」并列出需要/不需要的典型（真实历史/地域/机构/行业/法律/医学运作、时效信息、写错露馅的专业细节＝需要；纯虚构设定、情感走向、通用常识、可模糊带过＝不需要），只输出 {need, reason, queries}；②执行：判「需要」才按它给的关键词（上限 3 个）本地直调 Tavily——搜索本身不再经过大模型、不耗模型 token，每词取 3 条、每条 200 字整理成纪要附进材料。判断输出解析失败按「不需要」处理（宁可少搜不多搜）；大模型工具循环（chatCompletionWithTools/postCompletion/WEB_SEARCH_TOOL）整体退役删除；账单 toast 区分「判断 1 次：不需要（理由）」与「判断 1 次 + Tavily 直查 N 个关键词」；设置开关更名「分析前联网判断」，README/预览提示同步；两处「检索任务」措辞从「收集背景信息」改为「判断是否需要」（旧措辞本身就在诱导搜索） |
| 2026-08-24 | 分析/检查改流式显示＋延迟优化＋状态审计：①「第 3 步分析中」「检查当前剧情」页新增流式输出区——分析调用带 onDelta 走 SSE，模型原始输出逐字滚入（限高 280px 自动贴底），阶段标注（联网判断/检索中 → 模型输出中·已接收 N 字）；流式时请求带 stream_options.include_usage 请服务商在末包回传 usage（不认该参数自动去掉重发；仍未回传则账单 toast 如实说明「无实报数字」），流式正文为空时兜底聚合思考字段增量（与非流式 pickContent 同口径）。②联网判断预跑：进入「分析前确认」页即开跑（材料与事件此时已定型），分析时指纹（判断简报全文哈希）一致才采用——换事件/改构思/对话推进都会自动作废重判；打回重写不吃预跑缓存（修改意见可能改检索方向，且修改意见已纳入判断简报），一律重新判断。③向导状态审计修复：打回（全保留只覆盖产出）、不保存（清产出留输入）、确认采用（全清）路径核对无误，预跑句柄三处清理点补挂；新修两个真问题——分析/检查在途时切换聊天，旧结果会写进新聊天的向导（新增 analyzeToken 会话令牌，resetGuidance 递增作废在途回调与结果）；「分析前确认」页残留旧工具循环时代的「多轮往返会累加输入」提示字已改 |
| 2026-08-24 | 「剧情进度」检查项拆两套变体：此前输出 schema 无条件要求 progress 字段（只注明无剧情写「无」），首次生成时模型照样编出阶段/百分比、第 4 步照实渲染，出现「还没采用的规划先有进度」。现改为存在进行中剧情才要求该字段——planner.js 的 schema/系统提示词按 hasActivePlan 条件拼装（无剧情版本整体不含 progress，检查任务清单同步去掉「推进到什么程度」子句），GUIDANCE_SYSTEM_PROMPT 常量改为 guidanceSystemPrompt(hasActivePlan) 函数；向导 startAnalyze 记录 run.hadActive，第 4 步「剧情进度」行仅在该值为真时渲染（打回重写/重新分析每次重算，首次生成与无剧情打回均不显示）；「查看内置指令」预览跟随当前有无进行中剧情显示对应版本并注明差异；三处状态重置（打回前的丢弃、确认采用、切换聊天）同步清 hadActive |
| 2026-08-24 | 检查结论强制落进规划（判了不改等于白判）：分析系统提示词新增「检查与规划必须耦合」硬要求——checks 判出的每个问题都要在 plan 里得到处置（OOC 在 beats 写明怎么拉回人设/事实/关系，剧情与文风重复写明怎么绕开、往哪个新方向走，需要扮演模型持续注意的规避要点放 risks），不允许 checks 报了问题而 plan 与之无关；risks 的 schema 提示同步改为「可能跑偏的点；checks 判出、需要后续持续规避的要点也放这里」。修正随 planText（summary+beats+risks）注入自然到达人格模型，第 1 步「查看完整提示词」与预设区「查看内置指令」预览自动同步；不加 schema 字段、不拆二次调用（fixes 字段不会被注入、拆调用账单翻倍，均否决） |
| 2026-08-24 | 向导第 1 步勾选按对话记忆：记忆表格的表范围/标签匹配/标签、游戏玩法与预设的临时勾选原先只存内存，确认采用或切换聊天后清空，同一对话下一轮规划要全部重勾。现每次勾选立即写进 chatMetadata.plotPlannerPicks（防抖存聊天文件），进第 1 步时自动恢复，「按建议重写」也取同一批材料（与 startCollect 同口径）；presetIds 改 null 语义区分「未初始化（默认勾启用中的）」与「明确全不勾」，全不勾的选择也能留住；没动过勾选的对话仍用原默认（全部表全量、预设=启用中的、玩法=生效中的） |
| 2026-08-24 | 世界书「启用」改按对话绑定：原先书级启用存全局设置、对所有对话生效，多世界多对话每次切换要来回重勾。现勾选写进 chatMetadata.plotPlannerBooks.enabledIds（随聊天文件保存），每个对话一套书单、切换自动恢复各自的；某对话第一次勾选时先把各书现状快照成书单再改这一本，没动过勾选的对话沿用各书全局默认；新导入的书自动加进当前对话书单（否则已绑定的对话里新书面板是灭的，像没导成功）；检索管线（规划/检查/事件/反应/检索测试）统一按当前对话书单过滤（scanLorebooks 新增 enabledIds 参数，collectPlanningContext 注入），世界书页顶部加说明、切聊天时该页开着自动刷新。选「绑定对话」而非「自动检测」：条目级关键词命中本身就是自动检测，书级开关的价值是把别的世界（尤其不看关键词恒带出的常驻条目）挡在门外，全开自动挡不住跨世界污染 |
| 2026-08-24 | 「规划预设（固定要求）」管理区从剧情指导页底部挪到设置页（应用户要求：规划页只留规划流程本身，固定要求的维护归设置）：tab-guidance.js 摘除预设区挂载/状态/函数（guidanceSystemPrompt 导入随之移除），管理实现整体迁入 tab-settings.js（新增导入 planner/story，元素 id 改 pp_set_preset*，折叠/编辑中状态改为设置页模块内保持），功能不变——多条命名预设、勾选启用按顺序拼进系统提示词、上移下移、即时保存、「查看内置指令」预览（跟随当前有无进行中剧情显示对应版本）；向导第 1 步的临时勾选（按对话记忆）原样保留，其 tooltip 改为指向设置页入口 |
| 2026-08-24 | 游戏玩法区两处小改：①「按当前剧情重放」更名「立即重扫注入」并加悬浮注释——立刻按最近对话重查各条目的触发词：命中的注入、未命中的撤下；平时切对话/收到新消息自动做，自己编辑或删除消息后用它手动对齐；②触发词扫描窗口从写死 20 层改为可设置：settings 新增 storageScanLayers（默认 20、老安装迁移补 20、0 = 不限扫全部对话，与 scanDepth 同约定），游戏玩法折叠区新增「触发词扫描楼层」输入框（改动即保存并立刻按新窗口重扫），store.js 的 storageItemsInEffect/scanAndApplyStorage 默认窗口改从设置取、显式传参不受影响；主对话自动扫描（切对话/新消息）、向导第 1 步默认勾选与「生效中」徽标、检查报告附带的玩法条目全部跟随新窗口。TODO B④ 结案：不并 scanDepth，独立设置 |
| 2026-08-24 | 路人反应校准 v2——口径化并与规划协同（应用户两轮反馈：①生成文本不合理，唯一有用的是即时写法，3~4 层根本扩散不开、镜头硬切到 NPC 身上写内心戏；②要与剧情规划协同，材料须能带预设和选择性的世界书）：生成材料改为与向导第 1 步同一批勾选（新抽 materials.js：materialSections/picksMaterials/assemblePresets/withPresets 自 planner.js 迁出，planner 转发导出并在外层插「路人反应」小节，避免 planner→injection→reactions 成环）——预设拼进系统提示词、世界书按对话书单检索命中、记忆表格与玩法按勾选附带、另带进行中剧情；卡片结构砍掉按楼层分段的扩散链，改为 aftermath 余波口径单段（显著性 1-3 不外传自然平息、4-5 给传播走向，只给方向不写场面），提示词立铁律：反应织进当前场景、不切镜头到不在场者、不写路人内心戏、不虚构路人跟进后续；楼层预算改按显著性匹配（1-2 级 2-4、3 级 4-8、4 级 10-18、5 级 16-30）并在界面标注一层=一条角色回复（tick 挂 MESSAGE_RECEIVED 本就只数 AI 回复，user 消息不计，此前不可见）；旧版扩散链卡片（存在注入里的）由 composeReactionText 兼容分支继续逐层换段直到到期 |
| 2026-08-24 | 反应卡材料勾选独立化（应用户反馈：不该跟规划全量提示词挂钩、要去别的板块删减太麻烦）：上一版「材料随向导第 1 步勾选」改为反应区自带「材料勾选」折叠——世界书（默认＝本对话书单，点任意一本切为独立勾选）/记忆表格（勾选的表全量，默认不附带）/游戏玩法（默认＝生效中）/预设（默认＝启用中，拼进系统指令）/进行中剧情（默认附带）五组各自勾选，存 chatMetadata.plotPlannerReactionPicks（随聊天文件，与 plotPlannerPicks 互不影响）；materials.js 的 picksMaterials 换成 reactionPicks/saveReactionPicks，collectPlanningContext/materialSections 增加 enabledIds 书单覆盖参数（缺省仍＝本对话书单，规划/检查/事件路径不变） |
| 2026-08-24 | 世界书条目标签 + 反应卡按标签筛条目（应用户反馈：反应区世界书只有书级勾选、缺书内标签筛选）：LoreEntry 新增 tags 字段（世界书页条目行就地编辑，逗号分隔，全局共享；不参与关键词检索、只作筛选用途，酒馆 JSON 无此字段、导入后正常为空），scanLorebooks 新增 tagFilter（数组＝只带带其中任一标签的条目，常驻同样受筛；空数组＝一条不带，与缺省不筛语义不同），collectPlanningContext/materialSections 贯通 loreTags；反应区「材料勾选」的世界书组在书单之下新增「按标签筛选条目」层——标签 chips 计数只统计当前书单内启用条目、换书范围就地重建，开关与勾选随对话记忆存 plotPlannerReactionPicks.loreMatch/loreTags，开了筛选没勾标签时提示「世界书条目将一条都带不出」；规划分析/检查路径不传 loreTags 维持原样（筛选只管反应卡） |
| 2026-08-24 | 修第 4 步「注入参数」四组（深度/角色/过期/层数）竖排错位（应用户反馈：UI 丑且中文一个字一行）：根因是 CSS 层叠反杀——「抽屉内表单控件占满宽度」的全局规则（.pp-drawer input/select{width:100%}，初始提交就有、位于文件尾部）与 e9c56b1 加的小尺寸覆盖（.pp-gd-injrow input/select{width:auto}）同优先级（0,2,1）且写在它前面，后者从未生效过，四个小框被拉满宽、label 挤成窄条、中文逐字竖排堆叠。修法照 .pp-grid-tags 先例给选择器加 .pp-drawer 前缀抬到（0,3,1）稳赢，数字框定宽 4.5em 紧凑内边距、下拉按内容自适应，label 加 white-space:nowrap 双保险；全库排查同款「label 内嵌 number/select」仅此一处（pp-branch/pp-tag-vocab/pp-entry-* 靠位置靠后或 flex-basis 兜底，无同类问题） |
| 2026-08-24 | 接上：竖排修好后用户反馈深度/层数两个数字框「突然一片白」——这四个控件是全扩展唯一没挂 text_pole（酒馆深色输入框样式类）的裸 input/select，裸数字框吃浏览器默认白底。四个（深度/角色/过期/层数）统一补挂 text_pole，与全库其他输入框同款深色主题 |
| 2026-08-24 | 事件卡走向选项改版 + 注入层数可改（应用户反馈：①随机选项区文字横排挤到一块、页面宁可做长一点滚动没关系；②「转为隐身注入」写死 20 层想自己改）：走向选项从「走向 —— 提示」挤同一行改为整行卡片两行排版——走向加粗一行、幕后提示淡色小字一行，内边距与上下间距拉开，选项组上方补「走向选项」说明标签（选中反馈仍在组下方）；事件卡标题/描述/预览各字段补间距行高。「转为隐身注入」旁新增「注入层数」数字框：默认 20、1–200 夹取、text_pole 深色、4.5em 定宽（选择器带 .pp-drawer 抬到 (0,3,1) 防 width:100% 全局规则反杀，同 injrow 教训），input 实时回存 ev.injectLayers（点选走向整卡重渲染不丢），注入过期层数与成功 toast 按所填值生效；按钮 tooltip 顺带写明该按钮不经过第 3 步分析、也不影响事件作为材料融入本次规划 |
| 2026-08-24 | 世界书页三件事（应用户反馈：①删除想能恢复；②条目「启用+常驻」两个勾丑，要换别的表达；③问关键词检索与标签检索为何并存、关键词命中不就注入了）。① 回收站：删书/删条目从「直接过滤掉一锤子买卖」改为先进 settings.lorebookTrash（上限 30 条丢最旧，ensureDefaults ??= 迁移老安装），lorebook.js 新增 trashBook/trashEntry/restoreTrashItem/purgeTrashItem/clearTrash；页面顶部「回收站（N）」折叠区：恢复（书按原 id 放回、id 撞车换新，故各对话按对话记忆的启用勾选自动跟着回来；条目回原书、原书没了按书名找、uid 撞车换新；失败留在回收站 toast 提示先恢复原书；恢复顺手展开那本书）、单条彻底删除、清空（两次点击确认：点一次只变「确认清空？」文案再点才执行——库里无 confirm 先例、不用原生弹窗）；删除 toast 改「进了回收站，可恢复」。② 条目行两个勾（data-een/data-econst）合并成一个三段式状态选择器 .pp-seg：停用＝disabled / 关键词＝enabled+非常驻 / 常驻＝enabled+constant（底层字段不动，scanLorebooks、全选全不选、导入无关键词提醒全不受影响）；点击就地翻高亮不整列表重渲染（不丢输入焦点与滚动位置）并刷新书的条目计数；选中段加粗、关键词淡蓝 #7fb3ff、常驻沿用注入开徽章绿 #7fd18a、未选中段压 0.45 透明度。③ 页面顶部补说明行直接回答疑问：这套世界书是插件自己的资料库、不接入酒馆原生世界书、日常聊天不注入，只在插件调模型（规划/路人反应）时检索最近对话；停用/关键词/常驻/标签分工一句话讲清；检索测试命中规则、导入无关键词提醒同步改「状态」口径 |
| 2026-08-24 | 世界书标签退役 + 记忆表格「防重复标签」上线 + UI 说明文字全面转悬浮（应用户反馈：①世界书有关键词就够、标签的主场在记忆表格，条目标签框和说明行看着乱，去掉；②还原最初构思——同区域多角色的公共事件（如约会）流程高度雷同，把这类事件打上类型标签，规划时自动读到并执行「不要重复」提示词，没有就加上；③功能下面经常挂几行甚至十几行小字说明太乱，改悬浮；按键太挤，空间够、页面宁长勿挤）。① 拆除：世界书条目「标签」输入框与 data-etags、反应区「按标签筛选条目」层（loreMatch/loreTags/chips/提示全下线，plotPlannerReactionPicks 里的旧字段自然失效不留）、scanLorebooks 的 tagFilter 与 normalizeEntry 的 tags 字段（存量数据里的 tags 键保留不动、不再参与任何检索）、context.collectPlanningContext/materials.reactionPicks 的 loreTags 贯通、世界书页两行说明字；关键词+常驻就是世界书检索的全部。② 防重复：记忆表格「打标签」词表每行加「防重复」开关（matchTags 条目 repeat 标记，随聊天存），memoryTable 新增 repeatGuardTags/buildRepeatGuardContext（带防重复标签的镜像行→「· [标签]（表名）列:值」行式文本，2000 字截断，不受召回开关/表范围限制——查重与背景召回是两条通道）；materials.materialSections 加 repeatGuard 开关在记忆小节后插「## 已发生的同类事件（防重复：这些同类事件已经写过，新规划不要再复刻其流程与桥段——可以再安排同类型事件，但过程、重点或走向必须有明显新意）」；planner.materialSections 统一开（规划分析+随机事件生成+完整提示词预览同批材料），反应卡走 materials 底层不带；collectStats 加 guardRows，向导第 1 步与分析前确认统计行显示「防重复 N 行」（悬浮讲口径）。③ UI 整改：世界书页两行说明、检索测试/回收站说明、记忆表格打标/备份/召回/已删除四段说明、事件库 AI 建库草稿说明、提示词预览页两行长说明——可见小字全部撤掉，内容并入就近 title 悬浮（统计行/空态提示保留）；.pp-btn-row 与 .pp-section gap 8→10px、.pp-drawer .menu_button 统一 padding 6px 12px、词表行 .pp-vocab-guard 开关（灭＝淡色、亮＝绿描边，padding 覆盖带 .pp-drawer 防层叠反杀）、废弃 .pp-entry-tags 规则删除 |
| 2026-08-24 | 防重复开关整体拆除，改为「记忆行自带标签 + 规划提示词一句话」（应用户反馈：开关在词表行里根本没显示出来——两个 100% 宽输入框把行尾按钮顶出可视区；且方向错了，一句提示词就能解决的事不该做成功能，白增加用户与打标 AI 的工作量）。改动：buildMemoryContext 的每行行尾附带该行标签（`标签:约会/日常`，模型由此看见「同标签的同类事件已有多条」）；guidanceSystemPrompt 增加一句——记忆表格里若已有多条同标签或同类型的既有事件，视为这类事件已经写过：可以再安排同类事件，但不要复刻已有记录的流程与桥段，过程或走向须有新意；repeatGuardTags/buildRepeatGuardContext、materials.repeatGuard 参数与「已发生的同类事件」小节、planner 统一开与 collectStats.guardRows、向导两处统计段的「防重复 N 行」、词表行开关与 .pp-vocab-guard 样式全部移除；matchTags 里已写的 repeat 标记留在存档不清理、自然失效。零新增操作：标签照常打，防复刻自动生效 |
| 2026-08-24 | ①AI 打标多标签（应用户要求：每一栏可同时获得多个标签，符合条件就打、是否符合模型自判）：打标系统提示词从「每条挑 1~3 个最贴切」改为「选出该条目符合的所有标签——可同时命中多个、不设数量上限、符合与否按词表与注释自判，全不符合给空数组」，解析侧 `.slice(0,3)` 截断移除，记忆页「打标签/标签词表」悬浮说明同步；②预设全局化（应用户要求：发送给大模型的任何文本都必须附上启用了的预设）：拼装与注入统一收进 api.js——`globalPresetBlock`/`withGlobalPresets` 在 `chatCompletion` 出口把启用中的预设按顺序拼进第一条 system 消息末尾（头部带「不改变本任务输出格式」保护语），规划分析、检查报告、联网判断、三种事件生成、AI 建库、路人反应、AI 打标全部自动带上（连通性测试例外，`skipPresets` 只验管道）；assemblePresets/withPresets 及 planner 转发导出、reactions 的 presetIds 勾选、randomEvents 的 eventSystem 拼装、向导第 1 步「本次启用的预设」勾选（run.presetIds / plotPlannerPicks.presetIds）、story 条目 presetIds、反应卡材料勾选的预设块全部退役（历史数据保留、读回忽略）；设置页启用开关成为唯一开关，标题改「预设（全局固定要求）」、原可见说明小字折进区块悬浮；向导「查看完整提示词」改用 withGlobalPresets 同口径拼装（看到的=发出的），两处统计行显示全局启用数 |
| 2026-08-24 | 每聊天数据搬出聊天文件（治「点插件一卡一卡」：插件原先把记忆库/剧情档案/各类勾选/书单存 chatMetadata，几乎每次点击都触发一次 saveChat——酒馆在主线程上把整个几千层的大聊天（实测 6.4MB，最大分支 278MB）重新序列化，卡的是这个不是电脑配置）。新架构 js/chatdata.js 双层存储：热层 localStorage（每次改动同步写、只串本块数据毫秒级、完全不碰聊天文件）+ 冷层 settings.chatData 随全局设置存服务器端 settings.json（清浏览器缓存不丢），冷层只在低频时机冲写（切聊天/确认采用剧情/挂检查报告/表格合并/关面板/5 分钟兜底，走 saveSettingsDebounced 1 秒防抖只序列化设置文件）；读序＝会话缓存→热层→冷层→旧 chatMetadata 一次性迁移（迁完即从聊天文件删除，顺带瘦身 206KB，3 秒防抖一次 saveChat）。五个数据块改造：memoryTable 的 plotPlannerMemory、story 的 plotPlannerStory、向导勾选 plotPlannerPicks、反应卡勾选 plotPlannerReactionPicks、对话书单 plotPlannerBooks 全部改走 loadChatData/saveChatData，context.js 的 chatMeta/persistChat 退役删除；聊天身份＝角色头像｜聊天文件名——开分支/改名/换新聊天会被认成新聊天。附带：记忆库自动备份上限 20→3（每个聊天各自 3 份，加载时一次性收紧）；设置页新增「数据备份与搬家」区——一键导出全部数据成 JSON（全局设置+所有聊天数据，含 API 密钥，存任意位置）、导入恢复（全局整份覆盖+按聊天合并，导入后作废会话缓存与热层以冷层为准）、聊天数据过户到当前聊天（深拷贝别的聊天身份的全部数据块，配合导出导入覆盖「一万层换新聊天/开分支/改名」三个场景）；README 功能表加「数据备份与搬家」行。已知边界（向用户明示过）：酒馆自身每条消息的保存与记忆增强表格写楼层快照的卡不归本插件管，维持原样 |
| 2026-08-25 | 记忆表格标签检索 + 面板缩放 + 表格滚轮横移（应用户三件事：①记忆表格页要能按标签名搜到打了该标签的条目；②面板字太小——页面长度不限可以一直滚，但字不能这么小；③表格只能上下滚，放大后没法左右翻）。① 标签检索：镜像区标题下方新增「按标签检索镜像行」输入框——部分字样、不分大小写匹配行标签（state.tags[rid]），只列命中行、整表无命中先藏起来，右侧统计「命中 N 行 · M 表」（无命中显示「没有命中」），行号沿用原表序号与全貌视图对齐；检索框放在列表容器外，输入只重渲染列表、焦点光标不丢；清空即恢复全貌，纯显示层过滤不动数据。② 面板缩放：抽屉头部新增步进器（− / 百分比 / ＋，80–160% 步进 10，点百分比回 100%），存 settings.uiZoom（顶层缺省由 ensureDefaults 循环自动补老安装）；实现只给 #pp_tab_content 设 CSS zoom——抽屉壳宽度不动、内容等比放大页面变长（用户口径：宁长勿小），记忆表格默认 100% 尺寸不变；zoom 之下 scrollHeight/clientHeight 等几何量均为视觉像素，fitHeight 各量纲一致公式不用改，换挡时清空旧高度重新自适应（用户拖过的也重算）。③ 表格横移：.pp-mem-gridwrap 本就有 overflow-x:auto，另在页签容器上挂一次性 wheel 委托（bindGridWheel，wheelBound 防重复绑定）——表格横向超出容器时滚轮悬在表格上直接左右翻，滚到头或未超出时不管、交还页面正常竖滚；镜像/已删除/原表库三类网格全部受益 |
| 2026-08-25 | 第四步人工二检可读性整改（应用户反馈：第 4 步输出的所有内容字都非常小，等比缩放不是解法——放大后翻页麻烦；「剧情规划」编辑框千字长文黑字灰底费眼——根源：检查卡 0.92em、说明行 .pp-muted 0.85em、规划框 textarea_compact 0.95 倍字号 + 行高 1.2 太局促，且酒馆 textarea 字色跟主题走、浅色主题下黑字叠 black30a 半透明灰底对比度差）。规划编辑框 #pp_gd_plan 专项：id 选择器稳赢 .text_pole/.textarea_compact/酒馆全局 textarea——固定深底浅字（background var(--black70a)、color #ececec，不随主题翻车）、字号提到主字号 1.1 倍、行高 1.6、rows 10→14；修改意见框同步放开到主字号。其余阅读面整体放大：.pp-gd-check 0.92em→1em（标题 1.02em）、.pp-hit 0.92em→1em、检查报告卡 .pp-gd-report 0.92em→1em（字段名 1em）、进行中/历史剧情全文框 .pp-gd-planview 弃写死 12px 改主字号 1.02 倍 + 行高 1.55 + 限高 280→340、内置指令框 .pp-gd-builtin 12px→主字号 0.95 倍 + 行高 1.5、流式输出 .pp-gd-stream 0.95em→1em、.pp-muted 0.85em/0.6→0.9em/0.7、.pp-label 0.85em→0.95em。记忆表格页字号一律未动（用户明说现在尺寸刚好） |
| 2026-08-25 | 规划编辑框配色返工（应用户反馈：白字丑，既然背景已换灰就改回黑字）。深灰底压纯黑字对比度只有约 2.4:1 看不清，用户「看得清楚」的是原先浅灰底黑字的组合——所以底色从 black70a 深灰改为固定浅灰 #c8c8c8、字色纯黑 #000（仍是固定色不随主题翻车），字号 1.1 倍/行高 1.6/rows 14 的整改保留 |
| 2026-08-25 | 规划编辑框配色再返工：自定配色全部撤销（应用户反馈：固定浅灰底也丑，主题原生的米黄底黑字本来就很好）——background/color 覆盖删除，配色完全交还主题原生 text_pole，#pp_gd_plan 规则只剩字号 1.1 倍与行高 1.6（rows 14 在 HTML 上）。教训：用户最初的诉求只有字小，配色是主题原生的一直没毛病，不该动 |
| 2026-08-25 | 向导进度留底 + 四步自由跳转（应用户需求：测试时每次刷新页面都要重新花一次模型调用生成剧情；想随时切到任意一步、随手填字段看排版）。① 进度快照：向导状态（当前步 + 构思/事件材料/记忆与玩法勾选/生成结果/事件闸口）实时存 chatdata 新增 wizard 块（双层存储按聊天身份走，设置页导出/导入/过户整桶处理自动兼容）——刷新或切回聊天重开向导页自动回到离开的那一步，已生成未处理的规划停在第 4 步等操作；分析在途（running）碰上刷新回「分析前确认」重新发起，恢复渲染不触发联网判断预跑（restoring 标记，不无声花调用，点确认时照常内联判断）；确认采用/取消清空快照，「不保存回第 1 步」只清结果；检查报告流（reviewing/report）不写快照，不会冲掉第 4 步待处理的生成结果；research（Promise）与 busy 不入快照。② 步骤跳转条：向导进行中页首常驻 ①收集 ②事件 ③分析 ④二检 四个按钮（当前步高亮），随时互跳、已填内容与生成结果全保留，跳回第 1 步与正常入口同套（勾选从对话记忆恢复、玩法补默认）；分析/检查在途跳走后结果照常入账不抢当前页面，只 toastr 提示回去查看（startAnalyze/reviewStory 的落地与失败路径都改为仅在原页时才导航）；分析在途再点 ③ 回的是实时输出页而非新确认页，startAnalyze 加 analyzeBusy 防重复发起白花调用 |
| 2026-08-25 | 检查判定口径修正 + 规划框浅底（应用户三件事：①user 在对话里指导模型输出方向，剧情生成时被误判 OOC；②同一剧情线连续写了几十层、写了几条记忆表格，判定时结合历史摘要被误判高度重复；③第 4 步规划框要顺眼的浅色背景）。① OOC 口径：分析与检查报告两套系统提示词各加一条判定基准——只判 char 自身的问题，user 明确指示/纠正/要求改走向（含括号指令与作者式安排）时角色照做不算 OOC，用户指示优先于人设与既有走向；只有用户没指示、角色自行脱离人设/事实/关系/世界观才判（与文风重复的 user 豁免同款写法）。② 剧情重复口径：分析提示词加「同一剧情线的自然延续不算重复」——进行中剧情正是该接着写的走向，历史摘要与记忆表格里同一剧情线有多条记录只说明跨度大、还在发展，对照它们看的是推进到哪一步而非是否重复；只有把已完结、已发生并被总结过的情节当新剧情原样重演、或复刻已有记录的流程桥段才判；schema 里 plotRepeat 的 note 提示同步改写。③ 配色：#pp_gd_plan 固定浅纸底 #f4f1e9 + 深字 #211f1a（不随主题走，任何主题下都是这一套），字号 1.1 倍/行高 1.6 保留——应用户本轮点名要求，覆盖此前「配色交还主题原生」的决定 |
